/** Numerical source budget for the whole-galaxy view. The fainter
 * population stays in the diffuse integral; reducing this limit must
 * never delete light or brighten the remaining sources. */
export const OVERVIEW_STAR_COUNT = 8192;
export const OVERVIEW_RADIUS_PC = 26000;
