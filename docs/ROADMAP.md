# Roadmap & Progress

Milestones build outward-in: foundations → one star → its system → its worlds → their surfaces → the galaxy around them. Each milestone ends with something visible and a test suite locking its physics. This file is the living progress record — check items off as they land.

## M0 — Foundations ([plan](plan/00-foundations.md))

- [x] Vite + TypeScript + Vitest scaffold, layering rule enforced by test (`core/`+`universe/` free of DOM/Three)
- [x] Seed tree + PCG32 + distribution toolkit, determinism property tests
- [x] Units/constants
- [x] Branded unit types at API boundaries: enforced `Mu`/`Seconds` brands on the Kepler/orbit functions (the AU-vs-SI and days-vs-seconds bug classes), producers branding once via `core/physics/units`; length/mass/time constants carry their unit types.
- [x] Kepler solver (elements ⇄ state, any-time propagation) + tests
- [x] Noise primitives (3D simplex, fBm, ridged, warp) seeded + worker-safe
- [x] Blackbody → CIE → sRGB pipeline + temperature-color LUT + tests
- [x] Worker protocol + scheduler skeleton (landed with M5: typed request/response protocol, terrain worker pool with transferables and nearest-first dispatch, sky worker with per-seed cache)

## M1 — The Star ([plan](plan/02-star.md))

- [x] IMF sampling, MS relations, evolution stages, compact objects, classification
- [x] Rotation/activity/variability (spots, flares, pulsators) as functions of `t`
- [x] Multiplicity (flat companion list; the full hierarchy tree lands with 03's system builder)
- [x] Star model test fixtures (Sun, M dwarf, red supergiant, white dwarf, brown dwarf) + population distribution tests
- [x] Render: HDR pipeline + tone mapping + bloom
- [x] Render: photosphere shader (granulation, limb darkening, spots), corona
- [x] Render: photometric star sprites — one magnitude/color mapping shared by the backdrop, the 3D neighborhood, and the system's own stars once their discs fall subpixel; brightness follows true camera distance everywhere
- [x] Demo: seed → star viewer (orbit camera around any generated star)

## M2 — The System ([plan](plan/03-system.md))

- [x] Disk recipe, planet slots, element sampling, stability filter
- [x] Zones (HZ, frost line, lock radius); binary S/P-type placement
- [x] Belts + resonance structure (Kirkwood arithmetic), reservoirs
- [x] Fixtures + statistics tests (occurrence, period ratios, stability)
- [x] Render: system map view, live orbital motion, time controls
- [x] Demo: seed → explorable system map with placeholder planet spheres

## M3 — Worlds ([plan](plan/04-planet.md))

- [x] Bulk/interior/mass–radius, rotation & tidal state, magnetic field
- [x] Atmosphere retention/composition/greenhouse; climate EBM + albedo fixpoint
- [x] Appearance parameters (terrestrial palettes, giant banding, methane tint)
- [x] Fixtures (Earth/Venus/Mars/Jupiter/Uranus analogs) + population statistics tests
- [x] Render: planet spheres with banded giants, atmosphere limb, clouds v1 (ground-level sky scattering lands with M5 surfaces)
- [x] Demo: planet view with correct phases and star angular size (eclipses arrive with M4 moons)

## M4 — Moons, Rings, Small Bodies ([plans](plan/05-moons-rings.md), [plan](plan/06-small-bodies.md))

- [x] Satellite budgets, channels, Hill/Roche placement, tidal heating states
- [x] Ring systems with resonance gaps; ring render (phase function, shadows)
- [x] Belt cell instantiation, asteroid shapes/spins, comet activity + tails
- [x] Fixtures (Io calibration, impact moons, resonant chains, Saturn-class rings) + SFD tests
- [x] Render: comet apparitions, moon orbits with eclipse shadows, ring shadows (belt fields landed in M2)
- [x] Asteroid close-up rendering: notable belt asteroids are focusable bodies whose shape spec (ellipsoid axes, contact binaries, seeded lumps) becomes streamed terrain.

## M5 — Surfaces ([plan](plan/07-surface.md))

- [x] Cube-sphere quadtree LOD + worker chunk generation (skirts hide LOD cracks; geomorphing remains the one open M5 polish item, tracked under M7 performance hardening)
- [x] Layer stack: tectonics (Worley plate belts), craters, volcanic provinces, erosion smoothing
- [x] Deeper erosion suites: fluvial valley networks (dendritic ridge-crest carving with tributaries, elevation-scaled depth), glacial carving (freeze-masked trough cutting plus fine-relief smoothing), and dune fields (wind-aligned draa-scale transverse ripples over erg patches with sand tinting) — all pure, LOD-faded, and climate-gated.
- [x] Ice/snow + biome palettes from climate
- [x] Airless small-body surface mode: a dedicated asteroid surface field (shape lobes + saturated craters + regolith cascade to boulder scale) runs through the same cube-sphere streamer; the body stepper walks planets then each belt's landmark rocks.
- [x] Determinism/border tests + world-type fixtures (Earth-like, Moon-like, Mars-like)
- [x] Render: vertex-colored lit terrain, ocean sphere, ground-level sky + aerial fog
- [x] Scatter instancing: worker-placed deterministic boulders (with an airless large-block tail) and biosphere ground cover, instanced per tile with terrain lighting/fog and proximity-based visibility.
- [x] Demo: orbit → descend to the ground of any solid world, unbroken

## M6 — The Galaxy ([plan](plan/01-galaxy.md))

- [x] Density model (disks, halo, arms, dust) + deterministic sector sampling
- [x] Population gradients feeding star properties: thin-disk/thick-disk/halo mix drawn from the local density model, with disk radial metallicity gradient, old metal-poor thick disk, and ancient halo; shared draw sequence keeps sky photometry and generated stars in agreement.
- [x] Procedural sky from local sampling (resolved shells + Milky Way glow with dust reddening) in every view
- [x] Clusters, OB associations, nebulae — emergent from the model: giant molecular clouds are first-class objects (deterministic cells, dust- and arm-weighted, gathered into kpc-scale complexes) with anisotropic noise-carved density fields; the Milky Way glow extinguishes through them so every dark rift is a specific cloud; young groups form inside clouds and the lit natal cloud is the nebula — ray-marched through its own density field with illumination from the embedded group and self-extinction, colored by ionization physics from the hottest member (Hα → O III with temperature, blue reflection below the ionizing threshold) — baked to a sprite atlas in the sky worker; dispersed older clusters ride as bare coeval knots. Glow mean luminosity is IMF-derived; the galactic center sits dozens of optical depths deep, as observed.
- [x] The galactic nucleus: a supermassive black hole derived through the relation black holes and bulges actually obey — the galaxy's own stellar mass integrates out of the density model, a spheroid takes its share of it, and the pseudobulge branch (which this galaxy drew, as the Milky Way did) hosts a hole an order of magnitude lighter than a classical bulge would. Mass and spin then fix the geometry exactly (horizon, photon orbit, ISCO, shadow, radiative efficiency), the Eddington ratio decides between a Shakura–Sunyaev disc and a hot radiatively inefficient flow, and a nuclear star cluster wraps the whole thing in the densest stellar structure in the galaxy — visible from outside the dust layer, and a hundred magnitudes away from anywhere in the disk. Reachable: the galaxy panel's centre entry stands the camera at the hole.
- [x] Magnitude-count regression test (naked-eye counts the right order of magnitude)
- [x] Demo: galaxy view of the real 20 pc neighborhood; click a neighbor to travel — its system is waiting

## M7 — Depth & Polish

- [x] **Unified body viewer** (step 1 of the single universe renderer): planet + surface merged — solid worlds are streamed terrain at every altitude, envelopes are shader spheres, everything else (sun, moons, eclipses, rings, atmosphere, backdrop) shared in one scene.
- [x] **Single universe renderer, step 2**: star view and system map folded into the body scene — the unified viewer holds the whole system (real photospheres, planets on their orbits, belts, comets, companions) around whichever body has focus; star/system/planet are camera presets, the map is an orbit-line overlay that appears at map altitudes, and one wheel ride runs from the system's rim to a planet's ground.
- [x] **Single universe renderer, step 3**: galaxy view folded in — the 30 pc neighborhood rides in the scene as true 3D points that double as the night sky's near field (the backdrop keeps only far shells and the Milky Way glow), so every view is a preset of the one renderer and a single wheel ride runs from interstellar space to any planet's ground. Travel to a neighbor rebuilds the system in place. Open: whole-disk galaxy rendering beyond the neighborhood (clusters/nebulae land with the M6 leftovers).
- [x] Clouds over terrain: a translucent deck shell above the highest terrain, sharing the shader-sphere planets' drifting cloud field (the focus view keeps the climate the far view promised), with close-range detail fade-in and a pass-through fade. Open: cloud shadows on the ground; volumetric decks stay a later item.
- [x] Black-hole lensing: the galactic nucleus is drawn by tracing one null geodesic per pixel through the Schwarzschild metric (the Binet equation in Cartesian form — exact, no tuned bend factor), so the shadow at 3√3 r_g, the photon ring, the Einstein rings of the nuclear cluster and the accretion flow's far side lifted over the top of the hole are all one integration. The flow's temperature profile, thickness and optical depth are the model's; colour is the blackbody table read at the Doppler- and gravity-shifted temperature each patch is seen at, so the beaming asymmetry is δ⁴ rather than a dialled constant. σT⁴ across a flow runs five decades for a starved torus and eleven for a fed disc, further than a screen reaches, so the *source* alone is compressed under a display gamma derived from its own slope, to r^−1; what reaches the eye falls faster wherever the flow has thinned, because a thinning column emits less of what it holds. Nor is a disc opaque everywhere: its column is its surface density and that falls away with radius, so only the innermost disc is a surface and past a few inner radii it is gas the sky shows through — which is also what lets the clumping decide which lanes are shut and which are open. The model's cold disc reaches its true self-gravity edge thousands of r_g out, but the picture is drawn to a dozen inner radii, which holds five sixths of the light: the cold decades past that are an opaque plane carrying a few percent of the luminosity over nearly all of the area, and drawn they leave the hole a speck on a lit floor — which is why no photograph, render or GRMHD simulation shows them either. The camera arrives outside the flow it is looking at, for the same reason. The drawn edge is thinned in substance rather than in brightness — emission and opacity together — so the flow dissolves into the sky instead of ending in an opaque dark rim it does not have: at the radius drawn it is still forty thousand kelvin, and even at its true edge two thousand six hundred. The flow is not smooth: the magnetorotational instability that lets it accrete at all leaves the gas clumped on a log-normal density distribution of the width simulations measure, and the shear draws every clump into a trailing filament — so the structure is the instability's, sampled in co-rotating coordinates, not noise laid over a disc. The lensed background is the sky captured from the hole itself, so every star gets bent — inside the footprint where bending is worth what the capture's resolution costs, and no further, or a sharp star field would go soft the moment the camera crossed the boundary. Sprites drawn into that capture are scaled by its angular resolution against the screen's, since a point sprite is sized in pixels and would otherwise come back out of it fatter than the same star drawn straight to the screen beside it. Propagation is Kerr: Carter's separated equations in Mino time, exact, with each ray's constants read off the tetrad of a real observer at the camera rather than a flat-space approximation to it — which is the step that decides whether the answer is a Kerr shadow or a Schwarzschild one wearing a correction. Integrated over thousands of rays, the boundary between what falls and what escapes reproduces Bardeen's critical curve to a fraction of a percent at every spin, and the rendered silhouette agrees with the closed form pixel for pixel to sub-pixel accuracy; the deflection at large impact parameter carries the second-order 15π/4b² that separates general relativity from a Newtonian argument with the right factor of two. So the shadow is a circle of 3√3 only when the hole is still, and dragged into a D and offset from the ring at spin — 4.83 r_g looking down the axis of a Thorne-limited hole against 5.196 for a static one. The flow now reaches its own inner edge at any spin, because matter inside the last stable orbit is given the plunging geodesic that leaves it carrying the energy and angular momentum it had there: no floor, no free parameter, and the radial term switches itself off above the boundary because a circular orbit's own constants make it vanish. A thick flow is passed through rather than crossed — a starved hole's ion torus is half as deep as it is wide and translucent, so what reaches the eye is the path integral through it, which is why the ring is where the sightline runs longest through the plasma rather than where the material is. Stellar-mass holes get the same pass, and what they look like is decided by the company they keep: a hole is fed by whatever companion is close enough to lose material to it, through the wind it sheds (Bondi–Hoyle–Lyttleton capture, off a Reimers or line-driven mass-loss rate, at a speed the beta law says the gas has actually reached by the time it arrives) or through its Roche lobe once the star's own radius exceeds Eggleton's, on its thermal time if it is the heavier and its far slower nuclear one if it is not. Below a percent of Eddington the flow cannot cool as fast as it falls and its efficiency drops with its supply, so the light goes as the square of the rate — which is the whole reason a hole with nothing but interstellar gas to eat is not a catalogued class of object. Nearly all of them are exactly that, and are drawn as what they are: a shadow, a photon ring, and the star field bent into an Einstein ring around it, with no disc at all. The turbulence is not frozen: it advects with the flow's own rotation, shears differentially, and re-forms, since an MRI eddy turns over in about an orbit and at the innermost orbit here that is forty seconds. Two realisations run half a lifetime out of phase, each reseeded while it carries no weight and blended so the variance rather than the mean is preserved — the same construction the gas-giant deck uses, and for the same reason: one frozen field advected forever shears into threads that never renew. The bulk rotation is applied to both alike and never resets, so the flow's turning stays continuous through the handover. It shows on a thin disc, where the clumping is read at a single crossing; a hot torus is a path integral through a hundred decorrelated cells and averages its own fluctuations away, which is why one looks filamentary and the other smooth. Jets were looked at and not built, for a reason about scale rather than difficulty. A spinning hole threaded by a magnetically arrested flow does drive one — the Blandford–Znajek power follows from the spin and the flux GRMHD measures the arrested state saturating at, and comes out above unity in efficiency for the spin this nucleus has — but almost none of that power radiates near the hole. It is Poynting flux, and it becomes visible only where it dissipates, which for M87 is hundreds to thousands of gravitational radii out. Inside the dozen inner radii the flow is drawn to, a correctly modelled jet is nearly invisible, and making it show would mean exaggerating it. A jet is a different picture at a different framing — one where the shadow is a speck — rather than an addition to this one. Open: that wider framing; the black-hole X-ray binaries the physics is ready for are absent because a hole's companion is drawn at 0.15–0.95 of the progenitor's mass and so is always another massive star, dead by the time the hole exists.
- [ ] Volumetric clouds; multiple-scattering atmospheres
- [x] Real far starfield: the statistical shells are gone — every glint in the sky is a catalog star with a seed, hoverable and travelable like the neighborhood. Stars carry their identity (mass, age) in seed bits through the explicit IMF/population inverse CDFs, so the catalog constructs seeds for exactly the bright young stars a far cell can show while arbitrary seeds keep the same distributions; survey depth is mass-stratified (0.91 M☉ turnoff / 2.2 / 7 M☉ out to 90/150/600/2500 pc). Open survey-depth items: sub-turnoff giants beyond 150 pc, hot young white dwarfs beyond the near field, per-star dust extinction, unresolved-binary brightening; cluster/group members are identifiable but not yet individually addressable (their ages live outside the population model)
- [x] Whole-galaxy view: the galaxy renders as a raymarched volume of the same density model the sky integrates — the identical emission/dust/knee/reddening constants as the glow map, marched per pixel from wherever the camera is (fine steps across the disk slab, coarse through the halo). It crossfades in as the sky-sphere backdrop's parallax breaks down (60–450 pc out), the altitude ceiling now reaches 45 kpc, and the wheel rides toward the panned anchor at large scale so zoom goes where you look. Spiral arms, bulge, disk, and dust patchiness all emerge from density.ts; the volume's ray geometry provably matches the 3D neighborhood layer. Open polish: dust lanes aren't arm-correlated (the cloud population doesn't know about arms yet), the clump noise is the statistical limit of the cloud field rather than the clouds themselves, and no bright-star sparkle rides the volume
- [ ] Galaxy population: the universe currently holds one Milky-Way-like spiral; derive multiple galaxies (ellipticals with smooth band-less glows, irregulars, dwarfs) from parameterized density models so a seed's sky depends on which galaxy — and where in it — the system lives
- [x] Belt materialization: belts are full populations — orbital cells (semi-major-axis band × epoch mean-longitude sector, Keplerian-shear-predicted) instantiate deterministically near the camera; each member is a true body rendered as a shaped spinning rock when resolved and a reflected-sunlight glint (with a faint marker floor) when subpixel, and clicking any of them promotes it to a full streamed-terrain focus body. The additive point cloud remains the far-field statistical limit; counts follow a main-belt-normalized SFD, so belts are honestly sparse.
- [ ] Exotic showcases: circumbinary worlds, eyeball planets, Io-class volcanism, pulsar systems
- [x] Constellations: the sky chart now cuts the celestial sphere around the landmarks it actually shows, replacing the old projection of 3D territory adjacency (sight lines marched to the first neighboring province, which named the sky after territories you could not see while the visible nebulae hung unlabeled — in one probe sky, none of the seven visible nebulae anchored its own region and a border ran straight through the brightest complex). The sprite-carried nebulae and rifts — the same first-class clouds the provinces anchor on — rank by salience (angular size, emission counting beyond bulk) and seat a prominence-weighted angular Voronoi: each landmark owns its own face and reaches beyond it by stature, and one that could not hold its own heart against the seated stays a named object inside a greater constellation, the way minor nebulae live inside Orion. A sky region, the landmark at its heart, and the province that landmark anchors share one seed and name, so the chart, the hover tooltips, and the flat map finally agree — and like human constellations the cut is a viewpoint artifact: every home system letters its own sky
- [x] Provinces anchor on real landmarks: each chart territory is the region of a prominent molecular-cloud complex — the same first-class objects that carve the rifts and light the nebulae — selected by dust-mass prominence, with great complexes claiming broader reach through a weighted (power-diagram) metric and jittered frontier anchors only where clouds thin out (outer rim, halo). A province and its anchor cloud share a seed and therefore a name, so naming the nebulae in hover tooltips later is a lookup, not a feature. The map now reads as geography about the galaxy's contents instead of abstract Voronoi noise. The chart is a toggle in the control strip, and the sky's extended objects are hoverable: a nebula introduces itself by its cloud's name ("the Zutou Nebula · molecular cloud lit by its newborn stars"), a dark rift by the same naming ("the X Rift") — containment-only and last in pick priority, so they never steal a star's hover. Open: click a nebula/rift to travel toward it; cluster-member tooltips could carry their natal cloud's name
- [x] Gazetteer polish: provinces are density-weighted (extra territory sites where the stellar density runs high, so sectors run small along the arms and inner disk and sprawl in the rim and halo, capped in the core), and both charts are lettered — province names at their visible centroids on the flat map with home emphasized, and each neighboring territory's patch of sky named on the star map. The flat chart is now a local patch matching the discrete star catalog's 2.5 kpc reach, fading out at that survey horizon, so the whole-galaxy view shows a small charted bubble in an unmapped galaxy; labels withdraw with camera distance to keep the far view clean. Open: name the molecular clouds/nebulae and clusters
- [x] Galactic gazetteer, first cut: the structure the model already carries gets chartable names drawn deterministically from the universe seed — the two spiral arms have proper names, a 400 pc chart-sector grid names every locale, and the structural zones (core, arm, inter-arm gap, outer rim, halo) come from the density model itself. The galaxy panel shows the system's full address, stable across neighborhood hops and consistent for every traveler. Open extensions: name the molecular clouds/nebulae and clusters (they're seeded objects already), show sector names on the whole-galaxy view
- [ ] Ground-frame completeness: ring shadows on terrain; asteroid focuses ignore their random spin axis in the frame the way solid planets honor obliquity
- [x] Free flight, step 1: right-shift + drag pans the camera through space in every view — a screen-plane grab scaled by altitude (meters over a ridge, parsecs across the neighborhood), with altitude re-derived from wherever the camera ends up instead of snapped to the focus sphere, the terrain floor as a hard clamp, and the orbit pivot traveling with the pan (re-anchoring on focus change). Wheel ride, orbit drag, hover picking, and terrain streaming all compose with it. Scoped to the space views: on the ground (solid focus below the horizon-gaze altitude) the original surface controls stand untouched, and descending there re-anchors the orbit on the body — WASD walking is its own future feature
- [ ] Surface free camera, step 2: WASD + mouse-look movement on the ground. The ground-fixed frame is built for this — terrain vertices are static world geometry, up is radial, and the sky/frame contract (see frameQuat in unifiedViewer) keeps every sky object correct regardless of how the camera moves
- [ ] Zodiacal light: belts and comet reservoirs carry a collisional dust component — production from the belt's size-distribution cascade and the comet population, spread inward of the source belt by Poynting–Robertson drift — rendered as the ecliptic-plane sky band it actually is: a faint zodiacal wedge and gegenschein in surface and low-orbit views, emergent from belt geometry, dust column, and stellar luminosity (the frame contract already carries the ecliptic; the backdrop's band machinery is the template). The rocks stay honestly invisible; the dust is what a naked eye would actually see of a belt, and only from inside the system.
- [ ] Performance hardening (belt LOD, chunk budgets, memory eviction, terrain geomorphing)
- [ ] WebGPU renderer evaluation

## M8 — Human Scale ([plan](plan/09-human-scale.md))

Walking the worlds: to-scale terrain from orbit to the footstep, every feature a model object. Ordering: S1 → S2 → S4-graph → S3 → S4-rendering → S5 → S6.

- [x] S1 Boots: WASD + mouse-look ground camera — eye height, body-g gravity, terrain collision, slope limits; land-anywhere transition from orbit and clean exit back to it
- [ ] S2 Ground truth: detail cascade extended ~100 m → ~5 cm with per-substrate band parameters; geomorphing; normal-map micro-detail; chunk budget/eviction and local-frame precision hardening
- [ ] S3 The lay of the land: structural ridge/valley networks with consistent divides, fault scarps, talus at angle of repose, strata in cliff faces, coastal morphology (beaches, cliffs, tidal flats)
- [ ] S4 Water finds its way: global drainage graph (uplift × precipitation → basins, reaches with discharge/width/slope) as first-class model objects; valleys carved by the graph; rivers, lakes, waterfalls, deltas rendered to scale; orbital river traces and walked banks from the same data
- [ ] S5 Weather on the map: spatial climate fields (circulation bands, orographic rain, rain shadows, soil moisture, snowline) feeding hydrology and a first-class Whittaker biome field that colors every scale
- [ ] S6 The living ground: procedural plant species per biome (star-spectrum pigments), deterministic hierarchical placement, instanced trees → impostors → canopy-in-terrain-color LOD chain, undergrowth near the camera

---

## Log

- **2026-08-23** — Repository created; architecture and all level plans (00–08) written.
- **2026-08-23** — M0 core landed (seed tree, RNG/distributions, Kepler solver, noise, spectral color pipeline) and M1 star level landed (full generator with evolution/classification/activity/multiplicity, HDR photosphere+corona rendering, interactive viewer). 33 tests green; verified in-browser across K/M/A dwarfs and a red giant.
- **2026-08-23** — Corona reworked into a star-anchored 3D field: true parallax when orbiting, co-rotation with the surface, activity-driven structure over spot bands.
- **2026-08-23** — M2 system level landed: disk→slots→elements→stability generation with S/P-type binaries, zones, Kirkwood belts, reservoirs, and end-state effects (engulfment, supernova sterilization); system map view with live Keplerian motion and shearing belts. 45 tests green; verified in-browser on a circumbinary resonant chain and an M-dwarf eyeball-world system with a gapped asteroid belt.
- **2026-08-23** — M3 planet level landed: full characterization (bulk, interior, rotation, atmosphere retention/greenhouse, climate fixpoint with snowballs and biospheres, appearance) plus planet rendering (solid worlds, banded giants, scattering limbs) and a true-scale planet view with exact phases. 54 tests green; verified in-browser on a biosphere crescent, a banded gas giant, a brown-dwarf-lit mini-Neptune, and a locked ocean world.
- **2026-08-23** — M4 moons/rings/small bodies landed: satellite systems with Io-calibrated tidal heating (volcanic/cryovolcanic/subsurface-ocean states), Roche-limit rings with moon-resonance gaps, deterministic belt asteroid instantiation, and comets. Rendering: analytic eclipse shadows (moon transits, ring shadow bands), forward-scattering rings, live moons in the planet view, comet apparitions in the map. 67 tests green; verified on a ringed super-Jupiter around an L8 brown dwarf.
- **2026-08-23** — Comet legibility and moon discoverability fixes: activity-capped comas, physical dust-tail lag, motion trails; moon orbit guides and adaptive marker dots in the planet view.
- **2026-08-23** — M5 core landed: pure surface fields (plate-belt mountains, lattice-cell craters, erosion, solved sea levels, climate-driven color), worker-streamed cube-sphere quadtree terrain with floating-origin rendering, and the surface view descending from orbit to the ground. 75 tests green; verified on a crater-field Mercury analog and a locked habitable world. Open: fluvial/glacial erosion detail, scatter instancing, geomorphing, asteroid surface mode.
- **2026-08-23** — Surface shakedown from live testing: OrbitControls-based camera (no more flipped axes), seam-free border-ring normals, priority-ordered chunk requests, detail-band cascade to ~100 m features with LOD fading, chunk-aligned water tiles (ocean z-fighting gone), GPU mottling and micro-relief shading, relief-scaled color windows, minimum LOD floor for orbital views.
- **2026-09-04** — The haze band that travelled with altitude ("a band that travels across the terrain as you change elevation"): the run of air between the eye and a ground point was computed as the difference of the two vertical columns times the slant factor `dist/dh`, with that factor capped at the horizon air mass. The difference shrinks with `dh`, so beyond `horizon × dh` the product collapsed toward zero instead of the true column: from an eye 2 m up on a Titan-like world (methane haze depth 3 per vertical, scale height 1.47 km) the far ground's haze column froze at 0.39 where the exact run at 5 km is 10, and each rise of the eye moved both the frozen value and where it set in, so the haze arrived as a band marching outward with altitude — and from eye height a world that should vanish into haze within a kilometre showed its rocks to the horizon. The run is now the exact straight-line integral through exponential air — the run's length in scale heights at its lower end's density, times the mean density over the rise, (1 − e⁻ˣ)/x — capped at the lower end's tangent column, in the shader and its TS mirror; it agrees with a dense reference at every entry of a 35-point table and drops the level-run branch and its seam. Hazy worlds now look hazy at every range; that is the model's answer, not a new look.
- **2026-09-04** — The twilight sky loses its bands ("some bands are visible in the sky from the surface … I'd expect smoother gradients"): the ground dome integrates each view ray through the air in sixteen fixed segments, and two of the model's boundaries along a ray are hard — the cloud deck, which the ray crosses at one distance (view transmission through it, and the mean deck over any parcel below it), and the planet's own shadow, whose penumbra is only as wide as the sun's disc. Each segment was switched whole across those boundaries, so as the view turned and the crossing swept through the segments the sky stepped once per segment, and the band edges were the loci on the dome where a crossing sits on a segment boundary. Under a 61% water deck each switch was a large step. The dome now cuts every segment at the deck crossing and at the shadow's entry and exit (the shadow is a half-space behind the centre plane inside the planet's cylinder, drawn in by the disc's upper limb, intersected in closed form), and integrates the pieces with their own density, sight path and sun path; the zenith mirror that drives star washout takes the same cut, so the washout follows the shadow up without stepping. In a numeric mirror of the shader for that world's air, sixteen whole segments showed five step-like points in radiance against elevation at 7% from a dense reference; cut, none at 2.2%. Sampling density is unchanged, so the cost is the extra pieces only where a boundary falls.
- **2026-09-04** — Nearby star jumps reuse the sky: the star half of a sky build is now a per-cell survey — each catalog cell swept once from a centre into viewpoint-independent stars (galactic position, light, seed) with a 30 pc reach, so any viewpoint within the neighbourhood's radius projects the exact same field from it (pinned byte-for-byte in `galaxy.test.ts`) and a jump to a listed neighbour re-sweeps only the crescent of cells the move brings newly into range. The sky worker keeps the surveys across builds (budgeted by star count, letting go of the cells furthest away in units of their row's reach) and is no longer terminated to cancel a build — builds are given up by message so the cache survives the jump. Measured in a hidden tab: a fresh sky 8.3 s, a neighbour jump 1.7–2.0 s with the cached field on screen within a second; single-threaded, the home sweep pays ~15% more per fresh cell for the reach and a 10 pc jump costs a tenth of a fresh sweep. The survey exposed a catalog flaw and fixed it: a slot thinned against the density took one draw where a slot out of reach took four, so a cell's later stars depended on the sweep asking — every slot now takes the same draws, and a cell's stars are a function of the cell alone (the 3D neighbourhood and the sky's census finally agree in every boundary cell; the specific stars everywhere changed, their statistics did not). Open: the neighbourhood itself is still recomputed on the main thread at every arrival (~0.25 s at home, ~0.6 s in the inner disk) and could be served from the same surveys; the background bake, ~1–2 s, is the critical path of a nearby jump, so through a short jump (within the survey reach) the last sky's bake stays up — turned into the new system's frame and crossfaded out over a second once the new bake lands (`skyHandover.ts`) — rather than the sky going dark for the wait.
- **2026-08-24** — Single-renderer step 2: star, system, and planet views unified into one focus-centric scene (km units, focused body at the origin, everything else translated in doubles). The real photosphere is now every planet's sun — the additive sun proxy is gone — with companions on barycentric orbits, other planets at true positions with adaptive markers, belts/comets/zone rings folded in, and the orbit-line map as an altitude-triggered overlay; the sky dome went additive (scattering adds light, never occludes) and scales with surface pressure. One wheel ride runs from the system's rim to any planet's ground. Verified on a 12-planet G system (all presets, click-through, planet→system zoom-out) and a circumbinary brown-dwarf pair.
- **2026-08-24** — Milestone backlog cleared (every M0–M6 checkbox): galactic population mix feeding stellar age/metallicity; open clusters, OB associations, and nebulae in the sky (plus the galactic-bulge glow fix from live testing); photometric star sprites unified across all views; fluvial/glacial/dune erosion suites; asteroid close-ups as focusable streamed-terrain bodies; instanced boulder/ground-cover scatter; branded unit types at the orbital-mechanics boundary. Only M7 depth items remain.
- **2026-08-24** — Single-renderer step 3: the galaxy view retired as a separate renderer. The neighborhood (every sector star within 30 pc) is now 3D content of the unified scene under a pc-scaled group, photometrically identical to the backdrop stars it replaces but parallax-correct at every altitude; the backdrop carries only far shells and dust glow. Galaxy is a camera preset (focus star, 15 pc altitude, time frozen); the travel panel swaps systems inside the same viewer. Belt point clouds fade below sub-pixel size so distant systems don't bloom into false blobs; near/far planes scale into the interstellar regime. Verified: galaxy preset, neighbor travel (G dwarf → L6 brown dwarf), system→interstellar wheel-out, and the ground night sky retaining its near field.
- **2026-08-23** — M6 core landed: galactic density model with deterministic sectors, per-seed sky fields (resolved star shells + dust-reddened Milky Way glow) rendered as backdrops in every view with daylight washout on surfaces, and the galaxy view — the real 20 pc neighborhood, flyable, with click-to-travel to any neighbor system. 83 tests green. Open: population gradients into star properties, clusters/nebulae.
- **2026-08-25** — Travel became spatially honest: clicking a star now takes you to where it actually is. Destinations used to derive their locale by hashing the seed (always landing in the 5.2–12 kpc belt), so visiting a star near the core dropped you somewhere unrelated — invisible until the whole-galaxy view let you see your position. A catalog star's true galactic position now rides through travel (pick targets, the travel panel, the URL's `at=` param) into generateSystem: the destination's sky, neighborhood, galaxy view, and population mix are all built at the star's real place, and sky photometry draws each star's age from its true position (high-latitude stars now correctly skew old). Bare typed seeds keep the seed-derived locale as their address. 97 tests; browser-verified that the arrival locale matches the clicked star to 0.01 pc.
- **2026-08-25** — The band pointed the wrong way, and the whole-galaxy view caught it: the glow/rift maps are written with longitude in [0, 2π) but were sampled with an atan2 origin plus a half-turn — every interior sky's Milky Way band has been rotated 180° in galactic longitude since the glow map existed, its bright bulge end pointing anti-center, silently contradicting the far-star density gradient and the cloud sprites. Unnoticeable from inside; blatant the moment the volumetric galaxy rendered the truth beside it (reported from flight as "the angle is correct, but it's flipped" — exactly a longitude half-turn). One-line sampler fix; backdrop and volume now hand off seamlessly. Also: the far catalog stars are now true 3D points (direction × distance in the neighborhood frame) instead of a sky-sphere shell — parallax-correct at every altitude, they persist through the backdrop→volume crossfade (only the unresolved-glow representations swap), and hover/travel picks them at their real positions.
- **2026-08-25** — Moons rise and set: moon groups now carry the ground frame's diurnal sweep (spin only — the equatorial plane doesn't lean), so moons wheel across a fixed landscape like the sun and stars, lagging by their orbital rate, instead of hanging geostationary over one longitude with half of them below the horizon for weeks. The frame's spin sign flipped to prograde while wiring it: the sky had been sweeping opposite the planets' revolution, so every solid world was silently a retrograde rotator — now base spin matches the revolution (and the envelope planets' band direction), and genuinely retrograde worlds still emerge from obliquity > 90°, verified on a 150°-tilt rocky whose solar day correctly runs shorter than its sidereal day. The frame contract is documented in one place (frameQuat): new sky content parents under a frame group and is correct by construction. Considered and rejected: rotating the camera with the planet instead — a planet-radius terrain mesh spinning under float32 transforms jitters at the meter scale at ground zoom; static terrain is also what makes the planned surface free-camera easy.
- **2026-08-25** — Rings recovered on solid planets: since the unified body viewer, a terrain-focused planet's rings were added flat in the ecliptic — the very plane the sun moves in — so they sat at permanent equinox, edge-lit down to the shader's 5% floor, and read as lost (gas giants kept theirs because PlanetObject tilts its whole group). The fix models the cause: the ground frame now composes the planet's axial tilt with its spin, so the ecliptic — sun, planets, belts, sky — leans by the obliquity while rings and moons keep the equatorial plane, and seasonal sun declination emerges for free. Browser-verified on a 34°-tilt ringed rocky (ring lit and planet-occluded) with the icy-giant view unchanged. Remaining truth, not a bug: solid-planet rings are all faint dusty debris (τ ≤ 0.08) in the model, honestly near-invisible; the showpieces are the icy giants. Follow-up by request: a disclosed legibility lift in the ring shader (compressive optical-depth curve, softer illumination floor — the belt-glint marker-floor convention) so dusty rings read as a faint band, and ringed worlds now greet the camera from ~29° above the ring plane, since an in-plane arrival collapses any ring to a one-pixel sliver.
- **2026-08-25** — Real far starfield: the star catalog. A star's initial mass and population age now live in its seed (bit fields of the invertible mixed seed feeding the IMF and population-mixture inverse CDFs), and the galaxy's stellar field is realized as mass×age-stratified Poisson cells — fine cells for the faint bulk and the post-luminous slices, coarse cells for the rare bright young strata so the sky sweeps them to 2500 pc. The statistical far shells are deleted: every sky point is a real star whose seed travels, with hover showing its true spectral type and distance (a universe re-roll: all seeds keep their hex but resolve to new stars). 96 tests green; browser-verified hover identities across the sky and click-travel to a 313 pc B9V.
- **2026-08-26** — The interface became a survey console: a full-height sidebar replaces the floating info panel and bottom control strip, designed as a slate instrument — dim cool chrome with a teal accent kept below the sky's brightness, condensed-caps display type for designations and labels over mono measurements. Structure: a permanent sector-address eyebrow; a fixed catalog plate for the selected body (designation, a spectral strip smeared from the body's own model color, measurements, and the body stepper); a scrolling level table beneath it, under a sticky section bar cut like the address bar, listing the current level's contents as a real catalog (units in headers, right-aligned numerics) — companions, planets (rows click to focus), moons, or the nearest travel targets; and the four framing scales along the bottom, each annotated with the scale it frames (R☉ · AU · R⊕ · kpc). The instrument settings (seed, random, time, exposure) fold into a cog menu in the viewport's top-right corner, the chart toggle stands alone as a chip in the bottom-right, and time defaults to the slowest dial stop instead of per-view presets. The galaxy plate now reads out the full address (region, nearest arm, galactocentric radius, height); the travel table caps at the nearest 80 of tens of thousands. Browser-verified across all four levels: row-click planet focus, stepper, neighbor travel with honest at= URLs, chart toggle, no console errors.
- **2026-08-26** — Sky glints answer again at low altitude: since the far catalog went true-3D, most sky stars sit beyond the camera's far plane in the planet/star/system views (far floors at 75 pc; the catalog reaches 2500). The GPU kept drawing them — at those scales their float32 depth rounds to exactly the far plane, which three.js's less-equal depth test admits — but the CPU hover scan, computing the same projection in float64, saw z a hair past 1 and rejected them: the sky's brightest glints (distant giants, exactly the ones worth clicking) were visible yet unpickable. The pick scan now rejects only behind-the-camera, never the depth range, and the star-point shader clamps depth just inside the clip so beyond-far sky points draw by design instead of by rounding luck. Verified from the system and planet views: an F4V at 107 pc and a B8V at 273 pc hover and offer travel.
- **2026-08-26** — The sky chart became constellations. The complaint: you could see a named nebula in the sky, yet the sky chart didn't make it a region — the old layer partitioned the sky by which 3D province each sight line exited into, charting invisible adjacency while the visible landmarks went unhonored (probe: 0 of 7 visible nebulae anchored their own province; the brightest complex had a border through its face). The fix realigns the sky cut with the model's existing unification (province = cloud = nebula/rift, one seed, one name): buildConstellations partitions the celestial sphere by a salience-ranked, prominence-weighted angular Voronoi over the sprite-carried nebulae and rifts, reusing the lattice/marching/bisection border tracer (now cheaper — an angular argmax replaced the exit ray-march). Seating is self-consistent with the Voronoi cost: a landmark joins only if it would still win its own center against everyone seated, so the M78-in-Orion case falls out naturally. Tests assert every constellation label names a nebula or rift that same sky renders. Browser-verified at seed 78e011100ad27e30: the great Marusyaveim complex wears its own name, borders wrapping around it instead of cutting through, with the home province's namesake cloud (Syavelaekryak) lettering its own patch of the home sky.
- **2026-08-26** — The sky's extended objects are hoverable in practice, not just in principle: the star hover snapped from a shared 26 px radius (~17 px effective per glint), which in a catalog sky blankets nearly every pixel — the containment-only nebula/rift pass almost never ran. Point stars now snap only within 10 px (STAR_SNAP_PX; solid pickables keep their generous reach), so the space between glints reaches the clouds. And a nebula's face is dense with its own coeval members — physically honest, but they are anonymous and unclickable, and they smothered the very object they light: an unseeded cluster member now yields to a cloud that contains the cursor, standing only where no cloud claims it, while every seeded star keeps its priority. Browser-verified: the Marusyaveim complex's face hovers as "the Marusyaveim Nebula", matching its constellation, and neighboring glints still snap and offer travel.
- **2026-08-26** — Spin got its physics (a universe re-roll: generator draw sequences changed). Stars now carry a full spin axis — convective envelopes (the cool side of the Kraft break) tidally realigned to ~9° scatter, radiative stars looser, 3% wildly misaligned up to retrograde — rendered through the photosphere's orientation and read out on the plate. Activity quenches with photospheric ionization: below ~2400 K the atmosphere goes neutral and lets go of the field, so late-M/L/T objects lose their spots and flares for the physical reason, and the L/T transition carries patchy condensate clouds instead (a T6 now shows clouds, not starspots). Planet spin split by family: gas-rich worlds (giants and mini-Neptunes) spin fast, prograde, nearly upright with rare resonance-walked poles; terrestrials remember their last giant impacts with isotropic obliquity — retrograde as often as not; and near the outer edge of the despin zone thermal tides in a thick atmosphere can stall the lock into a Venus state — slow, nearly upside-down, day running backwards against its year (~2% of planets). Captured irregular moons (60% retrograde, always in the model) now render and list with their retrograde-capture notes instead of being filtered out beyond 100 planet radii. Verified in-browser: a T6 with 15% patchy clouds and no spots, a rocky HZ Venus-state world at 19.3 d retrograde under 1.13 bar of N₂, an M5V spinning backwards at 120° tilt. Deferred: asteroid spin axes in the ground frame (existing roadmap item), Mercury-style 3:2 capture never fires because close-in eccentricities damp below its 0.15 gate (pre-existing; needs an eccentricity-history-aware gate).
- **2026-08-26** — Wide binary companions exist again: setSystem skipped any companion beyond 3× the planet extent entirely — no photosphere, no glint — so a binary's partner at tens or hundreds of AU (brighter than magnitude −10 from anywhere in the system) simply wasn't in the sky, even while its truncation shaped the planet system. Every companion now gets its real photosphere and photometric sprite (only the orbit line stays map-scaled), and companions are hoverable in every view, identifying as "companion · click for star view". Companion/binary audit findings logged for follow-up: the second sun of a P-type pair casts no light on planets or terrain (climate already counts it; rendering lights from the primary only); distant binaries in the sky render as single primary-luminosity glints; companions are not separately focusable or travelable.
- **2026-08-26** — The second sun casts light: every lit shader (solid and banded planets, atmosphere limbs and ground skies, terrain, ocean, clouds, rings, scattered rocks) takes a second light direction and flux-premultiplied color, and the viewer feeds it each frame from whichever companion delivers the strongest flux — at the focus for the ground and moons, per-body for the other planets. Below a 0.4% flux threshold a companion stays a glint, not a light, so single-star systems pay nothing. Circumbinary worlds now get double terminators and their nights lit by the second sun; body shadows (moon transits) block both lights through the shared analytic occluders. Known simplifications, disclosed: star-on-star eclipse dimming is not modeled (occluder lists carry bodies only), and specular highlights come from the primary alone. Verified on an A8V+F3V circumbinary mini-Neptune — both photospheres in frame, second-light uniforms live at an 11.7° separation.
- **2026-08-26** — Binaries glint with their whole light: sky-catalog and neighborhood photometry now add unresolved companion luminosity — the same multiplicity draws generateStar would make, each companion evolved at the system age — so a binary's glint is up to ~0.75 mag brighter than its primary alone, matching what a traveler finds on arrival. Color stays the primary's (disclosed approximation); the sky build pays ~5% for it. The far-catalog acceptance floor still tests the primary's luminosity alone (a pair whose members each sit under the floor stays unlisted — edge case, disclosed). Remaining companion-audit item, deferred: focusing/orbiting a companion star directly (clicking one gives the primary's star view).
- **2026-08-26** — Companion stars became destinations: the star level's table is now "System stars" — primary first, then companions, the focused row marked — and clicking a row (or a star in the viewport) focuses that star: its own catalog plate, its photosphere at the origin, arrival orbit scaled to its radius, and a `companion=` URL param so the address survives reload. The focus machinery gained a companion target ({ companion: i } in FocusTarget); positions ride the same per-frame Kepler solutions the star nodes already use. Travel to a new system resets focus to the primary.
- **2026-08-26** — Chart lettering sinks to the back and bodies block the hover. The label sprites drew with depth-testing off at renderOrder 5 — names floated over planets and photospheres — and wore a generic monospace; they now render in the console's own condensed display face (tracked caps at 2x resolution), depth-tested so any body occludes them, and ordered with the sky domes so stars, borders, and nebulae all paint over the names, never the reverse. Picking got the same honesty: the hover previously tested occlusion only against the focus body and only for solid pickables and neighborhood stars, so far-catalog glints, and the nebula/rift pass, hovered straight through planets and stellar discs. Every star, planet, and moon in the scene now registers as a sphere occluder each frame; all four hover passes test against the list, and the extended-object pass tests the cursor's own sight line out past the whole scene (its first version tested a point at a quarter of the far plane — short of the photosphere in close views, found by hovering a star that still introduced the nebula behind it). Browser-verified in an isolated worktree (the working tree carried another session's in-flight edits): hovering the disc shows nothing, off-disc glints still travel, the open-sky nebula hover still answers.
- **2026-08-26** — Companions became systems, not just stars. Model: the planetary-formation pipeline (disk → layout → elements → stability → characterization → belts) extracted into a per-host core, and every stellar companion now runs it around itself — truncated by the primary's tide (Holman–Wiegert S-type with roles swapped), planets named for their host, coeval and deterministic from the companion's own seed. A close P-type pair's partner hosts nothing (the circumbinary planets are the pair's system). Primary systems are byte-identical (fork streams untouched — no re-roll). Viewer: each companion family rides its star as a group — zone rings, orbit lines, true-scale planet spheres, markers — lit by its own sun with any other bright star as the second light; family framing gives the system preset a per-host extent; hosted planets are pickable, focusable ({ companion, planet } focus targets), and arrive facing their own sun. UI: the current star (companionIndex, in the URL) scopes the system and planet levels, so the system table shows the K8V's seven worlds and its debris belt, rows click to visit, and the star table marks the focused star with a ring around its dot instead of an edge bar. Deferred, disclosed: companion belts render only as panel data (no belt points/streamers), comets remain the primary's, and companion-system planets skip the notable-asteroid stepper.
- **2026-08-26** — Course correction on companion rendering: no parallel pipeline. The per-companion render groups are gone; instead the ONE system pipeline (planet nodes, belt points, zone rings, orbit lines, belt streamer) re-roots on the selected star via viewer.setHost — the chart groups and every orbit solution simply ride the host's position, so selecting the K8V renders its system exactly the way the primary's renders (its belts now render too, for free), and nothing else's. Unselected companions are what you actually see at that scale: their photospheres, plus — new — their stellar orbit ellipses drawn primary-anchored like planet orbits, visible whenever the orbit map is. Focus targets simplified back to 'star' | number; the host rides the companion= URL param.
- **2026-08-26** — Stars join the gazetteer: hex designations give way to chartable names, layered the way human astronomy layers its own. The luminous few (L ≥ 10 L☉ — giants and hot stars, exactly the far catalog's bright hoverable tail) carry proper names drawn from the gazetteer's phonology, so sectors, nebulae, rifts, and bright stars all sound like one civilization charted them; the bulk file into their sector's catalog by number, Gliese-style ("Syavelaekryak 87451"). Bayer-style constellation names were considered and rejected as identity: constellations here are viewpoint artifacts, so "α Marusyaveim" would name a different star from every world. One pure function (starDesignation in star/naming.ts) serves the generator and both hover paths; components letter as B, C off the primary's name, and planets ("… b"), moons ("… c II"), comets ("…/C1"), and notable asteroids all inherit through star.designation untouched. Names are designations, not identities — the seed stays the id (a great sector can eventually repeat a catalog number, as surveys do), kept visible as a Survey id row in the star plate. Browser-verified: the travel table reads as a survey of the home province, a K2III glint hovers as "Naemiaoraer", and the plate titles "Syavelaekryak 87451 · M8V".
- **2026-08-26** — The debris-belt glow was fake and is gone: belt cloud points had a flat painted brightness whose additive stacking built a bright ring at system zoom — a structure reflecting ~10⁻¹³ of its star's light, invisible in reality (real debris disks are 10⁻⁴–10⁻⁶ contrast, coronagraph territory). Each point now carries the honest reflected-light luminosity of the large member it represents (~40 km at 10% albedo, the streamer's own formula) through the same photometric energy mapping as every star glint, with no marker floor — the belt disappears from afar and individual glints only surface as the camera closes in, handing off to the belt streamer's real rocks. Zoomed-out legibility moves to the chart where it belongs: a faint map-ink annulus alongside the zone rings.
- **2026-08-26** — The naming follow-ups: travel-table designations trim their sector prefix to three letters the way atlases write "61 Cyg" ("Sya O7G8" in the table, full name in plates and hovers; proper names stand whole), and the Bayer garnish landed — within each constellation of the current sky, the brightest glint with a seed of its own is its α, computed in the sky worker from the same partition that draws the borders and carried as a seed-keyed map into both hover paths. Viewpoint-local by nature, exactly as Earth's Bayer names are: every home system letters its own α stars. Browser-verified: the K2III giant near home hovers as "Naemiaoraer · α Syavelaekryak · K2III · 5.63 pc" — a proper name over a Bayer line, the Sirius/α CMa layering.
- **2026-08-26** — M8 S1 Boots: the wheel ride now ends on your feet. At the descent floor over wadable ground, one more scroll notch starts a touchdown glide to a 1.7 m eye height (streaming catches up on the way down); then WASD walks, ShiftLeft runs, Space jumps, click takes pointer-lock mouse-look, and a scroll notch out glides back to the orbit regime, which resumes untouched. Physics is honest and runs on the same analytic height field the meshes are built from: gravity is the body's own g with inverse-square falloff (a fixed human takeoff speed makes jump height emerge — 0.51 m on the 0.93 g Earth-twin, and on a kilometer asteroid a jump genuinely escapes, handing the camera back to orbit), grades above 42° refuse to climb, chest-deep water stops the walker, walking off an edge falls with carried momentum. Asteroids walk too, with g from taxonomy densities (new asteroidGravityMs2). Deep open water refuses to land, with the hint chip saying why; the near plane drops to centimeters on foot (mid-distance depth precision deferred to S2 hardening, disclosed). Six walker physics tests (108 total green); browser-verified on the Earth-twin — landing glide to exactly 1.700 m, 1.70/5.5 m/s paces, jump peak 0.513 m vs 0.526 m analytic, a 108 m run inland from a daylit surf line with zero eye-height error, liftoff back to the 50 m floor.
- **2026-08-26** — S2 lands its core: the ground is real at eye height. The detail cascade grows four bands (~30 m to ~24 cm features, amplitudes on the same ~1/f law), gated per substrate — sand seas, sediment lowlands and drowned floors, and ice read smooth at walking scale while highlands and airless regolith keep their rubble; all honestly derived (erg field, height-vs-relief, freeze mask). The streamer descends five more levels (MAX_LEVEL 22 ≈ 6 cm vertices) — which exposed a latent LOD bug: split distances measured to the datum sphere, so land riding hundreds of meters above datum stalled the quadtree that far short of the walker (level 16 forever); distances now measure to the local ground sphere. Geomorphing kills the swap pops without timers: every vertex carries its height above the parent-LOD surface, and the shader removes it as a function of camera distance, hitting zero exactly at the swap-in distance — a tile replacing its parent is byte-identical to it, detail grows in on approach, and adjacent LOD rings agree at their boundary by the same rule. The walker's feet low-pass the new texture (bands under a stride are stepped over, not bobbed across). Two new property tests (walked-scale texture invisible to coarse LOD; morph deltas reproduce the parent surface exactly), 110 green. Browser-verified on the Earth-twin shore: level-22 tiles underfoot, 2.7 ms frames, zero eye-height error running, the jump finally reads against sub-meter ground, and the 2,400 m highland came out snow — the lapse rate said so. Still open in S2: sub-geometry normal detail (planet-frame f32 blocks fragment noise past ~80 m wavelengths — needs chunk-local frames), velocity-aware stream priority, mid-distance depth precision (log-depth/reversed-z).
- **2026-08-26** — S4's keystone lands: water finds its way. Rivers stop being painted noise and become model objects — a global cube-sphere grid (98k cells, with seam-crossing adjacency built by uv-overstep re-projection, no hand-coded edge tables) routes every land cell to the sea by priority-flood, so depressions fill to their spill level (that's where lakes live), runoff accumulates downstream into per-cell mean discharge (placeholder latitude-curve precipitation until S5; arid eroded worlds route their paleo-discharge — the erosion parameter encodes the wet history that shaped them), and the cell→downstream segments are the river network. The graph carves the field: each nearby segment cuts a discharge-scaled valley (hydraulic-geometry widths, stream-power-flavored depth that digs hardest in highlands and grades to the sea) with a channel notch, yielding to the freeze mask; a seeded meander warp bends the straight cell-to-cell course at meander-belt scale (the first amplitude re-routed rivers 45 km — probe-caught, tightened to ~6 km). Line features fade by width but surface at half a sample, so continental rivers trace from orbit. The painted valleyRidge/tributaryRidge carving is deleted. Emergent bonus: near the coast the channel digs below sea level and the existing sea-surface tiles flood it — real estuary water with zero new rendering. One graph will feed the terrain, the orbital traces, and S4-rendering's water. 7 new grid/graph property tests (117 green); tsx-verified on the Earth twin: 6,724 river cells, max discharge 52,000 m³/s, and a 35,000 m³/s river crossing a clean 7 km-wide, 264 m-deep valley profile graded to 10 m above sea.
- **2026-08-26** — S3 lands the lay of the land, conditioned by the graph. Plates now ride at their own elevations (Worley gains id1, a stable per-cell identity): crossing a boundary steps — the fault scarp is the discontinuity itself, degraded by erosion and often buried under the belt's own ridges. River valleys grow gully networks on their flanks — ravines that strengthen toward the river and die at the divide shoulder, the sub-cell dendritic texture the 87 km graph can't carry itself. Waves work every shore: a flat beach and shallow shoreface in a ±6 m band about sea level (inactive until the level is solved, same closure trick as the rivers). Steep faces expose rock with a history: volcanic provinces show dark basalts, the rest gets elevation-keyed sedimentary bedding (seeded 35–115 m thickness, fading where vertex spacing would alias it to speckle). Performance en route: the crater field's string-keyed cache was the single hottest cost of the whole surface (54 lookups per height sample) — packed-integer keys plus merged-neighborhood caching cut heightAt 22.6→~13 µs and the test suite 16→7 s. 118 green (new beach-slope property test); strata banding probe-verified at the seeded period. Open: talus at angle of repose (nonlocal — needs its own model), wave-cut cliff vs beach differentiation by exposure, tidal flats from the moons, field creation still synchronous on the main thread (~1.4 s on wet worlds at focus).
- **2026-08-26** — Water renders to scale: rivers and lakes join the sea. The graph now computes a graded bed profile per cell — monotone downstream, using the same depth law the carve applies, so the two always agree — and the channel floor blends to it absolutely instead of carving relative depth, which means the water that fills it steps downhill reach by reach instead of stranding on band noise. Stage rides the bed at hydraulic-geometry depth with its own monotone clamp, and that clamp IS the lake model: where beds dip below the downstream-controlled level the stage flattens into a pool, so lakes emerge at the level of their breached outlet — a big river cuts through a basin rim that a small creek fills behind (the first version filled coarse-grid depressions to their raw spill and drowned a valley under 170 m of false lake; probe-caught). Water tiles take per-vertex levels (sea, lake fill, or river stage; dry vertices tuck under their terrain), the walker wades by the local water surface instead of the sea alone, landing refuses deep rivers and lakes too, and the camera floor rides lake surfaces. 120 tests green (graded-stage monotonicity; an emergent-lake property test with a rimmed basin). Browser-verified: a 9,400 m³/s river carries 1.9 m of water between sandy banks at eye height and winds as a sinuous blue ribbon from 5 km up. Still open in S4: distinct waterfalls at knickpoints, deltas and alluvial fans at outlets, flow animation on river surfaces.
- **2026-08-26** — S5's core lands: weather becomes a map. A climate field on the same global grid the drainage uses: zonal circulation bands from rotation (trades / westerlies / polar easterlies for fast rotators, one sluggish cell for slow ones) set a wind per cell, oceans evaporate by their local temperature (Clausius-Clapeyron-flavored), and moisture advects downwind to a fixpoint — raining a base fraction per cell plus a forced-ascent term. Nothing painted, everything emergent: wet windward coasts, rain shadows behind ranges, interiors that dry with fetch, polar deserts. The drainage graph now drinks the real precipitation field instead of its placeholder latitude curve, so rivers gather where the rain actually falls, and the biosphere's ground colors read moisture from the field — deserts sit where the shadows are. The Earth twin's headwater cells now span 0 mm/yr (true rain-shadow desert) to ~3,000 (soaked windward coasts), median 276. 123 tests green: rain-shadow and continental-drying property tests on synthetic worlds (a meridional wall with identical flanks separates them by wind alone), plus determinism. Browser-verified from orbit: tan desert interiors with green wet fringes where uniform green used to be. Open in S5: temperature/snowline as first-class field outputs with maritime moderation, soil-moisture coupling to the ravine/substrate gates, and formal Whittaker classes (arriving with S6's plant communities).
- **2026-08-26** — S6's core lands: the ground stands up. Tree species are model objects (universe/surface/flora.ts): two per biosphere world, seeded per species — conifer or broadleaf habit, trunk height, canopy spread and taper, blob count — with pigments derived from the world's own land palette, which already carries the star's spectrum. The renderer grows low-poly geometry from the recipe (bark and canopy colors baked per vertex, instances tinted by a neutral tone; the scatter material learned vertex colors). Placement rides the climate field: tree probability follows local precipitation through the same deterministic per-tile scatter streams as the rocks, so forests close in on wet coasts, thin to lone trees at the desert edge, and vanish in the shadows — the biome, standing up. Browser-verified in a rainforest cell: 9,400 tree instances drawn across the visible tiles, walked under at eye height. 124 tests green (species determinism + forests-in-the-rain property). Open in S6: undergrowth instancing near the camera, impostor mid-LOD and true canopy-in-terrain-color far LOD, wind sway, tree collision for the walker, and a skylight ambient term so canopy undersides stop reading black.
- **2026-08-26** — The spectrum pass, on the m8-human-scale branch (all M8 implementation moved here off main pending feel-approval): the mid-scale was starved. Between ~100 m and ~25 km wavelengths — where Earth's feel lives — amplitudes gave 0.3–1% slopes against Earth's 5–20%, tuned once for orbital smoothness; that was the "vast spans of not much going on." The falloff now runs much flatter through the middle (25 km band 40→160 m, 4 km 12→79 m, 790 m 4→30 m, 158 m 1.2→9.4 m, down to 24 cm features at 5 cm), landform bands ridge-blend along noise zero-sets into connected ridge-valley systems (free — same noise eval, shaped), and two vector warp fields (~120 km and ~3 km) shear the band domains so hills sweep and flow instead of sitting isotropic. The LOD fade divisor relaxes 4→2.5 — the geomorph absorbs transitions now, so detail arrives a level earlier and the finest band actually shows at max LOD. Land slope medians after: 18% at 5 m, 11% at 30 m, 5.9% at 200 m, 2.6% at 1 km. The beach test was rebuilt on medians — some coasts are honest wave-cut cliffs and should stay steep, and its old inland control sampled underwater. 124 green. Eye-height verified: hummocky flowing ridge-and-swale ground with shaded hollows where a featureless sheet used to be. Known limit for the from-altitude read: 2–6% plains under a high sun are physically subtle without cast shadows — terrain shadow mapping is the next legibility lever and its own feature.
- **2026-08-26** — Course correction on the spectrum, per feel-test: resolution, not exaggeration. The raised amplitudes made 0.7 m hummocks every 5 m — a half-meter jump cleared the terrain sea and read as hundreds of meters of altitude. Amplitudes recalibrated to Earth-reference relief per wavelength (steppe ~1 m over 30 m, ~15 m over 800 m, ~50 m over 4 km; 5 m median slope 9.3%, 30 m 6.4%, 200 m 3.5%), keeping the ridge structure and warp flow. The resolution budget went where it belongs: tile meshes densify 48→64 vertices per edge — ~4 cm spacing at the walking floor and a sharper mid-distance error angle at every LOD ring (chunk cap trimmed to keep GPU memory in its old envelope). Two tests rebuilt on the way: the walked-texture property moved to second differences (mid-band gradients now register at every LOD, so first differences no longer separate the fine bands) and onto the lightly-cratered fixture (crater walls survive all LODs by design). 124 green; eye-height verified — fine-grained ground over rolling relief, jumps back to human scale.
- **2026-08-26** — Ride out: a chip beside the chart toggle starts a hands-free pull-back from wherever the camera stands — a planet's ground included — to the galaxy frame. It drives the existing wheel ride rather than a separate camera path: each frame multiplies the pending wheel factor at a constant pace in scale decades (0.6/s — ground to galaxy ~28 s, a system view about half), so every crossfade, focus rule, floor, and ceiling behaves exactly as manual zooming does, and orbit drag still steers mid-flight. The wheel, a travel, or a second press takes the ride back; it ends itself at the altitude ceiling and the chip clears. From a walking start the first push clears the walker's liftoff threshold explicitly, whatever the frame rate. Browser-verified from a planet's face: terrain → orbit lines → constellations → the charted bubble adrift in the galaxy disk, chip active for the duration and self-clearing at the top.
- **2026-08-26** — The non-Earth audit: the spectrum had only ever been judged on the wet worlds, and everywhere erosion was low it was catastrophically wrong — airless cratered moons ran 900–5700% *median* slopes at a 5 m step (a 35 km moon carried the full 26 km relief cap), arid and icy dead worlds 110–130%, magma worlds 65%, against the real Moon's sub-10%. The model's only smoother was fluvial erosion, so airless worlds kept full fractal roughness to sub-meter scales; reality mantles every old surface. Mantling is now first-class: each detail band is capped at the grade its wavelength can hold — bedrock repose on fresh surfaces easing to a mantled ~10% below a gardening wavelength that grows to basin scale (200 m → 20 km) with crater age, since Gyr of impact gardening relax relief out to the megaregolith's kilometers; young unmantled crust (fresh volcanics, actively resurfacing airless worlds) deliberately keeps blocky 25–45% fine texture. Crater populations get degradation states (later impacts garden earlier bowls toward flat) instead of rendering every bowl fresh, and relief is capped at 8% of the body radius — beyond that it's shape, not terrain. Wet and hothouse worlds are numerically untouched (every tuned Earth-twin band clears its cap; probe medians byte-identical), and the archetype probe now sweeps living/dead-ocean, hothouse, arid, ice, magma, and airless worlds across four system seeds. 124 green. Browser-verified: a Pluto-amber cratered ice world walks as rolling mantled regolith under a rugged ridge horizon with visibly varied crater degradation from orbit; a young airless stagnant-lid world stays knobbly and harsh by design; the Earth twin is pixel-for-pixel the accepted steppe.
- **2026-08-26** — Shipped to the web: a GitHub Pages workflow builds main (tests, then vite build at /universe/) and deploys dist; the stylesheet moved from a JS import to a head link, deleting the flash of unstyled chips before the app module loads; and a first-visit welcome card introduces the survey — one deterministic seed, real physics, the sky as the model, the gazetteer — with an Explore button, dismissed once per browser. Verified against the built bundle via vite preview at the Pages base path: workers load, the dialog greets, dismisses, and stays dismissed.
- **2026-08-27** — Flight replaces walking, moons take focus, and moonlight arrives. The ground regime is a camera now, not a body: one notch past the wheel floor hands over to free flight (W flies along the gaze, A/D strafe, Space/C rise and dive, Shift boosts, one notch out leaves), with speed scaling by height above terrain — valley floors at walking pace, ranges in a keystroke — and a hard 2 m clamp over ground and water both, so open ocean stopped refusing visitors. Moons are destinations: ?planet=N&moon=M, a click on a moon in the sky, or the (now clickable) moon list all land the same terrain streamer on the moon, with the whole moon system shifted so the focus sits at the origin — the parent planet hangs at its true offset with rings and phases, and a tidally locked moon holds it fixed in the sky by construction. And the night has light: the brightest sunlit body in the sky becomes the surface's second sun with direction, phase, and flux from true geometry (albedo × (R/d)² × Lambert phase — ~2e-6 of sunlight for a full-Moon analog), displayed through a scotopic lift (ratio^0.2) that fades in as the sun sets, disclosed as eye adaptation. Browser-verified on a found Earth-analog pairing (seed 00000000000b8ef5 p1: 19.5 h day, 80% ocean, biosphere, a 0.7°-wide moon at 3× full-Moon flux): amber gibbous disc among the stars, moonlit ground texture under a clear night, a moonlit overcast deck glowing from above, daylight untouched; and on its arid sibling's moon, the parent super-earth hanging over the horizon. 131 tests green. Eclipse shadowing of moonlight (a moon in its planet's shadow still shines) is a known open item.
- **2026-08-27** — Travel desync fixed (user-reported: "you don't go/view what you clicked"). Two bugs compounding: clearSystem duplicated clearHostContent's disposal without resetting hostIndex, so setHost's change guard skipped rebuilding the destination system — planet clicks focused a notable asteroid out of an empty planet list, and star focus sized the camera off the previous star (once parking the view *inside* a 258 R☉ giant's photosphere, which read as "teleported but can't see it"). And the galactic locale — part of a seed's generative identity — was dropped on any same-seed reload (tab switches, body steps), silently regenerating a different star at the seed-default locale, which is why the info panel and renderer told different stories until a page reload restored the &at= URL param. Host teardown now goes through clearHostContent, and same-seed reloads keep the current locale. Browser-verified on the reporter's exact repro: random seed → click sky star → System tab → click planet → the ringed gas giant on screen is the one on the plate.
- **2026-08-27** — Follow-up to the travel fix (user: "orbit markers didn't load after travel"): not staleness this time — the orbit overlay's map-heights gate (25 stellar radii) is unreachable inside a giant star's own system, and the luminous travel-tail of the sky is mostly giants, so every traveled-to giant hid its orbit map. Gate now capped at half the system extent; verified on the same 258 R☉ system (zones, belts, orbit all surface) with dwarf systems pixel-identical. Swept the other radius-scaled gates (horizon gaze, pan availability, near plane, rotate speed) — all safe on giants.
- **2026-08-27** — M8 merged to main and pushed; rocky worlds signed off. Next pass planned: gas giants ([plan](plan/10-gas-giants.md)) — the atmosphere becomes a first-class circulation model (jets from the Rhines scale, bands with per-jet drift and shear turbulence, a deterministic storm catalog with lifecycles, polar cyclone regimes, aurora from the magnetic model, locked/quiescent regimes for hot Jupiters and ice giants) and the shader becomes its renderer, replacing today's eight-number sine-stripe banding.
- **2026-08-27** — The gas-giant pass, G1–G6 on the gas-giants branch ([plan](plan/10-gas-giants.md)): giants are circulation, not wallpaper. deriveCirculation runs a one-shot PV-mixing spin-up per planet — seeded convective stirring homogenizes potential vorticity in patches one local Rhines length wide (eddy-velocity closure: the arrest scale, which is why feeble interiors like Uranus really carve many narrow faint bands), with radiative restoration so the staircase equilibrates, a gauged inversion for the wind profile, mixing efficiency scaled to the forcing, and a deep equatorial jet the flow relaxes toward (prograde warm, retrograde cold — the solar system's split). Bands classify zone/belt from their shear; a chromophore draw gives each planet its own palette; storms are recurring catalog slots whose live population is a pure function of (seed, t); poles carry cyclone clusters, hoods, and seeded hexagon-analog waves; aurora reads the dynamo. The shader renders these as uniforms: per-band drift (bands genuinely shear past each other in sim time), festoons boiling where the model says the shear is, churn at the convective decorrelation rate, sub-band striping for the physics' surplus bands, aged storm ovals, the polar regime, tilted-dipole auroral ovals on the night limb, thermal glow, and a locked regime with superrotation-shifted hotspot and west-terminator cloud crescent. Focused envelopes take their moons as eclipse casters — transit shadows crawl the deck. 142 tests green. Browser-verified: a Jupiter-class ammonia giant with irregular zones/belts, festooned edges, polar aurora, and visible deck evolution at 0.5 d/s; a 999 K giant with alkali bands and thermal rim; a locked pale streaked mini-neptune; a teal methane world whose pole-on night side shows the auroral oval as a glowing ring; the full banded parent with rings hanging in a volcanic moon's sky. Open: visual confirmation of polar cyclone vortices and a live moon transit; fine-band striping tuning on cold worlds.
- **2026-08-27** — Century weather, per feel-test ("are we simulating the large century-scale storms?"): the spot analog was eternal and static — now it runs a century arc (swell fast, shrink through a long maturity as the GRS has, pale, die, successor at a rehashed latitude), condensable-rich giants erupt planet-circling white storms once per orbit (the true year, plumbed from the elements — Saturn's Great White Spot habit), and belts run decadal fade-and-revival cycles under fresh white deck (the SEB's). All pure functions of (seed, t). Also from the same feel-test round: the deck became a stirred tracer — multi-scale eddy displacement of the band lookup itself, so belts curl into zones and band widths wander with longitude; boundaries feather at a width set by their jet's shear (strong jets hold crisp fronts). 145 tests green; browser-verified a mature faded spot riding a stirred band on a white-dwarf system's ringed giant.
- **2026-08-27** — Depth, per feel-test ("flat and 2D"): the deck gained a cloud-top height field inherited through the color (bright decks high, belts deep, storm heads towering, cyclones sunk), bump-lit via screen derivatives so the terminator rakes across real relief, plus a slow-drifting high haze that thickens with the slant path toward the limb. The giant reads as a ball of weather with a floor and a ceiling. 145 green.
- **2026-08-27** — Polar fix, per feel-test ("central dot with smaller dots around it, like indents"): the cyclone gaussians — embossed into dimples by the relief lighting — became spiral-armed vortices with individual phases and drift rates, and the cap keeps its weather, shearing the deck into a converging polar spiral instead of airbrushing it flat under the hood. Pole-on now reads as Juno saw it: streaked convergence, a crowd of small cyclones ringing the central one, the auroral oval around everything. 145 green.
- **2026-08-27** — Resolution feel-tests: the polar cap's spiral was genuinely lower-frequency than the equator (hand-rolled polar coordinates collapsing at the pole) — replaced by differential rotation winding the true deck noise into converging filaments at full detail. Footprint-gated micro/ultra octaves keep the whole deck (storms and caps included) resolving as the camera closes, storms gained wound spiral cloud lanes, and the aurora became magnetic-frame rayed curtains — thin bright core, faint glow, flickering filaments — instead of frequency-9 fuzz. 145 green.
- **2026-08-27** — Polar cap artifact hunt closed: samples crossing the pole (kaleidoscope ring), jet anisotropy smearing azimuthally where no jets exist, and winding shear grinding noise into moiré — fixed by damping the stir with the jets, fading to isotropic cap turbulence, winding only the large structure, and noise-wound cyclones. Aurora became magnetic-frame rayed curtains. Pole-on close approach now holds together at every zoom. 145 green.
- **2026-08-27** — Cap texture closed out ("orange skin in the centers"): azimuthal two-tap smears cap grain into curved filaments, cap relief halved, and cyclones swirl gently (<1 rad) while modulating the underlying texture multiplicatively — the fingerprint-fringe discs and embossed dimples are gone. Pole-on close-ups now hold: arcs, knots, curtain rays. 145 green.
- **2026-08-27** — The poles are earned: cyclone polygons vs single eyes now split on spin × convective forcing (census over 146 giants: 10 polygons, 14 pairs, 122 lone vortices — Jupiter-crowds rare and deserved), and the hexagon analog stopped being a coin flip: the cap snaps to the poleward-most strong jet of the spun-up profile, the wavenumber is that jet's stationary Rossby number (m = R·cosφ·√(β_eff/u), curvature-steepened — Saturn's real numbers yield six), granted only to outlier jets (~1 giant in 11) and carried by the hemisphere that owns the jet. Earned waves draw a crisp boundary and their own bright cloud lane; browser-verified a wavenumber-4 rounded square around a cold sage world's north pole. 147 green.
- **2026-08-27** — Architecture turn, per feel-test ("how much of this is from forcing ourselves into one impl route?"): the giant deck moved from per-fragment analytic noise (no antialiasing, pole singularity, a new artifact at every zoom) to a baked mipmapped cubemap per giant — uniform texel density ends the polar problems structurally, mips end the shimmer/moiré/orange-peel class wholesale, and two bakes at nearby sim times crossfade so the weather keeps moving (cadence from the jets' own drift). Sampling stays live for lighting, relief, haze, thermal, aurora, and eclipses, plus one seam-proof procedural octave up close. The port fixed the band-drift seam (feathered like the colors), storm cutout edges, and fade steps. Browser-verified on the worst offender: clean night side, soft continuous close-ups, and a pole carrying its wavenumber-4 standing-wave lane at full equatorial detail. 147 green.
- **2026-08-27** — Bake follow-ups from feel-test: cube face bases had a spurious t-flip (four faces mirrored → dotted triangular seams — the framebuffer's row 0 is t=0, no flip belongs), planet-circling eruptions rendered as degenerate oval outlines (now churning band segments), and fine-band striping ringed the poles (now fades with the jets, winding ramp widened). Verified clean on equatorial and polar views. 147 green.
- **2026-08-27** — Streaks became flow: the deck texture smears along the local streamline in the bake (four taps, length from the jet speed, curvature inherited from the stir) instead of anisotropically scaling noise — "dragged gas, not stretched pixels," per feel-test. 147 green.
- **2026-08-27** — Stars-through-planets fixed (feel-test from a moon's surface): all background content (star points, backdrop, galaxy volume, chart lines) moved from the transparent pass to the opaque pass at its early renderOrder — mid-distance bodies and parsec-distant stars both quantize to the far plane, so the depth tie let later-drawn stars paint over planet discs and flicker on zoom. Draw order now guarantees occlusion; the depth-precision hardening (log-depth/reversed-z) remains the deeper roadmap item. 147 green.
- **2026-08-27** — Reversed-Z lands ("wicked glitchy": a star z-fighting through its eclipsing planet in shards, chart lines dashing across discs): the renderer runs a reversed-Z depth buffer via EXT_clip_control — quasi-logarithmic precision from centimeter near plane to parsec far plane, no shader changes needed. Mid-distance body-vs-body occlusion (eclipses, transits, chart lines behind discs) is depth-correct again; this retires the long-standing log-depth/reversed-z hardening item. 147 green.
- **2026-08-27** — Reversed-Z stabilized: the opaque-pass background move is reverted (three forces NoBlending whenever transparent is false — it killed every sky fade and left the inside-out star map visible at galaxy scale; reversed-Z makes the draw-order guarantee redundant), and the deck baker got a real OrthographicCamera (reversed-Z updates the projection inside setProgram; the bare Camera had none and black-screened the app). Browser-verified: galaxy-scale fade restored, and from a moon's surface the parent giant's night disc occludes stars with chart lines terminating at its limb. 147 green.
- **2026-08-27** — Shader clocks fold ("the orange peel comes back if you speed up time," feel-test): every time uniform now uploads folded into a 512-day window computed in float64 — unbounded churn/advection/granulation phases were quantizing float32 simplex noise into grit within weeks of fast-forward. Storms, fades, and orbits keep the absolute clock CPU-side; a fold lands inside one deck crossfade or one statistical shuffle. Giants, rocky clouds, star granulation, corona, and belts covered. 151 green.
- **2026-08-27** — Turbulence regenerates what the jets shear: the deck's advected noise ran on one frozen grid, so differential drift stretched it without bound — by t≈130 days the latitudinal correlation fell sub-texel and band edges tore into stitched concentric arcs (the fold bounded the precision, not the shear). Advection now runs on two staggered 32-day ages, each phase reseeded while weightless and blended variance-normalized: a parcel drags at most half a cycle of shear before fresh eddies replace it, motion stays at true drift speed through the crossfade, and the cycle divides the fold evenly so weights never step. Deep-time decks match day-five quality; browser-verified at the 50/50 worst case and t=2e6 days. 151 green.
- **2026-08-27** — Molten worlds got their own tectonic style ("the surface of this moon seems extreme," feel-test on a tidally-heated moon: cliff-grade chaos everywhere — 102% RMS slope at a 50 m step, 13.5 km of relief on a 3490 km body): the 'magma' regime now maps to 'molten' instead of hiding inside 'stagnant'. A lid floating on melt supports little topographic load (relief ×0.16 — Io-like plains under modest volcanic swells), resurfacing outruns the impact flux (crater retention 0.35 → 0.02; Io shows zero craters), and the surface is paved rather than mantled — sheet flows flood every wavelength out to whole flow fields, and only the newest flows keep blocky meter texture. Measured after: 10% at 50 m, 5% at 500 m, 26% flow-blocky at walking scale. Browser-verified at raking light: rolling paved plains, flow ripple, boulders, long soft swells. 151 green.
- **2026-08-27** — Magma seas became real seas (feel-test: the molten world glowed with crack networks from afar but landed as a matte rocky ball): the climate now derives an exposed-melt fraction for magma worlds — crust closes over as the heat flux falls toward the 2 W/m² threshold, irradiation past the silicate solidus melts it open from above — and that coverage floods through the same solved-level machinery as water. The surface view gets magma tiles on the water-tile stream: chilled plates over an incandescent crack lattice, melt pools clustered along active provinces like paterae, self-lit day and night, footprint-faded so orbit sees calm glow instead of speckle. The distant sphere now glows where its model seas are (ocean mask × melt emission) instead of painting cracks everywhere; basalt shores stay dark, flooded ground reads chilled basalt, and nothing rains on a molten world. Verified near and far on the tidally-locked 1800 K lava world and its moon view: a 95%-melt ocean with dark crust rafts, one story at every distance. 152 green.
- **2026-08-27** — One world at every distance ("no resemblance between the two styles," feel-test from a magma world's moon; "i also hate this perlin worms look"): the distant solid-planet material stopped painting its own fbm continents and now samples a cubemap baked from the body's real surface field in a background worker (rivers skipped — sub-texel from orbit) — coastlines, deserts, ice caps, and magma rafts are the focused view's own. The worm-ridge lava lattice (1−|snoise| zero-sets) is dead, replaced by Worley cellular plates: polygonal chilled rafts with melt in the seams, the geometry a lava lake actually has. The magma pattern is one shared GLSL function evaluated in the planet's kilometer frame with the planet's seed, so walk-up tiles and the distant sphere put the same plates and paterae in the same places; a prominence-priority job queue with a bake cache keeps focus rebuilds instant. Verified: the lava world shows the same crust rafts from its moon as from focus, and the Earth-twin's continents survive the chunk→sphere handoff. 152 green.
- **2026-08-27** — Two feel-test bugs from moon views: (1) planetshine brightness tracked the camera — the scotopic lift's nightness gate read the sun elevation at the camera's sub-point even from orbit, so the second light visibly brightened over the night hemisphere; adaptation belongs to a ground observer, so the lift now fades out with altitude (gone by 0.6 R) and the physical flux is all that remains from orbit, camera-invariant (verified: identical second-light luminance from four camera positions). (2) Total black frames under sped-up time: once the sim step outran the 0.02-day bake window, every giant's deck hit the "time leapt" branch and fully rebaked A and B every frame — measured 6 bakes/frame (two 256s + the 1024 parent, 130 ms/frame). The window now stretches to ~4 sim steps with a seeded stagger so a system's giants roll on different frames: 0.6 bakes/frame, 12 ms/frame at the same time scale — fast time coarsens the crossfade instead of stalling the GPU. 152 green.
- **2026-08-27** — The lava found its look (feel-test: "i still dislike that lava look", with a flow-noise reference): the melt pattern rebuilt on the classic flow-noise construction — nine sin-ridged octaves from ~180 km channels down to ~0.7 km filaments, each octave's domain displaced by a time-curling gradient field so the melt churns and streams zonally, mapped through a reciprocal palette that runs white-hot in the filament floors against dark cooling crust. Octaves fade at their own Nyquist limit with their means substituted, so the character holds from orbit to walking height with no quilting and bounded cost ("zoomed out some and all the details get lost" — the three macro octaves answer that, flow-free since displacement is invisible at their scale). The polished-plastic glint is gone too: chilled crust is matte, only hot melt keeps a sheen, and the flow pattern bumps the specular normal so the glint breaks along the filaments. Worley plates and their cellular-noise module deleted. 152 green.
- **2026-08-27** — Comets got real (feel-test: "currently they're getting a huge sphere around them"): the shaded coma ball and 1-px tail lines are gone. The coma is now an expanding-gas billboard — 1/ρ column density, condensed core, flux-conserved so a resolved coma is as faint as a real one — that clamps to a few pixels at system zoom as the findable marker. Tails are camera-facing ribbons: the ion tail pins anti-solar with slow kinks riding down it, and the dust tail's curve is honest syndyne physics — each sample is grit released along the true orbit days to weeks back and pushed outward by radiation pressure (β ≈ 0.3) ever since, so the arc between anti-solar and the reversed path emerges from the orbit's own history. Model fix underneath: the "pinned near perihelion" comet sat years past perihelion on near-parabolic periods (mean anomaly 0.05 ≈ 7 yr out — inactive); the pin now lands inside the active arc computed from r = a(1−e·cosE), and the test asserts activity at epoch, not anomaly. Verified: curved dust fan and straight blue ion tail from system view, marker dot at far zoom, and a tailed apparition in a planet's sky — correctly occluded by the disc when behind it. 152 green.
- **2026-08-27** — Comet feel-test round: the tails really were facing each other (~150° apart) — the dust tail's along-track "lag" term pulled samples 35% of the way back along the orbit arc, and near perihelion that dwarfs the radiation push a hundredfold, swinging the tail anti-motion. The lag now scales with the push itself (second-order, along the reversed emission velocity): measured after the fix, the ion tail sits 0.2° from anti-solar, the dust tail 30° off toward the trailing side — Hale-Bopp geometry. Findability at system zoom: tail ribbons floor at ~3 px wide with the squeezed light concentrated into them (the star-glint photometric convention), so the streak reads from anywhere without brightening the resolved close-up. 152 green.
- **2026-08-27** — Comet tail length sanity ("they're as long as systems are wide"): the system-spanning sweep was the syndyne approximation leaving its domain — every grain is anchored to the current head plus a push along the sun bearing at its emission time, and near perihelion that bearing sweeps fast, so old grains got pushed in wildly rotated directions and the tail curled into a returning hairpin arc no photograph shows. The emission window now caps where the bearing has swept ~35° (from the orbit's own angular rate), and the ion cap eases to 0.9 AU. Measured after: dust 0.066 AU, ion 0.43 AU at a 1.87 AU apparition — real comet proportions (Hale-Bopp dust ran ~0.3 AU; Hyakutake's ion tail 3.8 AU), which in a compact system still legitimately rival the inner orbits. 152 green.
- **2026-08-27** — The rocky-planet arrival freeze is gone ("a little UI freeze that happens when you first visit a rocky planet"): profiled at ~1.4 s of main-thread stall, almost all of it the 98k-sample cell-height sweep feeding the climate/river grid — work the terrain workers each redo anyway. The main-thread field now builds with a deferred grid (33 ms measured in-browser, down from ~1450), and terrain worker 0 ships its finished grid products back as a transferable survey — heights, temps, precipitation, flow routing, graded beds — which the field wraps in query closures for 0.1 ms. climate.ts and drainage.ts split into build and wrap halves so worker and main thread construct the identical objects from the same arrays; a shared memoized cube grid drops the per-field neighbor-table rebuild. New test pins the deferred+survey field to exact equality with the full build. 153 green.
- **2026-08-27** — The corner grew a decal switchboard ("extra controls for decals like the orbital lines, next to the chart toggle, and use icons instead of text"): four icon toggles at bottom-right — orbit lines (planet, moon, and stellar traces), zone rings and belt annuli, the marker spheres that carry subpixel bodies, and the sector chart — each with a native tooltip and lit while its layer shows. The zone rings and belt annuli moved into their own overlay group so orbits and zones switch independently; moon orbit traces gathered into a group under the moon frame so the orbit toggle reaches them too. Comets keep their marker clamp regardless — the coma's flux calibration is tied to it, and a comet with no marker is a comet you can't find. The text CHART chip is gone; RIDE OUT stays a worded action chip beside the icons. 153 green.
- **2026-08-27** — A generation lamp above the decal switchboard ("some indication that generation is happening"): a slow-turning arc with a line naming what the background workers are producing — terrain tiles in flight from the chunk streamer, distant-world bakes holding the surface baker, the focused world's climate survey, and sky fields still building. Polled from the viewer's generationStatus every 250 ms, lingering one poll past idle so single-tile blips don't strobe, faded out entirely when the universe is caught up. 153 green.
- **2026-08-27** — The sky stopped depending on where you look (feel-test: "the visible stars change based on the camera's rotation... nothing is blocking them"): the star-point shader's depth rescue — clamp z just below w — was written for forward-Z, and under the reversed-Z pipeline (far plane at z = 0) it became a no-op, so every catalog star beyond the camera's far plane clipped on view depth. The far plane cuts along the view axis, not radially: a star just past it vanished at screen center and reappeared near the edge, so rotating the camera reshuffled the sky — measured 10,040 vs 9,849 of 63,289 far stars surviving along two view axes, meaning ~85% of the resolved catalog sky was also silently missing at the 75-pc far floor, in every view including planet nights. The clamp is now reversed-Z-aware (pin z into (0, w]), shared by the system glints, neighborhood, and far catalog through the one star-point material. Verified: the same camera frame went from a sparse shell to the full catalog field, identical density at every rotation. 153 green.
- **2026-08-27** — Clicking a star in the sky now arrives at that star ("it'll try to focus what the previous one was on, so planet for example"): the travel pick kept the previous view mode, so arriving from planet view focused the destination system's planet at the stale index instead of the star clicked. Neighbor picks now switch to star view with the body indices reset; the galaxy map keeps its own framing so neighbor-hopping stays on the map. 153 green.
- **2026-08-27** — The spiral arms stopped being drawn with a compass (feel-test: "the arms are too clean, too uniform, it just doesn't feel organic"): the arm enhancement grew from one constant gaussian on a perfect log spiral into a structured profile — ridges wobble off the spiral in sweeping bends, the second arm sits 0.22 rad off point-symmetry, widths breathe, amplitudes run in slow bright segments studded with compact star-forming knots at half the body's width (arm 0 stronger than arm 1), five short steeper-pitch spurs feather the interarm space, everything emerges smoothly from the bulge, and the dust disk gains narrow lanes hugging each arm's inner edge. All of it is fixed sinusoid sums in the winding phase, so the CPU model and the volume shader compute the identical galaxy — no noise tables to keep in sync. Iterated offline against a face-on preview that replicates the volume march (z-integral, knee, reddening) after two in-engine rounds proved the first tunings too timid; one lesson kept: cloud-clump extinction must stay arm-neutral or the arms eat their own light. Downstream for free: molecular clouds now chain along the lanes, arm-knot locales carry twice the neighborhood (one test seed's address flipped to "the Krodya Arm" with a visibly richer sky), and the catalog ceiling rides a new ARM_BOOST_MAX = 6.1 bound (sampled max 5.45). 153 green.
- **2026-08-27** — Decal ink brightened ("it's pretty hard to see the orbit lines and other decals if the sky is dense with stars"): the organic-arm change made on-arm skies twice as crowded, and the map layers washed out against them. Orbit lines went 0.45→0.75 opacity with lighter strokes (HZ green and slate both), stellar orbits 0.3→0.55, moon traces 0.22→0.45, frost and tidal circles up to 0.55/0.5, the HZ fill 0.09→0.14, belt annuli 0.05→0.09. Verified legible over the Krodya-Arm test seed's dense sky. 153 green.
- **2026-08-27** — The generation lamp grew into a proper readout ("a progress bar type element" under the framing scales): the corner spinner is gone, replaced by a panel just above the sidebar tabs (which stay anchored while it grows) with one row per background producer — climate survey, terrain tiles, distant-world bakes, sky fields — each a label and a thin accent bar. Countable queues fill as they drain with burst tracking (the total grows with discovered demand, the fill is done-over-peak — the honest shape of a streaming queue), one-shot builds sweep side to side, a finished bar holds at full for a beat before its row folds away, and the chunk streamer now reports its full outstanding demand rather than just tiles in flight. 153 green.
- **2026-08-27** — The clock and the seed moved onto the viewport ("click to copy seed... a dice icon... an improved timescale slider with scales, as well as the option to pause"): a new instrument strip in the lower-left — pause/play chip, the time throttle on a labeled log scale (real time · 1m · 1h · 1d · 1mo · 1yr · 10y per second) with a live rate readout that names its natural unit, the seed as a click-to-copy chip with a "copied" flash, and a dice for a fresh universe. Pausing zeroes the sim rate and survives view changes; dragging the throttle while paused resumes. The settings cog keeps only the exposure dial. 153 green.
- **2026-08-27** — Time throttle detents ("snapping on the slider while dragging would be good, too fine how it is now"): the slider snaps to a ladder of 18 useful rates from real time to a decade a second (rt · 10s · 30s · 1m · 5m · 15m · 1h · 3h · 6h · 12h · 1d · 3d · 1wk · 1mo · 3mo · 1yr · 3yr · 10y), each with a tick mark on the scale and a readout naming its own unit; the boot default is the exact one-minute-a-second stop. 153 green.
- **2026-08-27** — Viewport controls sized up ("the labels feel cramped... bump up the sizes of all the controls"): every corner chip went 30→38 px with larger glyphs — decal toggles, pause, dice, seed chip, RIDE OUT, the settings cog — and the freed room went to the time throttle: a 300 px track with side margins for the edge labels' overhang, a bigger readout, and 10 px scale labels with real clearance; the detent ticks cross the track like a ruler and the whole scale centers in its chip. 153 green.
- **2026-08-27** — The walk hint moved to the top of the viewport: with the clock strip and decal switchboard now owning the bottom edge, the WASD/landing chip was crowding them; it floats top-center instead.
- **2026-08-27** — The band's hard edges dissolved (feel-test: "a clear sharp edge to the galaxy there, it's not a soft gradient"): three causes, three fixes. The glow map's bilinear filtering creased every texel-wide gradient — the backdrop now samples both the glow and rift maps with bicubic B-splines (four bilinear taps), so gradients are C1-smooth on screen. The map itself went 192×96 → 256×128. And the build got FASTER while doing it: profiling showed 4.5 s of the glow march burned summing per-cloud fields beyond 1500 pc, where the march steps (up to 550 pc) outpace a cloud's whole reach — shot noise painting texel-scale dark mottle, not structure. Distant clumping now enters at its expected field, a Monte Carlo constant against the real population (pinned by a new test), and the march samples the new sightlineDensities primitive — one arm-profile evaluation feeding thin, thick, halo, and dust, with the piecewise accessors delegating to it so the fused and separate views cannot drift. Sky build: 15.3 s → 11.5 s at 2.5× the glow resolution. 154 green.
- **2026-08-27** — The galaxy stopped dimming by who's looking at it ("it'll sometimes render the galaxy almost totally dark, like its brightness is dependent on the selected star"): exactly right — the light-per-star conversion used by both the exterior volume and the interior band was estimated from just 12 age draws through the viewer's local population mix, and since the bright tail rides on whether the youngest draws keep massive stars alive, the scale swung 54× between seeds (0.074 at the Krodya-Arm locale — near-black galaxy — vs 4.02 elsewhere). Replaced with MEAN_POPULATION_LUMINOSITY: one stratified, well-sampled constant over the reference population's age CDF (the honest estimate came out 1.76 — the old home draw was itself 6× undersampled), with the display photometric scale recalibrated (2.3e-4 → 9.2e-5) so the typical brightness stays where the sky was tuned. The galaxy and the band now carry the same light from every locale. 154 green.
- **2026-08-27** — The galaxy stopped looking airbrushed (feel-test against Sa/Sb references and M77: "tiny thin wisps of arms, no real organic details... very uniform"): three changes, one physical root. (1) Arm light decoupled from arm counts: the ARM_YOUNG_LIGHT factor weights the arm overdensity's EMISSION 4× beyond its star count — the luminous young population lives in the arms it was born in — applied in both the band glow and the exterior volume, so bright grainy arms now stand over a dark interarm disk instead of faint modulation on a bright smooth one. (2) The arms themselves went broad and shock-asymmetric: 1.15 kpc bodies with a compressed leading edge and fluffy trailing edge, knots refit to the new width, lanes and spurs rescaled. (3) The volume's statistical texture grew up: a swirl transform (rotation by log radius — differential rotation in closed form) shears isotropic noise into trailing spiral filaments, feeding both a third-octave filamentary dust webbing (lane-coupled, M77-style) and new unresolved-cluster grain that textures only the arm emission, mean 1 so the interior glow's smooth expectation still agrees. 154 green.
- **2026-08-27** — The galaxy rebuilt on density-wave theory, and made of stars (Ben: "check out this code... it's simply amazing visually", pointing at beltoforion's spiral galaxy renderer): the arm model is no longer drawn — it emerges. armProfile now rides a Lin–Shu-style orbit family (construction after beltoforion.de's density-wave renderer): nested oval orbits, circular in the middle, most eccentric at the core boundary, each tilted a little further with size; the arms are the crowding caustics of that family (inverse Jacobian, fixed-point inversion — cheaper than the sinusoid stack it replaces), so arm width variation, the soft emergence from the round core, and the rim dissolve all follow from geometry. The dust lane is the same family wound 0.07 rad further. Star-formation patchiness rides the wave in wave coordinates. And the exterior view is now a particle body: ~110k deterministic particles — field stars, arm-born young stars with natal scatter, giant faint dust billows, filament chains strung along single orbits, and H II regions whose size is the wave-spacing probe (they only ignite where the caustic packs orbits) — every one placed on the same orbit family the smooth model integrates, so the particle image and the analytic density agree by construction, and clumping, grain, and broken arm patches are simply what discreteness looks like. The raymarch volume steps back to smooth base + extinction (grain hacks deleted). Ridge test now derives from waveTilt + RIDGE_PHASE; cloud expectation refit (0.0092). 154 green.
- **2026-08-27** — Galaxy view profiled back to speed ("FPS on my machine for the full galaxy is like 2"): frame captures showed the raymarch volume was the whole bill — 43 ms of a 52 ms frame at retina pixel ratio; the wave-crowding armProfile was running two full orbit inversions plus finite differences per march step, ~15k transcendentals per pixel. Now: one inversion serves the wave, the lane, and the patchiness coordinates; two fixed-point iterations instead of four; the profile held for four march steps and the clump noise for two; 72 steps instead of 112; and the off-slab halo loops dropped their pointless arm evaluations. Volume cost 43 ms → ~1.3 ms, whole frame 52 → 9.6 ms at the same vantage. Also fixed in passing: particle sprite sizes were authored in pc but divided by km distances — every sprite clamped to a single pixel; with sizes actually in km the dust billows bloom at their true extent and the galaxy turns into the fluffy clumped mass of overlapping clouds the references show. 154 green.
- **2026-08-27** — The disk edge dissolved ("the galaxy has a sharp edge where it ends... ideally not in a hacky way"): the smooth model never had an edge — the particle sampler did, truncating its radial domain at 16 kpc, and the spread-out half of the dust billows was constant-density to exactly that line. The honest fix real disks use: no truncation. The star CDF now runs to a 26 kpc far field where the exponential profile itself has faded to nothing, and the evenly-spread populations follow a Type-II outer taper — beyond an 11 kpc break they decline on the thin disk's own 2.6 kpc scale length. The rim is now a gradual thinning of billows and stray stars into black. 154 green.
- **2026-08-27** — Two galaxies at planet focus ("happens if im viewing a planet, but not the other levels"): the particle body hung on the scene root with a static transform, but a solid-body focus spins the whole sky around the ground frame — the raymarch volume compensates per frame, so the two galaxies crossed at the frame angle. The particles now ride inside the sky's pc group like the neighbor stars and the chart, transform in pc units, and the ground frame carries them for free. 154 green.
- **2026-08-27** — The sky field got real progress, in two lines ("watching progress bars makes the 5-20 seconds feel a lot shorter"): buildSkyField reports through a staged callback — the catalog sweep weighted by each mass stratum's measured cost with slab-level progress inside every row (sweepRowStars reports its cell slabs), then nebulae, dark clouds, charting, and the glow map row by row. The worker throttles reports by advance (no clocks — determinism holds), the service tracks the build actually running (the oldest pending; later requests queue behind the serial worker), and the readout shows the SKY FIELD bar with a child line naming the current step — "red dwarfs", "sunlike stars", "hot stars", "giants & rarities", "milky way glow" — with its own bar. 154 green.
- **2026-08-27** — The sky build went parallel ("just curious if we can thread that in the browser"): the star sweep — 84% of the build — is embarrassingly parallel by construction, because every cell seeds its own generator, so any partition of a row's slab range concatenated in order is byte-identical to the serial sweep (a new test proves it). The sky worker became a coordinator: it farms each catalog row's ix-slabs across a pool of sweep workers (one row in flight at a time, so the progress stage stays truthful), merges in slab order, and runs the serial tail itself; requests queue so one build owns the pool. buildSkyField refactored into sweepRowSlab / rowSlabSpan / assembleSkyField, with the serial path recomposed from the same pieces for tests and fallback. Measured in-browser: ~11.4 s → 6.6 s with the tab actively rendering; sub-step labels also went caps to match the readout, and the first stratum is now honestly "dwarf stars" (it spans everything below the turnoff). 155 green.
- **2026-08-28** — The whole galaxy became travelable ("at the galaxy scale, we have no click targets other than what we've generated for the sky map"): the gazetteer's own province anchors — the prominent molecular complexes sectors are already named after — are now a galaxy-wide landmark catalog (galacticLandmarks, ~240 of them, enumerated once per session in a worker). Each carries a gateway system seeded from its anchor cloud, so clicking one travels there and arrives with the complex overhead — and the destination's designation naturally inherits the landmark's name, since names flow from locale. At map altitudes the landmarks render as hollow chart diamonds (marker-decal family, sized by territorial prominence) with hover naming and click-to-travel through the standard pickable plumbing; the galaxy tab gains a LANDMARKS table under TRAVEL TO, nearest first. Fair warning discovered en route: gateway skies are the heaviest in the galaxy — a system inside a complex can hold 12,000 stars within 30 pc, and its sky build takes as long as that implies, which is precisely what the staged progress bars are for. 155 green.
- **2026-08-28** — The tabs sorted themselves by scale ("move the star list to the star tab, pin companion stars to the top... move the galaxy tab to be first, so its in descending order"): the tab bar now runs GALAXY · SYSTEM · STAR · PLANET (kpc · AU · R☉ · R⊕), each level listing destinations of its own scale — the galaxy tab keeps the landmark complexes, and the stellar neighborhood travel table moved to the star tab beneath the system's own stars, which pin first.
- **2026-08-28** — Right-drag pans ("you cannot pan the camera around, its orbit is fixed"): the free-flight pan that lived behind right-shift now answers plain right-drag at space altitudes — the same screen-plane grab scaled by altitude, meters over a ridge or parsecs across the neighborhood — while right-drag below the horizon-gaze line keeps steering the head. The wheel already rode toward a panned anchor and descent already re-homed it; the missing piece was a RETURN TO FOCUS chip, top-center, whenever the anchor sits off the body. Works at every level: galaxy, system, star, planet. 155 green.
- **2026-08-28** — The universe became literal ("lets add that, a galaxy seed, fixed to a shared one, but the option to change it"): `?galaxy=<hex>` now selects which galaxy the session materializes — its own density wave (pitch, eccentricity depth, wobble, patch phases — amplitudes never vary, so the ARM_BOOST_MAX ceiling holds for every seed, and the caustic's ridge phase is measured numerically per galaxy), its own cloud population, particle body, names, addresses, and seed→locale placement. The derivation rule is one XOR: galaxyRoot(base) = base ^ seed ^ PRIME, so the shared prime galaxy ("SIM_UNIV" in ASCII) is bit-identical to everything ever explored — proven by the 155 pre-existing tests passing untouched. The seed locks at first read; every module-load-time derivation went lazy to let the URL choose first (the last two holdouts were catalog row salts and the thin-fraction cap, which now rests on the smooth wave-free profile all galaxies share — the same number, no galaxy commitment); workers receive the seed in their requests; the volume shader's wave constants are templated per galaxy at compile. Verified live: the same system seed in galaxy beefbeef… lands in the Naori Sector between the Kraekroul and Muashaeoreil Arms as a different star under different arms. 156 green.
- **2026-08-28** — The welcome dialog asks the question ("a second step that either lets them use the prime shared galaxy everyone is viewing or generate their own personal galaxy"): after the cover-page info, first-time visitors now choose their galaxy — THE SHARED GALAXY (the prime everyone charts; links and addresses match across the world) or A PERSONAL GALAXY (a crypto-rolled 64-bit seed, rebooted into cleanly since the galaxy locks at first use). The choice persists per browser (`universe-galaxy` in localStorage): bare revisits restore the personal galaxy into the URL, an explicit `?galaxy=` link always wins and becomes the new persisted choice, and the prime hex — or the dialog's shared option — clears it. Arriving through a shared link skips the question; the link already chose. 156 green.
- **2026-08-28** — Bookmarks ("clicking it saves whatever body we're looking at to a list of POIs"): a bookmark glyph now sits beside every catalog plate's name — hollow until clicked, filled once the body is saved — and a fifth POI tab (★) closes the tab bar, listing the survey's shipped highlights (the Earth analog Doni LIY7 i, the moonlit ocean super-earth Thoubreim WJQR c, the Marusyaveim-owned sky of Syavelaekryak P7N9, the 48°-tilt ocean world Marudiak JOQT k — designations verified live against the prime galaxy) above the traveler's own marks, names up top with their stories beneath. A mark is a travel link — galaxy, seed, locale, view, body indices, the plate's designation — so a click restores the exact stand: in-galaxy marks in place, marks from another galaxy through a clean boot flagged as a visit, so hopping to a prime highlight never overwrites a personal home galaxy. Marks persist per browser; the POI tab is an address book, not a camera level — the plate above stays on the current body, and its bookmark keeps working from there. 156 green.
- **2026-08-28** — Marks take dictation ("they can click the default one and change it, an inline edit would probably work well there"): the note under a saved mark's name is now the owner's to write — click it and it becomes an input seeded with the default (the plate's subtitle at save time), Enter or blur keeps the new note, Escape doesn't, and an emptied note leaves a faint "add a note" to click next time. The survey's own highlights keep their curated captions. 156 green.
- **2026-08-30** — The metric spins. The tracer was Schwarzschild with the disc carrying spin on its own, which is where the reference implementations stop too: theirs adds a perturbative frame-dragging term with an adjustable "shadow squeeze", and its exact solver initialises each ray from flat-space expressions and so was never validated. Both halves are now done properly — Carter's separated equations in Mino time, and constants read off the tetrad of a locally non-rotating observer at the camera, which near a spinning hole differs from the flat-space answer by tens of percent. Verified rather than asserted: the capture boundary found by integrating thousands of rays lands on Bardeen's critical curve to under 0.4% at a★ = 0, 0.5, 0.9 and 0.998; the traced edge-on silhouette of a Thorne-limited hole runs from −2.11 to +6.99 r_g as the closed form says; the rendered image agrees with the analytic verdict on 99.96% of pixels, the rest being the silhouette's own boundary; and the weak-field deflection carries the 15π/4b² second-order term, not just 4/b. Three real bugs surfaced on the way, none of which announce themselves. The second-order form conserves dr² − R(r) analytically but not in floating point, and since R falls as r⁴ an absolute error picked up at eleven orders of magnitude is still there at five — rays that should have been swallowed were turning around at twenty r_g and escaping, with the shadow simply coming out the wrong size. Steps also walked straight over polar turning points near the spin axis, putting |cos θ| above one, which no point in the reconstruction has. Both are fixed by taking magnitudes from the potentials and leaving only signs to the integrator, reflecting off a turning point the step went past. With the metric carrying spin, the flow reaches its own inner edge at last: matter inside the innermost stable orbit is given the plunging geodesic that leaves it carrying the energy and angular momentum it had there, so the old 4 r_g floor is gone and a starved torus is drawn from its horizon. That torus is now passed through rather than crossed — it is half as deep as it is wide and optically thin, so the picture is a path integral and the bright ring is where the sightline runs longest through the plasma, which is what the Event Horizon Telescope resolved. Separately, the composer was never given the renderer's pixel ratio, so every pass in the app had been running at half resolution and scaling up; fixing it doubled the linear resolution of everything and quadrupled the trace's cost, so the geodesics now run on their own grid at 0.55 and are scaled up while the stars keep every pixel the display has — 128 ms a frame to 49 at the galactic centre, 15 ms for a fed disc. 232 tests green. Browser-verified: a Kerr shadow with the flow's far side lifted over the top and the approaching limb beamed, and a starved nucleus as a crescent hugging a dark shadow.
- **2026-08-30** — Stellar-mass black holes stopped being black spheres. The trace already had no scale in it — a ten solar mass hole is the galaxy's nucleus reduced by seven orders of magnitude and nothing else — so what was missing was not a renderer but a supply. It comes from the system: every other star is offered as a donor with the separation between them, and whether a hole is dark, wind-fed or spilling over is settled by the pair. Winds are Reimers on the cool evolved side and line-driven on the hot, and the speed that matters is not the terminal one but what the beta law says the gas has reached by the time it has crossed to the hole — a companion three stellar radii out meets it at two thirds, and capture goes as the fourth power of speed. Overflow starts where the donor's radius passes Eggleton's lobe, and runs on the donor's thermal time if it is the heavier and its nuclear time a thousand times slower if it is not. The piece that matters most is the efficiency: below a percent of Eddington a flow cannot cool as fast as it falls, so its efficiency drops with its supply and the light goes as the square of the rate. Left out, the hole drifting through the warm neutral medium shines at a third of a solar luminosity and would be an easy find; with it, it is twelve orders of magnitude below Eddington, and dark. Which nearly all of them are: a scan of 120,000 systems turned up 222 stellar holes and not one accreting, because a companion is drawn at 0.15–0.95 of the progenitor's mass and a black hole's progenitor is 20 M☉ and up, so its companion is always another massive star and has always died first. So they are drawn as what they are — a shadow, a photon ring, and the star field bent into an Einstein ring — and the flow is suppressed outright below a part in ten billion of Eddington, since brightness is shown against a flow's own peak and a starved one would otherwise be drawn as brightly as a quasar. One photometric bug fell out of it: sprites drawn into a sky capture are scaled down to correct their angular size, and a sprite's light is its area times its per-pixel energy, so the correction was throwing away the light with the area — the nuclear cluster had been captured 44× too faint all along, and the lensed arcs around the galaxy's own hole came back with it. 247 green. Browser-verified: a 10.1 M☉ hole at a★ = 0.98, 292 km away, ringed by lensed starlight at 44 fps.
- **2026-08-30** — The flow stopped standing still. Clumping was a frozen realisation — the magnetorotational instability's own log-normal density field, drawn once — so the gas around the hole was structured but static. It now advects with the flow's rotation and re-forms on the only timescale it has: an MRI eddy turns over in about an orbit, and at the inner edge of this galaxy's nucleus that is forty seconds, of a thin disc's three and a half hours. Advecting one field would only show half of that, shearing it into ever-finer threads that never renew, so two realisations run half a lifetime apart, each reseeded while weightless, blended on squared weights so the contrast holds through the handover; bulk rotation goes to both alike and never resets, keeping the turning continuous. The result divides cleanly by regime, as it should: a thin disc reads its clumping at one crossing and visibly churns, while a hot torus is a path integral through a hundred decorrelated cells and averages its own fluctuations out — which is exactly why one looks filamentary and the other smooth, and why the volume march can re-read the field every third step for free. That last change more than paid for the doubled realisation: the starved nucleus went from 49 ms a frame to 45. 247 green.
- **2026-08-30** — Three bugs the user found by looking, all of them mine, all of them from this pass. The lensing footprint had become a hard-edged disc of sky with black beyond it, because the composite quad that draws the traced image into the scene lost its blending settings when the trace moved to its own render target: opaque, it painted the trace's transparent surround over everything as a disc of nothing. And a black line ran up the spin axis, wide enough to be the first thing the eye went to. That one took the longest and was the most worth having: it is not step size, not the step budget, not the escape direction, not the cube map, not the flow — it is the subtraction 1 − μ². A ray passing near the axis has sin²θ down at a hundred-millionth, and in the single precision a shader has, μ² there agrees with one to seven digits, so the difference comes back with a fifth of its value in error and sometimes as zero. ξ/sin²θ then runs away, the azimuth overflows to infinity, and the ray leaves for nowhere. Confirmed by running the identical march in float32 on the CPU, where it reproduces exactly: −102 radians of azimuth where there should be −1.57, and spurious captures beside it. sin²θ is now carried as its own state, advanced by −2μ dμ/dσ and never differenced, with μ derived from it below a quarter and it from μ above; no RK4 stage may put it under the value that makes Θ vanish; and the handful of rays that would still thread the axis closer than a part in ten thousand are held off to that distance, since the trajectories either side agree to a thousandth of a radian and only one of them is representable. The axis is clean at every framing and every inclination, including looking straight down it, which used to be the worst case. Also fixed on the way: the escape direction was taken by differencing two reconstructed positions, which on the axis is the difference of two points both sitting on it — it is now differentiated in closed form. 252 green, 42 ms a frame.
- **2026-08-30** — Galaxies became somewhere you can go. Every 64-bit seed already named a whole galaxy, but reaching another one meant editing the address bar, and the address bar was not to be trusted: a link carrying a galaxy quietly rewrote where you lived, and a link *without* one was read against wherever you had last been sent — so the same sixteen digits named a different star for every reader, because the locale differs and the star is drawn from it. Both are gone. The galaxy is now written into the address every time, prime included, so a link is a complete instruction; following one decides the trip and nothing else, and home is set in one place only, by the traveler choosing it. The centre is addressable too, which it needed to be before it could be a destination. On top of that the POI tab opens with a catalogue of eight galaxies — a quiescent hole at three parts in a million of Eddington, a quasar at a third of it, the Thorne limit at a★ 0.998 against one barely turning at 0.07, a two-hundred-million-solar-mass hole against an eleven-thousand one — found by generating nine hundred galaxies and keeping the ends of each distribution, one click each. What the rows claim is checked: every figure is recomputed from its seed by galaxyCatalog.test, so an entry that stopped being true fails rather than misleads. Also fixed, user-reported: the accretion flow's turbulence ran its lifetime on the inner edge's orbit while each radius rotated at its own, so gas at twice the inner radius — turning at a third of the rate — was replaced after a third of a rotation, and the pattern restarted before it had been anywhere. The clock is local now, and the two rotation terms sum to exactly one turn per lifetime at every radius. 264 green.
- **2026-08-30** — Two the user found by looking down the spin axis, and both were mine. A seam ran down the middle of the frame, and the more nearly you looked down the axis the worse it got. The cause was the previous entry's own fix: rays that would have threaded the axis too closely to integrate were held off it, and the stand-off scaled as √(η+a²), so out at the frame's edge — where η is large — every ray within a tenth of a unit of zero angular momentum was handed the same value, with the sign of whichever side it had started on. A flat band a hundred and seventy pixels wide with a step down the middle of it. It was never a few rays; at the galactic centre it was most of the picture. The dodge was never needed. dφ/dσ carries ξ/sin²θ, which no step rule can follow near the pole, but the term splits exactly — ξ/sin²θ = ξ|cosθ|/sin²θ + ξ/(1+|cosθ|) — and the whole of the difficulty is in the first piece, which is elementary: written in s = sin²θ the polar potential is exactly the quadratic Θ = −a²s² + Bs − ξ², and ∫ds/(s√Θ) is an arcsine. Offset to vanish at the polar turning point it can be differenced across a step that turned around without the step knowing it did; the equator turns s around too and the jump that costs is a constant of the ray, taken off when μ changes sign. What is left over is bounded by ξ, divides by nothing small and subtracts nothing. Refined, this lands where integrating ξ/sin²θ directly lands, to two parts in a hundred thousand — the same integral, not an approximation to it — and it costs a hundred and twenty-three steps where the direct form needs fifty-two thousand, the same as a ray nowhere near the axis. Walking the aim across the axis, the worst jump between neighbouring rays was three hundred and seventy times the typical one and is now one and a fifth; on screen the frame's midline went from the sharpest column in the picture to among the smoothest, at every inclination including straight down the axis, and the rate cap and the stand-off are both gone. Second, the flow's clumping dissolved into an even glow while you watched. Three things: the clock counted turns of the inner edge straight off sim time, and the time control reaches ten years a second — eight million turns a second at the galactic centre, and six thousand at a stellar hole on the default setting — so the count swallowed the azimuth in a float and left perfectly smooth concentric bands; each new eddy generation was drawn a fixed step further out in the noise until the offset outgrew what a float resolves and the field came back flat; and the two realisations the crossfade blends were frequently one realisation, since floor(t) and floor(t+½) agree for half of every cycle, which at the inner edge left the clumping beating between one and √2 of its contrast every orbit. The count is folded into a single turn before φ is added to it, generations are hashed into a bounded box, the two are half a generation apart, and the clock is held to a fiftieth of a turn a frame — the limit at which a pattern sampled on a twenty-five-cell ring stops being resolvable between frames, past which drawing it turn faster shows a wagon wheel and then nothing. The field's contrast and structure now hold to within a percent from turn zero to turn a hundred million. 272 green; 39 ms a frame at the galactic centre and 35 looking down the axis, which used to be the expensive case.
- **2026-08-30** — Looking down the spin axis, the shadow was behind a veil and the picture read as fog. The lensing was not what was wrong, and checking that first was what made the rest findable: the shadow measures 4.9 r_g against the closed form's 4.83, the step budget is never exhausted, and replacing the sky cube with a smooth function of direction drops the picture's radial ripple two hundred and fifty fold — so the escape directions are smooth and what rings is the sky's own content, which is lensed cluster stars and correct. Two pieces of the flow model were wrong instead. Its vertical profile was a Gaussian in μ; for a razor-thin disc that is the same thing as a Gaussian in height, but for a torus half as deep as it is wide it leaves nineteen percent of the midplane density sitting on the spin axis, and looking down at one the eye travels the whole way through that. Hydrostatic support against the vertical pull of a body rotating at the local Keplerian rate gives ρ ∝ exp(−cot²θ/2ε²) — near the midplane the same Gaussian, since cot θ is z/R exactly and the scale height is therefore εR as it should be, and toward the axis falling faster than any exponential. That is the funnel every simulation of a hot flow shows and a jet would occupy. With it the shadow's centre reads exactly zero instead of a tenth of the ring, and the contrast across its edge goes from sixteen to one to seventy-six. And the optical depth was the constant 0.45 whatever the hole was eating, so a flow starving at three parts in a million of Eddington drew the same veil as one fed a thousand times harder. It follows from the supply and nothing else — steady inflow puts Ṁ = 2πRΣv_r through every radius, a thick flow drifts inward at α v_K (H/R)², electron scattering gives every kilogram the same cross-section, so τ = κΣ/2 with no free parameter beyond α — and what comes out is anchored at both ends of the model's own range without being told to be: τ ≈ 2 at the percent of Eddington where the flow stops being radiatively inefficient, so it turns optically thick exactly where it turns into a disc, and τ = 0.03 three decades below that where most galactic centres sit. That needed the real rate: a hot flow cannot radiate what it is given, its efficiency falls in step with its supply, and the same light is bought with fifty times more gas than the disc efficiency implies — so the flow now carries its own efficiency and its own Ṁ, which keeps L = ηṀc² exact and stops the nucleus panel quoting a disc's fifteen percent for a hole running at a quarter of one. 273 green.
- **2026-08-30** — Two more the user found by looking, and both were in what the traced image is combined with rather than in the trace, which is why measuring the trace's own buffer kept saying it was fine. The traced image is not opaque out to LENSING_REACH_RG — it fades across the last stretch of it, so pixels out there are part traced sky and part the galaxy's dome and nuclear cluster behind. The dome was being switched off at the far end of that fade rather than the near one, so a camera between 88 and 160 gravitational radii had frame edges where the dome was already gone and the trace had not yet arrived: correct lensing in a disc at the middle of the frame, and a scatter of bright rings on black outside it, flipping between one picture and another with a single notch of the wheel. Since a ray's impact parameter can never exceed the camera's own distance, a camera inside the near end has every pixel at full coverage and the dome completely hidden, so switching there is exact — the frame does not change as it happens — and the two radii now come from one shared constant. The second was flashes of light inside the shadow that moved with the camera and ignored the clock. −p·u is the energy a photon has in the frame of whoever is looking; it is positive for every real photon and every real observer, and the renderer divides by it for the colour and again, to the fourth power, for the beaming. It was passing through zero, because kerrFlowVelocity takes the radius where circular orbits stop existing and was being handed the flow's inner edge instead — the same radius for a cold disc, but a hot flow reaches its own horizon, so the circular branch was being asked for orbits from the photon sphere down, where r² − 3r + 2a√r is negative and the clamp under its square root returns an energy ten thousand times too large. At a★ = 0.89 and r = 1.474 the resulting four-velocity has norm +8.8×10⁷ where it should be −1. A stray ray a percent above the horizon came out three hundred times blueshifted, the fourth power of that saturated the half float it was written into, and one pixel landed two thousand times brighter than its neighbour. The worst isolated pixel is now nine times its surroundings rather than two thousand nine hundred, and the frame's peak 2.2 rather than 636; what is left at nine is a lensed star, which is supposed to be a bright point. 274 green.
- **2026-08-30** — The last of the axis, and it was the initial condition all along. Looking straight down the spin axis, the star field collapsed into smooth concentric circles — one notch of the camera either side of it and the picture was fine. |μ| there is within a part in a million million of one, so 1 − μ² is zero in the single precision a shader has, and ξ carries a factor of sinθ: it came back as exactly zero for every ray on the screen. Every one of them was then a photon with no angular momentum about the axis, travelling in the observer's own meridian, and since they all started at the same azimuth they all left at it too — a whole ring of pixels landing on one point of sky, which is what a ring of one colour is. Measured, walking a circle of screen azimuth moved the sky azimuth from 2.0° to 3.0° and back, sixteen samples landing on two values. This is the same subtraction that drew the original black line up the axis, still sitting in the trace's initial conditions after being taken out of its loop: sinθ now comes from where the camera is — x² + y² = (r²+a²)sin²θ, which subtracts nothing — and is passed into kerrPhoton rather than recovered there. A camera exactly on the axis has no azimuth at all either, so it is held the least distance off it that gives atan something to work with, the same standoff and the same size as the one the disc plane already gets. One turn of the screen is now one turn of the sky, sixteen distinct azimuths of sixteen, slope 0.99. 275 green.
- **2026-09-01** — The nebulae grew from sprites into volumes, and the two tiers became one object. Each resident cloud is baked onto a 96³ grid — its own turbulent field with its natal group's ionizing budget spent through it ray by ray, the front standing where the photons run out, so a dense clump shadows everything behind it — and marched by a camera-riding dome, with every star point carrying its own extinction and reddening through the boxes its sightline crosses. What closed the sprite→volume seam was physics, not blending: a region's natal Strömgren radius is sub-parsec, but an 11 Myr region has been driven out for its whole age — Spitzer's solution, the interior diluted as R^(−3/2), which conserves the recombination budget exactly, so the march runs in contracted coordinates and keeps its carved shape — and the pink object the sky promised is tens of parsecs when the volume stands. Hue comes from budgets in both renderers, line output weighed against scattered continuum surface brightness, so an O group reads pink and a dozen B stars stay a blue reflection complex, in sprite and volume alike. The frame paid for the first cut — 19 fps where there had been 60 — and the cost was measured rather than guessed: the galaxy dome was re-solving the orbit family and evaluating simplex per step per pixel, 81 of 96 ms. It now reads its own bake (the arm profile as a polar LUT, the clump field as a tiling 3D texture), and the two most expensive fragments in the frame — the galaxy and nebula domes — render together at half resolution and composite once: 10.7 ms and 90 fps at the same view. The nebula bake went to the GPU too, in the worker, on an OffscreenCanvas: the seeded simplex ported to GLSL with the galaxy's own permutation as an integer texture (near-equal is enough — the CPU field stays the physics authority), the carve filled layer by layer into a half-float 3D texture, the march one pass over a tiled atlas, the same finish quantizing either path's grids. Seconds a bake became 60–180 ms, dark clouds bit-identical, refs and emission coefficient within 0.1%, the only visible differences one-step front flips where single and double precision disagree on the step the budget runs out. Then a family of ordering bugs the user found by looking, one sin with four faces: things that shine nearer than the sky were drawn before it, so the volume layer's occlusion multiplied their light away. The sky map's borders erased exactly where they had something to name; the daylight washout that fades every stellar layer never reached the newest one, so a nebula glowed through a noon sky and a rift punched a black hole into it; the haze dome gated every light's scattering by the primary sun's elevation, leaving moonlit and companion-lit nights scattering nothing; and — bisected layer by layer on a 121-bar hothouse's night side after every other suspect came up clean — the uniform brown its sky wore was the planet's own cloud deck at the wrong order, with hard nebula-shaped cutouts where the volumes ate its light and stars shining through solid overcast. Each now sits where it stands: composite, then stars, then haze, then deck, with the chart and its lettering as instrument overlays on top. Last, the residency grew up: sixty-four volumes stand at once (a dome pays only for the pixels its box covers; the GPU fills a cold arrival's set in ~20 s), admission drops to a degree of apparent size, and the tiers crossfade — a volume dissolves in over a second while its sprite carries the complement, an evicted one fades before disposal, and one the camera swings back to fades up again instead of flickering. Still open, in the plan's ledger: the one-pass multi-box march for overlapping enclosing volumes, photometric unification of sprite against volume, the line-ratio LUT, and a decision on the 0.4% of catalog systems (and every cloud gateway) that sit inside dark clouds under honestly black skies. 330 green.
- **2026-09-01** — Stage 5 became physics rather than promise, and it ended in a correction that moves every sky. First the wind: each region's dominant source ploughs the Spitzer-diluted interior as a momentum snowplow (Weaver's energy-driven radius deliberately passed over — real bubbles leak, and the momentum limit lands on the surveys' 0.3–0.7 cavity ratios), hollowing the filled disc into the ring an evolved H II region actually is, with the emission books closed by construction since the finish renormalizes total line light to the ionizing budget wherever the density moves. Then champagne: ten-thousand-kelvin gas holds together only where the cloud confines it, so the interior is gated by the natal field at each cell and streams to a residue where the bubble has outrun the cloud's body — the gate is the cloud's own carved boundary, which opens face-blisters into horseshoes and makes rims ragged along real filaments. Then the dead: members drawn past their model lifetime are struck from the group — a neutron star's million-kelvin cooling track had been setting the hue of every older region and posing as an ionizer — and each death hands the cavity its terminal momentum, sixty times the whole wind's, so a first supernova blows a region toward its front and old regions read as thin broken shells. And the change that finally killed the wrapped-on-a-sphere look the user kept catching: the swept shell's inner skin is now ionized — the front eating into the shell is where recombinations concentrate — so the glow follows the budget march's own directional front instead of the spherical wind wall. Every mechanism mirrored in the GPU march and re-A/B'd against the CPU authority at a fraction of a percent; the tests moved from proxies to the physics itself (the Spitzer invariant pinned directly on raw interior density, the champagne gate as an exact A/B against its disarmed twin). Last, the correction: chasing why three-megayear groups already counted several supernovae exposed msLifetimeGyr as the bare fuel estimate 10·M/L, which ignores the growing convective core and runs massive stars ~4× short. It now carries the Eddington asymptote as an additive 3.2 Myr floor — 60 M☉ lives 3.5 Myr, 20 M☉ 7.2, 9 M☉ 33, sunlike stars untouched, within ~25% of the track grids throughout. Decided knowingly: this shifts which massive stars are alive in every sky — skies with O stars gain a few, old regions lose their earliest supernovae — and the whole suite absorbed it with a single fixture retuned. 333 green.
- **2026-09-02** — Photometric unification, prompted by the user asking why nowhere in the galaxy mirrors how nebulae look from Earth. The honest answer is that nowhere does: surface brightness is distance-independent, so no viewpoint makes a nebula brighter per patch of sky than Earth already sees M42 — a gray smudge to the eye, pink only in long exposures — and the sim's brightness was two unrelated dials pretending otherwise. Now there is one law, `universe/galaxy/displayLaw.ts`: the star points' curve (display energy as brightness to the 0.36) extracted as the shared transfer, and every extended tier — nebula volumes, sprite impostors, the Milky Way glow — displaying its radiance through the same curve as the marginal response above a subtracted sky pedestal, which is precisely what a sky-subtracted deep exposure shows. The naive route was tried first and taught the physics: the pure power applied to extended radiance lifted the diffuse floor (the integrated starlight of every column, about a solar luminosity per pc² per steradian) into gray fog across the entire display — thirty-two volume skirts and the whole band, each stretched by x^0.36 — and the marginal form D(P+R)−D(P) kills the fog while a nebular core two hundred units above the pedestal keeps its full stature. The volume's provisional NEBULA_PIXEL_SCALE is deleted; the sprite's 95·√L/d² impostor law is deleted, its tile now carrying exact flux closure — the cloud's line-plus-scattered budget over the tile's own luminance integral, the distance cancelling as surface brightness demands — with the peak display energy riding in the one scalar the shader multiplies; the glow map joined the law with its own darkest column as pedestal, self-calibrated per viewpoint, and dark clouds dim it as transmittance to the gamma, which the pure power makes exact for points; the bake's emission books close on the quantized grid the renderer actually integrates. A harness pins the law against the star shader exactly, pins real-sky anchors (pole sky subtracted to black, the band a few hundredths, an Orion-class core ~0.37), closes the sprite's flux books to ±10%, and pins volume-against-sprite total flux at the measured ~0.12 — the gap is dominated by the impostor's 0.3 continuum interception standing in for scattering the volume only single-scatters, and that pin is the reflection pass's inheritance. Verified standing at the brightest complex's gateway: pink emission standing over a structured band, rifts silhouetting, dark poles, no fog. 340 green.
- **2026-09-02** — The reflection pass: multiple scattering as a solved table, and the blue of reflection nebulae as an emergent flux ratio. A voxel lit by a star was only ever scattering the direct beam; the diffuse field — light scattered elsewhere arriving from every side and scattering again — rivals the beam at the optical depths real nebulae carry, and no frame shader can march secondary rays for it. So the transfer is solved once per session, deterministically, in the canonical configuration (a point source in an infinite homogeneous medium of the model's own albedo and grain asymmetry, both promoted to universe constants): successive orders of scattering, each order's source function swept into radiance along straight rays and scattered once more, ~33 ms to convergence, kept as Magnor's table over the two things a voxel knows — the optical depth back to its source, which the bake already marched through the real inhomogeneous field, and the scattering angle to the camera. Normalized so first order alone is the attenuated Henyey–Greenstein term the shader already used, it dropped into the shader as one fetch; measured against single scattering it runs ×1.1 at a tenth of a depth, ×3 at two, ×14 at ten, the forward peak washing out with depth and clump shadows now filling with softly diffused light instead of holding pitch black — all pinned structurally. The chromatic piece rides the same fetch: extinction ratios 1.324 blue and 0.748 red on the R_V = 3.1 curve scale the scattering coefficient and the per-channel depths, the march's transmittance went to three channels, and the classic colours fall out instead of being painted — thin columns turn a blue illuminant bluer (scattered-only blue-to-red ~3.3–4.4 against ~3.05 for the grey twin), thick columns eat the blue twice and redden, deep nebular light reddens through its own dust exactly as transmitted starlight does, and one pinned subject that read as a "reflection complex" by budget turned out to be a wispy veil with nothing to scatter with — its light honestly Balmer magenta, which the test now respects by pinning the mechanism against a grey twin rather than anyone's prior about the outcome. The sprite tier wears the luminance-normalized ratio tilt as its stand-in. The bake is untouched — no GPU mirror needed — and the sprite-against-volume flux pin moved from 0.12 to 0.14, the remaining gap now named: the impostor's whole-cloud interception estimate against a bubble-scale box that holds only part of the cloud's dust. 346 green.
- **2026-09-02** — Display modes: the sky got its instruments. First the enabling refactor — the law had been applied in three places (shader constants for volumes, baked into the tile atlas for sprites, baked into the map for the glow), so the maps now carry physics and the law rides as uniforms: the glow map holds column radiance and reddening (which made cloud shadowing exact — transmission dims the light itself, and a rift can carve the column below the sky's pedestal and show honestly black), the tile atlas holds relative luminance and the line-vs-continuum mix with the calibrated peak radiance and unit-luminance hue pair as uniforms, and the pedestal every extended tier subtracts became the standing sky's own measured floor, carried from buildGlow through the workers to the backdrop and every volume. The sprite's flux closure now pins to a tenth of a percent in the linear domain. Then the instruments themselves, each a preset seated onto the whole sky at once — backdrop points and glow, sprites, the 3D star tiers, every standing volume: the camera (the sky-subtracted deep exposure everything was calibrated under), the dark-adapted eye (points cut off at sixth magnitude, the brightest near unit display, the band landing at the barely-there grey it really is, colour draining below a mesopic knee into the rods' blue-green cast — and the brightest home H II complex vanishing outright, which is the honest answer to the question that started this arc: nowhere in the galaxy mirrors an Earth dark night because nowhere in the universe does), and SHO (the mapped [S II]/Hα/[O III] palette computed from the same line grid and carried on every bake and patch so the switch never re-bakes, continuum cut to the sliver the filters pass). One sky-depth dial slides pivot, cutoff and mesopic knee together — integration time on the camera, dark adaptation on the eye. In the settings cog: camera / eye / SHO and the depth slider. 348 green.
- **2026-09-02** — A review pass over the whole nebula arc, in a branch of small commits. The tiers had drifted apart in three places the tests were pinning rather than closing: the volume spent and closed its emission books on the dominant member's photons where the sprite, the Strömgren radius and the Spitzer growth all used the group total (a median 54% of it); the impostor's scattered continuum was a constant 0.3 of the group's light where the bake's own first-order interception runs 0.002–0.2 by cloud; and the two were being compared over different bodies — the whole-cloud sprite against a bubble-scale box. The sprite now marches the object the bake marches: the model carries the front's radius along sixty-four rays, spent by the bake's own budget integral through the natal field in contracted coordinates, and a helper re-plumbs the cloud as the region has — diluted interior, wind cavity and wall, swept shell and ionized skin — so lines go as n² and scattered light as dust times flux with each closing on its own budget over what escapes toward the viewer, and the interception is that same structure marched from the illuminant with exact per-step capture. The tier pin moves from 0.07–0.4 to 0.2–1.5 (measured 0.3–1.0). Two quantization and transfer errors fell out on the way: the dust byte, quantized linearly against the clump peak, zeroed 92–99% of a bake's dusty cells and up to half its dust mass — the thin columns that dim the sky behind a cloud — so it is stored as a square root now and every bake keeps its mass to a tenth of a percent; and the beam's transmittance was marched through the interior's full natal dust where the stored dust carries the ionized region's depletion. The SHO instrument seated the full broadband pedestal against continuum it had cut to six percent, so faint line emission stood against a sky three times too bright; the pedestal now carries the continuum share. Overlapping sprites add radiance before the law rather than display energies after it. Then the running costs: the GPU bake reads its atlas home a strip of tiles at a time and keeps its textures per grid size; the scattering table is solved in a worker behind the analytic first order instead of as a hitch on the first arrival; the sprites' seven uniform arrays became one small float texture, under the uniform budget a fragment shader is guaranteed; every volume is re-seated on the sky's measured floor when it lands; a fine bake landing after residency moved on no longer sits on the heap; the bake shelf is bounded by bytes; the volume pass allocates nothing per frame. And the hygiene the rules ask for: the CGS anchors in one place, the region-structure constants in the ionization module where the model, bake and sprite share them, and the comments and ledger saying what stands rather than what was waited for. 350 green.
- **2026-09-02** — The frame, measured instead of guessed. GPU timer queries around the sky layer with one volume visible at a time put 43 of a 50 ms frame in the nebula pass and nearly all of that in two volumes: the dark rift the camera stood in, whose detail octaves spent four extra 3D fetches per step marching void, and the Musas volume, a hundred steps through a box its carve fills a few percent of; the other thirty residents cost under a millisecond each. So every bake now carries a 16³ occupancy grid — exact, since a block counts as occupied if any cell a trilinear read inside it could reach is non-zero — and the march crosses an empty block to its far face in one iteration, paying for samples, warps, table fetches and the light arithmetic only where there is gas. Same view, all thirty-two volumes standing: the sky pass 43 → 5.9 ms, the frame 50 → 8.4 ms, which is the display's own refresh quantum and the frame the scene draws with no nebulae at all. 352 green.
- **2026-09-02** — The residency cap became a controller. It had gone up and down by hand as the march got dearer and cheaper — sixty-four, thirty-two — and no constant can price a resident across machines and viewpoints, since a volume costs the pixels its box covers times the gas it marches. So the cap now answers to the frame: it starts at thirty-two, grows by eight every second and a half while the frame's cost sits under seventy percent of a 60 fps budget and every requested bake has landed (a volume's cost shows only once it stands, and a cap grown on grids not yet drawn would overshoot), and shrinks by a quarter the moment the smoothed cost runs over, between twelve and sixty-four. The first cut read the frame interval and stalled at forty-eight: on a 120 Hz display an interval is quantized to eight or seventeen milliseconds, so a frame drawn in nine reads as a twelve-millisecond average and a 60 Hz display could never show headroom at all. The pipeline now times its own draw on the GPU where the timer extension exists, against the script's share of the frame, and the interval is the fallback. At the Musas gateway the cap climbs to sixty-four within ten seconds of arrival at eleven milliseconds of GPU a frame. 352 green.
- **2026-09-02** — The frame's instruments, top-left of the viewport: rate and the smoothed interval behind it, the pipeline's own GPU time where the timer extension gives it, the script's share, the renderer's whole-frame draw and triangle counts (the counters now cover every pass rather than whichever drew last), the star points standing as glints, and the nebula tiers — volumes standing against the cap the controller has settled on, sprites in the sky, bakes and terrain tiles still in flight. Polled a few times a second, since a readout redrawn every frame would be part of the cost it reports; a dash where nothing is measured. Building it caught a controller bug: a hidden tab's throttled frames, at 188 ms, sat just under the outlier cutoff and drove the cap down while nobody was looking, so frames drawn while the document is hidden count for nothing now. 352 green.
- **2026-09-02** — Two artefacts of the empty-space skip, found by the user's eye at two complexes and fixed the same afternoon: nebulae missing gas in rectangular chunks, because the occupancy grid came from the cloud-scale bake alone while inside the bubble box the march reads the bubble-scale grid, which holds the filaments and the diluted interior the coarse cells quantize to nothing, so a block empty on the coarse grid's word took the fine grid's gas with it; and volumes rendered as stacked slabs, because every ray resumed after a skip at the block face plus one fixed nudge, losing the per-pixel jitter at every boundary so the samples lined up. A volume's occupancy is now the union of both grids, each occupied fine block marking the coarse blocks its extent overlaps, and a skip resumes at the pixel's own jitter. 353 green.
- **2026-09-02** — The box hung in the sky around Zetalu was the sprite tier: its tile spanned 1.6 cloud radii, and Zetalu is drawn out 2.3× along one axis, so the tile sliced the body off in a straight line and the light along the cut stood as a square — seen only from far enough that the sprite carries the cloud, which is why it looked fine viewed directly. The tile now spans the cloud's reach, and a pin holds its outer ring empty on every bright subject. 354 green.
- **2026-09-02** — The last compass-drawn surface. The user kept seeing perfect spheres inside ragged nebulae, and the cause was the wind cavity: one scalar radius per region, hollowed to two percent with a wall nearly three times dense, in every lit region and blown to the cap in half of them by their supernovae — while the front around it had long since been marched ray by ray and eroded against the cloud. Wind-blown shells are real (the Bubble Nebula, RCW 120), and a thin shell in projection really is a ring, but real walls are corrugated by thin-shell instabilities, lopsided by the density gradient, wrapped around clumps, and mostly invisible in a photograph. So the cavity is eroded now the way the front is: a momentum snowplow's radius goes as the ploughed density to the −¼, so along each ray the mean cavity is scaled by that ratio, bounded, blown out where the interior is thin and stalled against filaments, with the cloud's own turbulence as the corrugation; where it outruns the ray's front the interior is simply empty to the front and the front's skin is the only shell. In the model, the CPU march and the GPU mirror alike, re-A/B'd to the same fraction of a percent. Pinned in the sense the physics says: the thinnest third of directions carries the edge further out than the densest third. 356 green.
- **2026-09-02** — The glints, priced. Half a million star points at the galaxy frame were suspected of the frame, so the far field was cloned to eight times its count in a visible tab and timed on the GPU: the whole star tier adds under two and a half milliseconds at any altitude, and the pulled-out frame belongs to the galaxy dome and the bloom pass. No culling exists and none is needed — the points are one draw and the GPU clips them after a cheap vertex stage. What was wrong was physics, not cost: the display floor held every point at a dim constant at every distance, so from forty kiloparsecs the sky field drew as a blob at the origin, light the galaxy dome already carries. A point a decade below the floor's own brightness now fades, two below it leaves the clip volume; at a locale nothing the sweep kept is that faint, so the night sky is unchanged. 355 green.
- **2026-09-02** — The spheres in the pulled-out sky. Each catalog row's sweep ended at a budgeted radius well inside where its brightest members are still visible, and the near census ended where the neighbourhood does, so the star density stepped down on nested spheres a wide view drew plainly. Every reach now tapers past its radius, the sweep thinning candidates by a unit fixed by their position while the cell generators draw exactly what they always drew, so no star inside a reach changes identity; measured in shells the density falls across every old edge instead of dropping. The same pass closed a hole nobody had noticed: where the disk is dense the neighbourhood shrinks its radius below thirty parsecs while the sky split at a fixed thirty, and the shell between went undrawn. 356 green.
- **2026-09-02** — Two levers from the frame breakdown. The galaxy dome's disk crossing is sampled by where the light is: seventy-two steps shared by length between the thin disk's own slab, which holds the dust and nearly all the light, and the thick disk either side counting a third, neither taking more steps than sixty parsecs of thin or a hundred and eighty of thick call for — a ray along the plane keeps its seventy-two, a ray from above crosses in forty, and the dome at a kiloparsec and a half went from eleven milliseconds to five. And an arrival no longer lands as thirty full bakes on the GPU at once: residents stand up at a first grade of forty-eight cubed, a bake of milliseconds, and climb to their grade one bake at a time whenever nothing else is baking, the most apparent first, with headroom admitting more only once the standing ones are at grade. A lesson on the way: a page through several hot reloads accumulates state, and a measurement that read sixteen milliseconds with everything off read three on a fresh load. 356 green.
- **2026-09-02** — The near grade's price. Zooming in on Musas to where its volume takes the near grade dropped the frame into the twenties, and the bisection put twenty-six of the volume's thirty-one milliseconds in the detail octaves: a three-fetch domain warp on the cloud grid, another on the fine grid, and the sub-cell octave — nine texture fetches per occupied sample against the plain march's two. Three changes that alter no value: the clump tile carries the noise at the warp's three offsets in its colour channels and the octave's doubled-frequency noise in its alpha, so warp and octave come from one fetch; the fine grid takes the cloud grid's displacement rather than fetching its own, bending both grids into one space; and the occupancy grid doubled to thirty-two cubed, so fewer void samples inside occupied blocks are marched. A detailed sample is three fetches now. Same view, same canvas: fifty-two milliseconds to twenty-seven, the volume thirty-one to nine. 356 green.
- **2026-09-02** — The two display levers, A/B'd rather than argued. Captured from the drawing buffer at one-to-one, since the screenshot tools downscale and cannot show what a resolution change does: the sky layer at 0.35 of the buffer saved six milliseconds at the near grade over Musas — 26 to 20, sixty frames a second — at a mean pixel difference of 1.2 in 255 from the current frame, nothing the eye finds at buffer resolution; bloom's targets at half saved nothing there, its cost being the full-resolution passes that do not shrink. So the sky layer's scale became a sample pitch in CSS pixels — a dense display no longer quadruples a march for glow that carries nothing at pixel frequency — and bloom stays as it was. 356 green.
- **2026-09-02** — The sky's background maps bake on the GPU. The glow map, the rift's transmission map and the dark-cloud tiles are per-pixel line integrals through deterministic fields — the arm profile, the smooth model, the carved clouds — and were five and a half seconds of CPU on the background worker at every arrival. They are three fragment programs now on that worker's own OffscreenCanvas, the arm profile read off the polar LUT as a float texture and the carve from the seeded noise the nebula bake already used, shared as one GLSL module; the CPU builders stay the authority and the fallback, and the worker demotes itself to them on any GL failure. A/B'd against the CPU maps from the page console: radiance to a mean relative error of one part in ten thousand, the sky floor to seven digits, the rift and dark tiles within six parts in ten thousand with no texel off by a hundredth. Glow one millisecond, rift nine, tiles a few; the background from 5.9 seconds to 0.75, all of it the nebula atlas now. And the climb no longer waits on the controller's tick — it asks the moment nothing is baking, so a resident's finer grid lands while the first grade is still dissolving in rather than swapping under a settled sky a second and a half later. 358 green.
- **2026-09-02** — The nebula sprite atlas joins the maps on the GPU. Its tile is a sixteen-step march of the region-re-plumbed cloud per pixel, the last CPU seconds of the background; the fourth fragment program marches the same helper — contracted interior, eroded cavity, champagne gate, swept shell and skin — from a float table of per-tile scalars, with the front's sixty-four marched radii packed into texels and the model's own latitude–longitude lookup as an integer texture. The CPU tile split into frame, march and closure so the budgets, peak and escaped share are closed once from whichever march ran. Eight lit tiles at Musas: mean error two parts in ten million of a tile's peak, worst one in seven hundred, escaped shares equal to four digits; 726 ms to 13. The whole background, 5.9 s at the start of the day, builds in 0.42 s cold and 60 ms warm. 358 green.
- **2026-09-02** — The vented residue thins. The champagne gate left a region that had outrun its cloud with a flat five-percent residue everywhere the front reached, cut off wherever a bake box or a sprite tile ended — the last flat cut in the region. Every marched ray now carries its opening, the last radius the cloud still confined the interior, and beyond it the residue falls as the inverse square of distance, a steady outflow diverging through its opening: in the model's helper, the CPU march, the GPU mirror and the sprite program, which reads the vent beside the front in its ray table. Pinned by the falloff along vented rays over every lit region near home; re-A/B'd CPU against GPU at Musas to the usual fraction of a percent. 359 green.
- **2026-09-02** — Where stars are, checked against the model as it now stands. The field is right as built: the catalog starts at thirty million years, a star that old has left its cloud by hundreds of parsecs, and a cloud's gravity is nothing to it, so field stars stand in cloud gas at the gas's own filling factor — measured around Musas and pinned, and the proposed veto dropped as wrong science. Two things were not right. The open clusters were a sprinkle laid out relative to the viewer, the same hundred and thirty offsets from every sky; they are a seeded population of the galaxy now, tracing the dust disk on the arms at the observed density, aged from the clouds' dispersal to the survivors, each with its own members. And every landmark's gateway was a mature planetary system minted at its cloud's centre, where even the thinnest direction out carries one to fifty magnitudes of extinction; a cloud is visited from the nearest real star outside its gas, off its thinnest side — a lit region's venting face — with the camera arriving on the far side of the star so the body lies beyond it. 364 green.
- **2026-09-02** — Stage 5 closes: the region past the Spitzer floor, and photoevaporation. A region's edge is whichever has gone further, the ionized gas's own expansion or the shell its wind and supernovae have swept through the natal cloud — a superbubble with the front trapped in its wall where a supernova wins. And the erosion pass the plan asked for: a ray a clump stops short of the mean front now photoevaporates its face, Bertoldi and McKee's boundary layer streaming off at the sound speed for the region's age, so the face loses a column — thin gas eaten far, dense clumps barely, a trunk pointing at its star — in the model's rays, the CPU march, the GPU mirror and the sprite. Trunks are transient as the real ones are: every stalled ray stands at a third of a megayear, most are eaten through by the regions' own ages. 367 green.
- **2026-09-02** — The volumes the camera stands inside march together. Each volume was its own dome composited whole over the next, which no order makes right for boxes the camera is inside, whose gas lies before and behind each other's along every ray. A carrier now marches up to three boxes per pixel, each at its own pitch with its own empty blocks leapt, the ray servicing whichever box's next sample comes first — the single march exactly when there is one box — and the viewer hands the enclosing volumes to one such carrier. Measured inside Musas with two boxes enclosing the camera: the same GPU time as the separate domes, and the same pixels where their gas never crosses. 367 green.
- **2026-09-02** — Residency ranks by what is at stake. Volumes were admitted by projected size alone, so a bright complex could lose its slot to bigger rifts; the rank is now the cloud's solid angle times the radiance it puts on the sky — its own light for a lit cloud, the band it blots out for a dark one — so a complex outranks a rift of its size and rifts still rank by size among themselves. 369 green.
- **2026-09-02** — The console's navigation became a ladder ("the tabs on the sidebar don't have much space for another tab… create totally new designs that follow their own logic"). Four alternatives were drawn on a canvas beside today's bar — a containment ladder, a kind-within-reach query, an instrument rail, a log-scale ruler — and the ladder was chosen: the universe nests, so the console's navigation is the path from the galaxy down to the world under the camera, one rung per level (galaxy, sector, nebula, nearby, system, world) naming the thing you are inside there and how much it holds, with the marks as a tray beneath. One rung stands open and lists its contents; opening another is browsing and moves nothing — the camera changes only when a body is clicked, and a click always frames its target at that body's own scale (a neighbour star arrives as a star, a cloud as a cloud, the system map from its own row). Two rungs are new: the sector lists its anchor complex, the nebulae and rifts its territory claims and the open clusters in it, each a destination (a cluster is visited from the nearest catalog star to its core); the nebula rung names the cloud the locale stands in or off, lists its ionizing stars, and every cloud within 300 pc. A worker charts each arrival's surroundings off the frame loop — a province is swept around its anchor so it reads the same from every system inside it. Also: the console folds away on a wide screen and the corner toggle brings it back, the viewer resizing with it; the corner instruments carry the console's own instant tooltips instead of the OS's; the dice says what it does (a random system, not a new universe); the sector chart starts off; a capture button saves the view as a PNG at twice the display's pixels (capped at 8192 px a side); and the welcome is one short screen whose galaxy choice now actually appears — the old second step never could, because the address bar carried the galaxy before Continue was clicked. Browser-verified: rung switches leave the camera still, planet and cloud rows frame their bodies, fold/restore, tooltips, a 2440×3600 capture, the phone drawer. 369 green.
- **2026-09-02** — The open rung decides what the cursor reaches for, and the galaxy rung is where galaxies are switched (both "solid solutions, go for it"). Hovering a crowded sky used to be a fixed order — body, glint, cloud — so a nebula's face was rarely reachable through the stars over it; now the ladder says what you are looking for: with the sector or nebula rung open a cloud under the cursor beats any glint over it, with the nearby rung open the clouds stand down, with the system or world rung open only the scene's own bodies answer, and a held Shift (stars) or Alt (clouds) overrides the rung. The galaxy rung gained a Galaxies section — the catalogue's four, home, and any galaxy holding a mark, each a reboot into its centre, plus "a new galaxy" rolled from a fresh seed and a line naming home with a "make this galaxy home" action — so the marks tray lists only marks, grouped by galaxy. The home choice lives in one module the welcome, the boot and the rung all read. The ride-out chip is hidden for now ("we'll revamp that feature later"); the viewer keeps the ride. Browser-verified: each rung sets its preference, the galaxy rows and the home line render, the marks tray shows only galaxies with marks. 369 green.
- **2026-09-02** — The console is resizable on a wide screen ("that fixes the fitting problem and is nice"): a grip along its outer edge drags the width between 320 px and six tenths of the window, a double-click puts it back to 390, the choice is remembered per browser, and the view follows through its resize observer. Wider, the rung heads stop truncating their kinds and counts. The phone layout stays as it is by decision — this is not a mobile app. Browser-verified: a drag to 525 px, the saved width, the canvas refilling the remainder, the double-click reset. 369 green.
- **2026-09-02** — The rate slider became a clock, and the corner instruments one bar ("B with D, removing the time per second bit"). Four alternatives were drawn beside today's strip — a ratchet, clocks, a scrubbable timeline, a whole instrument bar — and the clocks were chosen inside the bar. The rate is now named by a clock the focus carries and how long one turn of it should take on screen: a world offers its day, its year and its first moon's month, a moon its day, month and the parent's year, a star its rotation and the innermost and outermost orbits, the system map the orbits, an asteroid its spin and orbit, the centre the last stable orbit's period; real time is always first, and the durations are 5 s, 30 s or 3 min. A day every 30 s reads at once, and the setting follows the body across travel. The pause, the clock, the seed chip and the dice, the decals, the capture and the settings cog now stand in one captioned bar along the foot of the view — time, address, instruments — with the cog down from its top corner and its menu opening upward; a narrow view sheds the seed's hex before the bar wraps. Browser-verified: the clocks change with the focus, the rates match period over duration, the label sits level with the choices, the menu opens above the cog. 369 green.
- **2026-09-03** — The galaxy level is one list ("the galaxy level is too busy"): the hole at the centre first, then every sector nearest first, each visited at the complex it is named after — the nuclear-cluster row, the bulge line and the section titles are gone, and "complex" is gone with them: the galaxy lists sectors, the sector lists its nebulae, rifts and clusters, and one word names the territory throughout. The galaxies moved up a rung: a Universe rung above the galaxy holds the galaxies the survey can name, the fresh one, and home — the ladder's own logic, since the universe holds the galaxies. Every pick now opens the rung its target's level lists it under ("clicking a complex in the galaxy list opens the Sector section"): a sector opens the sector, a star its system, a world its moons, a cloud the nebula rung, the centre the galaxy. Lists with one kind of row lost their titles and their footnotes — the belt lines, the "nearest 80 of…" note — and a focused moon's world rung shows the parent world then its moons. 369 green.
- **2026-09-03** — The clocks say whose they are ("it's unclear what rotation means here"): the time group's caption names the focus — time · Zika CUPS — and the clocks are named for what turns: a star's spin and the orbits of its innermost and outermost planets by letter (b orbit, f orbit), a world's day, year and month, a moon's day, month and year, an asteroid's spin and orbit. A place where nothing turns — a cloud, the galaxy map — no longer offers a lone "real" with dead choices beside it; the chip says real time, nothing here turns. The world rung lists a moon's siblings without the parent row, which the rung's own head already names. And the generation readout is one status line at the foot of the console, always there — idle when nothing runs, otherwise what is being built with a hairline fill for the busiest producer — instead of rows that appeared and folded and shifted the ladder above them. 369 green.
- **2026-09-03** — Planets are lit by their own sun ("I think there is a problem in how we render planets, their color, and the sun they're orbiting"): the audit found illumination taking the star's peak-normalized hue with no luminosity or distance in it — a Y dwarf's 14 K world rendered as brightly as Earth — and the fix makes the light carry instellation: star hue × the adapted level (L/d²)^0.2 × the star's live pulsation/flare multiplier, the same one exponent the moonlight lift already used, so sunlight at one AU still displays at one and the second sun rides the host's level. The air became one model for everything lit (`render/lighting/surfaceLight.ts`): a per-channel vertical optical depth from the atmosphere's column mass and its own scattering tint (Earth: a tenth of green), slant air mass with a √(πR/2H) horizon, direct beam exp(−τX) plus two-stream skylight fading through a modeled twilight — so sunsets redden, hothouse grounds go dim under a bright deck, and the sun disc and corona seen from the ground take the same transmittance along the camera path. Terrain, sea, magma, scattered rocks, and cloud shells now carry the eclipse and ring-shadow code (a focused moon sits in its parent's shadow; the parent's ring band crosses its ground), each light's penumbra takes that light's own angular radius, oblate bodies use the true inverse-transpose normal, the second sun gets specular and haze-rim terms, and the four different star-tinted night floors became one adapted skyglow floor that is zero from orbit. Typecheck, 387 tests, and the build pass; browser-checked on the F9V Earth-twin from orbit and at a 1° sunset (sun transmittance 0.26/0.12/0.02 R/G/B) and on a T6 dwarf's inner rocky (dim, deep red). Disclosed gaps: the resolved star disc keeps its fixed display seat rather than T⁴ under the same adaptation; bodies seen through a ground's air (moons, a parent planet) are not extinguished; giant decks and the sky dome's tint skip the air model.
- **2026-09-03** — Eclipses from the ground ("lets try to find the eclipse daddy planet and then do the sky work"): the sky dome now carries the analytic occluders and samples the shadow where the scattering air actually stands — a scale height up overhead, out at the horizon column near the limb — so the umbra darkens the zenith while the horizon keeps the twilight ring of totality; the sun disc and corona left their fixed display seat for the same adapted scale everything else uses: a disc's radiance against a white ground under its own light is F/θ², so it displays at adapted(F)·θ^(−0.4) (about nine times the ground under our Sun, reddening through the air at sunset, standing over the sky's glow), with the K-corona a millionth of that on the adapted scale — visible only once the sky is shadowed — and both settling back to the resolved seat as the disc fills a few degrees and the eye adapts to it. Seed sweeps found the Earth coincidence readily (a moon within 10% of its sun's width on ~10% of systems; `50e05cbb0682f9bd` planet 7 is a G3V super-earth with a 31.6′ sun and a moon that swings annular to total) and, among star-locked worlds where the sun never moves, five that eclipse on every moon orbit — all cold M-dwarf rockies with a moon three radii out, `b91c338cbc577d9d` planet 2 (M8V, 1 bar N₂, 155 K) the best at 306 eclipses in 120 days. Browser-verified at totality on the G3V world: black lunar disc, corona ring, dark zenith, twilight band. Both timing probes live in the session scratchpad, not the repo — an in-app eclipse finder is the natural follow-up.
- **2026-09-03** — Eclipse-world follow-ups ("real bug when viewing a moon, it's invisible"): the vanished moon was the eclipse world's inner moon standing inside its parent's umbra — 52 km, three radii out, it spends about an hour of every nine-hour orbit in full shadow, and with the night floor now zero from orbit it renders black against black; browser-verified lit again as the orbit carries it out. Two real defects found on the way: a body lighting the ground by reflection sat in its own occluder list and eclipsed its own shine (each light now carries a reach, infinite for a star, the near surface for a shining body), and an occluder smaller on the sky than the star darkened the ground to its umbra as if it covered the whole disc (a shadow now deepens at most by the occluder's share of the disc, so a small far moon transits rather than eclipses). Reported one-frame black rectangles blanking the moon and the galaxy glow while stars stay lit were not reproduced in 600 pumped frames or a screenshot burst; the pattern points at a multiply-composite pass (volume/backdrop) rather than lighting, and remains open.
- **2026-09-03** — The black flashes, found and fixed ("the flashes have happened for a long time, go ahead and try to replicate it and find a fix"): reproduced by pumping frames through a MessageChannel loop while the camera orbits, reading the half-float scene buffer before the bloom pass on every frame. One frame in sixty to a hundred carried a single NaN pixel at the projected centre of the parent planet's disc — the atmosphere shell's fresnel raises one minus the view cosine to a fractional power, and two unit vectors can dot a hair past one, so the base went negative and `pow` returned NaN. The bloom's high pass carried that one texel into the separable blur, which spread it along a row at the coarse mips and then down every column, and the additive composite painted a full-height black band over the frame (the diagram pass, drawn afterwards, survived inside it — the tell in the recording). Two fixes: every fresnel-style base is clamped (atmosphere shell, ocean and magma sheen, aurora curtain path, and the new twilight root, which had the same flaw), and the bloom's input is guarded ([bloomGuard.ts](src/render/fx/bloomGuard.ts)): NaN and infinite texels count as dark and nothing above the half-float ceiling goes in, so a bad pixel can only ever be a pixel. Verified: 846 orbiting frames, zero NaN pixels, zero flashes; typecheck, 387 tests, and the build pass.
- **2026-09-03** — The sky is the column ("lets start with 1 then"): the ground-level dome left its painted scattering for the same per-channel optical depth the ground stands under — sunlight singly scattered by the column in closed form (the beam extinguished on its way down to each parcel and the light on its way out to the eye, Rayleigh's phase, the ground's own twilight after sunset), in the units of a white ground under the beam, thinned by the eye's altitude into space and still shadowed where its air is eclipsed. Blue at the zenith and grey toward the horizon at noon, orange-brown along the horizon with a dark zenith at a two-degree sun, black in a vacuum; a test pins the dome's hemispherical integral to the two-stream skylight the ground model receives, so the two are one model. The corona left the compressed seat for its true ratio against the ground (a millionth of F/θ²), which a daytime sky outshines and a shadowed one shows. Disclosed: pure Rayleigh, no aerosol — a real sky is two to five times brighter and its setting sun redder than this one, whose disc the tonemapper still washes pale; daylight washout of the star backdrop remains the elevation-driven seat, since the backdrop's points live on the sky display law rather than the ground's linear scale. Verified on the F9V Earth-twin at 72° and 1.7° sun; 392 tests, typecheck, and the build pass.
- **2026-09-03** — Everything in the sky is seen through the air ("yeah, lets keep going"): one shared chunk ([airView.ts](src/render/lighting/airView.ts)) gives every material a sky holds the transmittance along its own sightline — the column above the eye, thinned by altitude, over the sightline's air mass — and the viewer seats it each frame on the bodies (solid and banded spheres, atmosphere limbs, aurorae, rings), the resolved suns and their coronae (replacing the CPU-side extinction the sun disc alone had), the three star-point tiers, the backdrop's points and glow and nebula domes, and the volume composite, whose sightline is rebuilt per pixel from the inverse projection. A moon setting behind a planet's air now dims and reddens the way its sun does, and the Milky Way fades into the horizon. Zero depth seats a vacuum, an envelope, or orbit, and the chunk costs one exponential per fragment or point. 392 tests, typecheck, and the build pass.
- **2026-09-03** — The way out through the air ("yes sir, amazing work, lets keep going"): the view-path half of the air model, one closed-form integral for what a surface's light keeps on its way to an eye above the column and what sunlight that column scatters toward the eye, seated on the distant solid sphere (limb darkening and the blue limb a planet shows from space), the gas giant deck under the clear column above its visible tops (the painted limb, haze veil and rim are gone — `uHazeAmount`, `uHazeColor`, `uRimColor` with them), and the atmosphere limb shell, which now yields to the body over the disc and draws only the air beyond it as the tangent column's own scattering, Chapman's horizontal air mass at the grazing height. Inside the air the same physics runs along the straight segment from the eye to what it sees — exact for an exponential atmosphere, capped at the horizon column — so the terrain family, the sea, the rocks and the cloud deck lose their fog tint and density knobs for a derived aerial perspective: blue by day, red under a low sun, black in a vacuum. The envelope's visible deck sits where the column above it is still thin (`deckOpticalDepth`). Six more mirror tests pin the column integral against the sky seen from below and the segment column against the vertical and horizon limits; 398 tests, typecheck and the build pass.
- **2026-09-03** — Haze, and the stars by day ("lets take 2 of them from the list"): the column now carries two scatterers. The gas's Rayleigh depth comes from its column mass, its scattering per bar (CO₂ more per molecule and heavier, hydrogen little per molecule but many to the bar) and λ⁻⁴ across the bands; the haze is a class property with its own depth and tint — a clear terrestrial sky's thin faintly-blue aerosol, a giant's stratospheric haze, Venus's sulfur veil, Mars's dust, Titan's tholins — since the dust in a thin CO₂ sky is what makes it, not how much gas holds it up. One integral still serves both (they share the scale height): the phase is their weighted mix of Rayleigh and Henyey-Greenstein at g = 0.7, and the two-stream back-scatter their weighted half and sixth, so every consumer of the column — ground, sea, clouds, dome, discs, limb, aerial perspective — took the haze without changing shape. A sunset sky now glows wide and orange around its sun instead of a thin Rayleigh band. The star backdrop's daylight washout left its elevation seat for the eye's own adaptation: the sky display law seats the points for an eye settled on the night sky, everything on screen scales with the level the eye has settled on, and the level is what this ground's column hands down at the camera — beam and skylight over the eclipse shadow at the eye — so the same points display at the night seat over the adapted ratio: 2×10⁻⁶ at noon, 3×10⁻⁶ at a 1.7° sun, 2×10⁻⁴ at civil twilight's end with only the brightest points showing, and the whole sky by −12°, or under a total eclipse. Browser-verified on the F9V Earth-twin through that sequence; 401 tests, typecheck and the build pass. Learned the hard way: the console's rate buttons overwrite `timeScaleDaysPerSecond`, and pausing the clock parks the render loop — test at the real rate.
- **2026-09-03** — The setting sun, after Codex's review of the visuals: the fireball it saw was the pre-haze disc under bloom, and the white circles beside it are the planet markers, which stay UI decals by request. Two real corrections came out of it. Extinction now goes in under the disc's own adapted power (the seat is the adapted radiance, so what the air takes must be taken there too), which turns the setting sun into a bounded pale-orange disc still brighter than its sky instead of a dark spot on it; and the column refracts every sky vertex — Sæmundsson's fit at true altitude, scaled by ground pressure over temperature — so suns, moons, planets and star points stand higher near the horizon and a disc flattens as its lower limb lifts more than its upper, emerging from the same rule that lifts the whole. 404 tests, typecheck and the build pass.
- **2026-09-03** — The fireball, second look ("I'm seeing the same thing"): at the default field of view the thing that ballooned was never the disc but the haze's aureole — one Henyey-Greenstein lobe at g = 0.7 is broad enough to hold the sky within twenty degrees of the sun at a sunlit white ground's brightness, and the tonemapper and bloom made that whole region white. The haze phase is now two lobes, half each: the sharp diffraction peak a few degrees wide that is the aureole, and a wide shoulder for the horizon; back-scatter follows at an eighth. A clear terrestrial sky's aerosol depth is a tenth, the rural clear-day value, not 0.15. At a 30° sun the blown region is now a few degrees across; at 1.7° the disc is a small bounded sun in a modest glow. Verified at the default field of view on the G3V world; 404 tests, typecheck and the build pass.
- **2026-09-03** — And the polygon itself: with the aureole made sharp, the dome's coarse triangles showed it as a diamond — the fragment stage took the interpolated direction unnormalized, shortest at each triangle's middle, and the peak faceted along the edges. The direction is renormalized per fragment now. (Four unrelated tests — the Kerr integral and the galaxy catalog — failed once while the browser was rendering alongside the suite and pass alone; timing, not the model.)
- **2026-09-03** — The sky from orbit ("a small orbit hides the sky as the camera passes the terminator"): the star washout had taken the ground's irradiance at the camera as the eye's level, and that term never thins with altitude — from orbit on the day side it still read full sun and wiped the backdrop, the nebulae and every star tier off the sky, switching on at the terminator. The level is now the sky's own radiance above the eye, this sun scattered by the column overhead over the eclipse shadow at the eye, and the column thins with altitude, so from orbit the sky is black and every star stands on either side of the terminator; a sunlit disc in the view is a thing the eye looks at, not a sky it stands under. Ground noon still reads a few millionths. Verified on the M2V snowball from your link.
- **2026-09-03** — The bright "reflection" on hazy worlds ("a suns reflection from the surface of a planet seems unrealistically bright or defined"): a sign. The view-path scatter took the cosine of the angle between the sun direction and the direction to the eye, where the scattering angle runs from the beam's own travel to the way out — so a fully lit disc, sun at the observer's back, was handed the haze's forward peak as a bright defined blob at its centre, and a backlit crescent the dim back-scatter, the wrong way round; a full moon read as shiny for the same reason. The dome and the limb shell had it right; the solid sphere, the giant deck, the terrain family and the cloud deck did not, and do now, with the convention written once at the scatter function. The "grid" on the Titan-like world was not in the frame — a pixel walk across its disc steps 62→58 with no discontinuity — but eight-bit banding on a gradient a few levels deep, which a capture turns into blocks; the final copy to the screen now dithers before it quantizes.
- **2026-09-03** — Solid-world clouds stopped being another haze layer. The old model was a color and one coverage number — Titan's global tholin aerosol became a second 100%-opaque “cloud” shell, every condensate deck sat at the same fraction of a scale height, and one softly thresholded fBm value owned footprint, opacity and lighting — so the result could only be a translucent stain. Condensate is now its own physical layer: water, CO₂ ice, sulfuric acid, methane or mineral vapor, with coverage, visible optical depth, top and thickness in kilometres, synoptic feature scale, wind drift, convective relief and a day/night bias. Titan keeps its orange aerosol in the air column and gets sparse bright methane clouds; a hothouse gets a high global acid deck; ocean, frozen and dry nitrogen worlds separate by moisture and convection; locked wet worlds gather convection toward the star while rock vapor condenses on the cold side. One shared domain-warped weather field draws both the distant sphere and focused shell, separating footprint, density, relief and radiometric structure so optically saturated overcast still has form; optical-depth opacity gives thick cores broad translucent margins, relief normals give tops and shoulders light without black cut-paper edges, and an eight-step near-deck integral gives the modeled thickness billowing structure instead of a geometric sheet. The world plate discloses condensate, coverage and cloud-top height. Browser A/B on the K3V ocean world `000000000000002c` planet 4 and the M2V hothouse `ca7381cb135273b1` planet 2; 418 tests, typecheck and build pass.
- **2026-09-03** — The close cloud volume no longer reveals its sphere mesh as a grid of circular holes. Its ray march had started at the interpolated face of the 256×128 shell; each flat chord sags inward between vertices, and on a shallow deck that put polygon centres below the cloud base. March bounds now come from analytic camera-ray intersections with the inner and outer cloud spheres, every close-range lighting position uses the same exact boundary, and the non-facing transparent pass is discarded so it cannot integrate the same volume twice. Reproduced and browser-verified through orbit, deck and ground on reported seed `f68fb8159095e042`, planet 9: no circles, curved bands or shader errors.
- **2026-09-03** — Cloud edges lost the cut-paper contour: the weather field now crosses a wider derivative-aware condensate fringe, fine noise breaks that fringe into wisps, and opacity combines a thin linear optical contribution with the dense cubic core. The core remains opaque while its outskirts fade gradually instead of jumping from clear air to saturated white; the close volume inherits the same footprint fade. Browser-checked at orbital and near-deck scales on `f68fb8159095e042`, planet 9.
- **2026-09-03** — The console under the mouse ("even when the scene itself is rendering at 50+ FPS, the UI feels like it's running at 2 FPS"): measured in Chrome with the Event Timing API, a hover over the ladder reached the screen 82–88 ms after the mouse moved, with script at 2–4 ms a frame and nothing long on the main thread. The compositor paints the hover into the same frame as the canvas and waits behind the three or four scene frames the GPU still holds, so the lag is GPU milliseconds: 54 ms at a 1× render scale, 24 ms with rendering off, 40 ms with one frame in flight behind a fence. The frame was then cut where the ground bisection put it. The five-level UnrealBloom, thirteen passes each paying a tile-based GPU's load and store, became a three-level pass of the sim's own ([compactBloom.ts](src/render/fx/compactBloom.ts)) with the NaN guard and input ceiling in its bright pass and the composite folded into the additive blend — its two coarsest levels at 0.018 and 0.004 had shown nothing — and the ground frame fell from about 24–30 ms to 16–17 ms. The cloud shell's non-facing side now leaves before sampling the weather (about 8 ms to 4 ms). Every lit shader keeps its second sun's shadows, lighting and scatter behind one uniform gate (`secondSunLit`), so a single star pays for one. The zenith radiance that seats the directional star visibility is computed once a frame (`airZenithRadianceGreen` in [airView.ts](src/render/lighting/airView.ts)) instead of in every sky fragment and star vertex, and the stellar tiers take the air once per frame with the sun's geometry in hand. One correctness fix on the way: the corona's linear ratio to the ground grows as the square of the distance and passed the disc itself beyond 25 AU (2.59 against a disc of 9 from 11.9 AU); it is now bounded by the disc's own adapted seat, a millionth of the disc on the display's scale (0.57 there, unchanged at 1 AU). Browser-verified on the F9V Earth-twin's ground, star and outer-planet views with before/after captures at one camera and sim time; typecheck and 427 tests pass. Still open: the absolute hover lag follows whatever else the GPU is doing, and the fps readout counts rAF ticks rather than rendered frames.
- **2026-09-04** — The orbit controls, made one thing ("their feel is so inconsistent and hacky" … "I'd like the camera to not rotate on its own during any transition" … "space has no up so we shouldn't have gimbal locks"): OrbitControls is gone. In space the drag is a free arcball ([orbitArcball.ts](src/app/orbitArcball.ts)): a left-drag or one finger turns the camera bodily about its anchor in the screen's own axes, so whatever sat under the cursor follows the hand however the view happens to lie, the anchor stays where it was in the frame, and there is no pole and no stop — over the top is just more of the same. The wheel scales the distance to the anchor and nothing else, and nothing in space ever touches the camera's orientation but the hand; the glide after release keeps a time constant rather than a per-frame factor. The ground is the one place with a true up: inside the horizon band (below 0.12 R) one gaze function ([cameraGaze.ts](src/app/cameraGaze.ts)) aims the camera — the head's heading on top of an altitude-driven tip from nadir to the horizon, the head's pitch coming in with the tip so the top of the band is exactly nadir and the floor exactly where the head aimed, and the flight camera's tangent frame shared. Crossing into the band the head reads its heading off whatever lies up the screen, and climbing out the arcball takes the view as the head left it, so neither hand-over moves the view by anything. A flight had ignored any wheel event under 12.5 delta units, so a slow Mac wheel could not leave the ground; outward wheel input now sums across frames until it adds up to a departure, a pointer lock granted after its drag ended is released on arrival, and the wheel reaches the canvas while it holds the lock. The ride from the ground to orbit is four decades deep; events in quick succession ramp the wheel gain up to 2.8× so a sustained scroll crosses it in about twenty notches while a lone notch keeps its 1.17× step, with lines and pages normalized to pixels. Verified in the browser on the Earth twin: flight leaves on the third delta-4 event, the two hand-overs at the band top move the view by nothing (only the altitude tip does), a radial zoom from orbit to 600 pc changes it by 0.00001°, a pause changes nothing, a drag after a surface visit and a drag at 600 pc both move the scene along the hand, and a vertical drag runs straight over the top and on round.
- **2026-09-04** — The reorient gizmo ("a reorient button would be nice … a small orbit control widget, like in 3D software kinda, but more built for space"): four prototypes were benched side by side on a live arcball (compass, axis gizmo, globe, cluster) and Ben chose the compass with the other controls as icons at its four corners. It stands at the bottom-right of the view above the instrument bar ([reorientGizmo.ts](src/app/ui/reorientGizmo.ts)): the gizmo knows which rung the camera stands on and names it under the dial — ground, world (below the map height, where the body fills the view), system (the map, until the whole system spans under a fortieth of the altitude), galaxy, or core — and the needle is where that rung's pole lies on screen: the body's spin axis, the system's orbital pole (a planet's obliquity away from its own axis), the galactic pole, or north on the ground; it shortens as the pole leans toward or away from the eye and resting dim when it is up. Pressing the dial is the one rotation the camera makes because it was asked to: a roll about the view axis, 350 ms eased, anchor pinned ([reorient.ts](src/app/reorient.ts)); end-on there is nothing to roll to and the dial says so. The corners: return to focus (top-left, lit only when the anchor is off the focus; it turns the view to the focus from where the camera stands and re-anchors there — the old top-centre chip is gone), ride out (top-right, toggling the pull-back to the galaxy, which had no control since the chip was hidden), face-on (bottom-left, down the pole from its north side, screen-up kept), edge-on (bottom-right, along the plane with the pole up). On the ground only the dial and ride out remain, the dial facing north and levelling the gaze. Any drag on the canvas takes over from a move in progress. Browser-verified on the Earth twin: needle 13.7° → 0 with the position unmoved, face-on and edge-on exact, the focus corner enabling on a pan and re-aiming on press, the ride toggling, the ground dial zeroing heading and pitch.
- **2026-09-04** — The corner chrome, regrouped around the gizmo ("move the decal buttons to a row of 4 above the gizmo … move the camera and settings icons to the top right corner … remove the address section entirely … gizmo will now sit in its bottom right corner"): the instrument bar is a clock strip alone now ([clockStrip.tsx](src/app/ui/clockStrip.tsx)); the four decal switches are a rail of orbs under the gizmo (they began above it; under looked better); the shutter and the cog are orbs at the top-right ([instrumentsCorner.tsx](src/app/ui/instrumentsCorner.tsx)), the menu opening downward; the address chips (seed hex, copy, dice) are gone, the URL carrying the seed; and the gizmo stands in the very corner, widened to a 124 px square so its four corners fall in line with the rail's outer orbs. One `.orb` style serves every corner button — the gizmo's four moves, the decals, the shutter and cog — and the touch chrome stacks above the gizmo. On a phone the clock takes the whole foot and the column stacks above it. The frame-timing readout at the top-left is a pill in the same style now, the rate inside it; a press opens the full readout in its place, corners as round as the pill's ends, and a press on that folds it back.
- **2026-09-04** — The camera keeps to the sky above the map ("when we zoom out far enough to be considered in the system level, I'd like it to stop being locked to the planet and instead just orbit it"): the scene is a ground-fixed frame — the heliocentric world sweeps around a spinning solid focus so its terrain can stream in place — and the camera, standing still in that frame, rode the ground with it, so from the map height the star, the orbits, and the whole sky wheeled round the planet once a day (once every thirty seconds at the opening rate). Now above the map height ([unifiedViewer.ts](src/app/unifiedViewer.ts) `updateWorld`) the camera is turned each frame about the body's axis by the same angle the frame turns — its position, its orientation, the anchor it orbits, and any move the gizmo has in hand (`turnAbout` in [reorient.ts](src/app/reorient.ts)) — so it keeps to the sky: the body turns under an orbiting eye, the system stands still around it, and the planet stays exactly where it was on screen. Below the map height nothing changes: the terrain streams and the horizon band hands the view to the head in the ground frame as before. Crossing the map height either way changes only how the camera moves from then on, never where it is, so no hand-over is visible. The camera still moves with the body along its orbit, as a satellite would. Verified in the browser on the Earth twin with a day every thirty seconds: at 2.2 radii the camera is fixed in the scene and turns 14° against the sky over a 14° spin; at 30 radii the camera is fixed in the sky to the frame's precision over a 20° spin, the planet stays at the centre of the frame, and a dial press mid-spin still lands the pole up the screen to a thousandth of a degree.
- **2026-09-04** — The clock, as a slider again ("we definitely need a slider type control … but something new still"): four slider designs were benched on a live orrery (a pill rail, a dial, a transport-style scrubber, a lens) and Ben chose the lens, without the strip's caption and with the axis spanning the whole row. The clock strip is now a pause orb and a pill: the rate as a multiple of real time and what it means here — "×1.2k · a day every minute", "×4.5k · an orbit every minute" ([clocks.ts](src/app/clocks.ts)). The pill is the slider ([timeControls.tsx](src/app/ui/timeControls.tsx)): press it and drag sideways and the rate axis unfolds above, from the pause orb's left edge to the pill's right, real time at one end and ×10⁸ at the other, and the cursor steps between detents ("instead of a continuous flow through the ranges, I'd like to have detents on expected chunks"): real time, then the focus's one clock at every pace from a turn an hour down to a turn a second — hour, 30 minutes, 15, 5, 3, 2, minute, 30 seconds, 15, 5, second — spaced evenly along the axis whatever the rate, so the slider reads as how fast the clock turns rather than as a number line. The clock is the one motion you watch at the focus: the day at a world, a moon, or an asteroid (from the ground or a close orbit the turning you see is the body's own; a year's paces would spin an Earth's day in ten seconds at their slowest and a Jovian one in a third of a second), the star's rotation at a star, the innermost planet's orbit on the system map (the one lap you can always follow, every other orbit slower), the inner edge's orbit at the core; nothing turns at a nebula or in the galaxy view, and the pill says so. This took two rounds: the first slider ran each of the focus's clocks in turn — a day's paces, then a month's, then a year's, shortest clock first, with a line above the axis when a close moon's month came before the day ("this world's day is longer than its month") — and Ben found the units didn't fit ("I liked day/month/year without thinking about it much and honestly it just doesn't really fit for most planets and systems … lets have the scale unit be whatever the correct scientific unit should be, i guess orbit?"): the orbit is that unit from space, the day from the ground. Real time and the clock are ticked, the clock named where its run begins; the paces along it are felt, not shown. Letting go folds the axis away. A double-click is real time, a scroll steps a stop, arrows step, shift-arrows leap five. Each focus opens at its clock turning once a minute (it opened at half a minute first; Ben asked for the minute). The pill keeps one width whatever it says, so the axis never shifts under a drag. Pause keeps the rate.
