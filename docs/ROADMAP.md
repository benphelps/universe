# Roadmap & Progress

Milestones build outward-in: foundations → one star → its system → its worlds → their surfaces → the galaxy around them. Each milestone ends with something visible and a test suite locking its physics. This file is the living progress record — check items off as they land.

## M0 — Foundations ([plan](plan/00-foundations.md))

- [x] Vite + TypeScript + Vitest scaffold, layering rule enforced by test (`core/`+`universe/` free of DOM/Three)
- [x] Seed tree + PCG32 + distribution toolkit, determinism property tests
- [x] Units/constants
- [x] Branded unit types at API boundaries: enforced `Mu`/`Seconds` brands on the Kepler/orbit functions (the AU-vs-SI and days-vs-seconds bug classes), producers branding once via `core/physics/units`; length/mass/time constants carry their unit types.
- [x] Kepler solver (elements ⇄ state, any-time propagation) + tests
- [x] Noise primitives (3D simplex, fBm, ridged, warp) seeded + worker-safe
- [x] Blackbody → CIE → sRGB pipeline + temperature-color LUT + tests
- [x] Worker protocol + scheduler skeleton (landed with M5: typed request/response protocol, terrain worker pool with transferables and nearest-first dispatch, sky worker with per-seed cache)

## M1 — The Star ([plan](plan/02-star.md))

- [x] IMF sampling, MS relations, evolution stages, compact objects, classification
- [x] Rotation/activity/variability (spots, flares, pulsators) as functions of `t`
- [x] Multiplicity (flat companion list; the full hierarchy tree lands with 03's system builder)
- [x] Star model test fixtures (Sun, M dwarf, red supergiant, white dwarf, brown dwarf) + population distribution tests
- [x] Render: HDR pipeline + tone mapping + bloom
- [x] Render: photosphere shader (granulation, limb darkening, spots), corona
- [x] Render: photometric star sprites — one magnitude/color mapping shared by the backdrop, the 3D neighborhood, and the system's own stars once their discs fall subpixel; brightness follows true camera distance everywhere
- [x] Demo: seed → star viewer (orbit camera around any generated star)

## M2 — The System ([plan](plan/03-system.md))

- [x] Disk recipe, planet slots, element sampling, stability filter
- [x] Zones (HZ, frost line, lock radius); binary S/P-type placement
- [x] Belts + resonance structure (Kirkwood arithmetic), reservoirs
- [x] Fixtures + statistics tests (occurrence, period ratios, stability)
- [x] Render: system map view, live orbital motion, time controls
- [x] Demo: seed → explorable system map with placeholder planet spheres

## M3 — Worlds ([plan](plan/04-planet.md))

- [x] Bulk/interior/mass–radius, rotation & tidal state, magnetic field
- [x] Atmosphere retention/composition/greenhouse; climate EBM + albedo fixpoint
- [x] Appearance parameters (terrestrial palettes, giant banding, methane tint)
- [x] Fixtures (Earth/Venus/Mars/Jupiter/Uranus analogs) + population statistics tests
- [x] Render: planet spheres with banded giants, atmosphere limb, clouds v1 (ground-level sky scattering lands with M5 surfaces)
- [x] Demo: planet view with correct phases and star angular size (eclipses arrive with M4 moons)

## M4 — Moons, Rings, Small Bodies ([plans](plan/05-moons-rings.md), [plan](plan/06-small-bodies.md))

- [x] Satellite budgets, channels, Hill/Roche placement, tidal heating states
- [x] Ring systems with resonance gaps; ring render (phase function, shadows)
- [x] Belt cell instantiation, asteroid shapes/spins, comet activity + tails
- [x] Fixtures (Io calibration, impact moons, resonant chains, Saturn-class rings) + SFD tests
- [x] Render: comet apparitions, moon orbits with eclipse shadows, ring shadows (belt fields landed in M2)
- [x] Asteroid close-up rendering: notable belt asteroids are focusable bodies whose shape spec (ellipsoid axes, contact binaries, seeded lumps) becomes streamed terrain.

## M5 — Surfaces ([plan](plan/07-surface.md))

- [x] Cube-sphere quadtree LOD + worker chunk generation (skirts hide LOD cracks; geomorphing remains the one open M5 polish item, tracked under M7 performance hardening)
- [x] Layer stack: tectonics (Worley plate belts), craters, volcanic provinces, erosion smoothing
- [x] Deeper erosion suites: fluvial valley networks (dendritic ridge-crest carving with tributaries, elevation-scaled depth), glacial carving (freeze-masked trough cutting plus fine-relief smoothing), and dune fields (wind-aligned draa-scale transverse ripples over erg patches with sand tinting) — all pure, LOD-faded, and climate-gated.
- [x] Ice/snow + biome palettes from climate
- [x] Airless small-body surface mode: a dedicated asteroid surface field (shape lobes + saturated craters + regolith cascade to boulder scale) runs through the same cube-sphere streamer; the body stepper walks planets then each belt's landmark rocks.
- [x] Determinism/border tests + world-type fixtures (Earth-like, Moon-like, Mars-like)
- [x] Render: vertex-colored lit terrain, ocean sphere, ground-level sky + aerial fog
- [x] Scatter instancing: worker-placed deterministic boulders (with an airless large-block tail) and biosphere ground cover, instanced per tile with terrain lighting/fog and proximity-based visibility.
- [x] Demo: orbit → descend to the ground of any solid world, unbroken

## M6 — The Galaxy ([plan](plan/01-galaxy.md))

- [x] Density model (disks, halo, arms, dust) + deterministic sector sampling
- [x] Population gradients feeding star properties: thin-disk/thick-disk/halo mix drawn from the local density model, with disk radial metallicity gradient, old metal-poor thick disk, and ancient halo; shared draw sequence keeps sky photometry and generated stars in agreement.
- [x] Procedural sky from local sampling (resolved shells + Milky Way glow with dust reddening) in every view
- [x] Clusters, OB associations, nebulae — emergent from the model: giant molecular clouds are first-class objects (deterministic cells, dust- and arm-weighted, gathered into kpc-scale complexes) with anisotropic noise-carved density fields; the Milky Way glow extinguishes through them so every dark rift is a specific cloud; young groups form inside clouds and the lit natal cloud is the nebula — ray-marched through its own density field with illumination from the embedded group and self-extinction, colored by ionization physics from the hottest member (Hα → O III with temperature, blue reflection below the ionizing threshold) — baked to a sprite atlas in the sky worker; dispersed older clusters ride as bare coeval knots. Glow mean luminosity is IMF-derived; the galactic center sits dozens of optical depths deep, as observed.
- [x] Magnitude-count regression test (naked-eye counts the right order of magnitude)
- [x] Demo: galaxy view of the real 20 pc neighborhood; click a neighbor to travel — its system is waiting

## M7 — Depth & Polish

- [x] **Unified body viewer** (step 1 of the single universe renderer): planet + surface merged — solid worlds are streamed terrain at every altitude, envelopes are shader spheres, everything else (sun, moons, eclipses, rings, atmosphere, backdrop) shared in one scene.
- [x] **Single universe renderer, step 2**: star view and system map folded into the body scene — the unified viewer holds the whole system (real photospheres, planets on their orbits, belts, comets, companions) around whichever body has focus; star/system/planet are camera presets, the map is an orbit-line overlay that appears at map altitudes, and one wheel ride runs from the system's rim to a planet's ground.
- [x] **Single universe renderer, step 3**: galaxy view folded in — the 30 pc neighborhood rides in the scene as true 3D points that double as the night sky's near field (the backdrop keeps only far shells and the Milky Way glow), so every view is a preset of the one renderer and a single wheel ride runs from interstellar space to any planet's ground. Travel to a neighbor rebuilds the system in place. Open: whole-disk galaxy rendering beyond the neighborhood (clusters/nebulae land with the M6 leftovers).
- [x] Clouds over terrain: a translucent deck shell above the highest terrain, sharing the shader-sphere planets' drifting cloud field (the focus view keeps the climate the far view promised), with close-range detail fade-in and a pass-through fade. Open: cloud shadows on the ground; volumetric decks stay a later item.
- [ ] Volumetric clouds; multiple-scattering atmospheres; black-hole lensing pass
- [x] Real far starfield: the statistical shells are gone — every glint in the sky is a catalog star with a seed, hoverable and travelable like the neighborhood. Stars carry their identity (mass, age) in seed bits through the explicit IMF/population inverse CDFs, so the catalog constructs seeds for exactly the bright young stars a far cell can show while arbitrary seeds keep the same distributions; survey depth is mass-stratified (0.91 M☉ turnoff / 2.2 / 7 M☉ out to 90/150/600/2500 pc). Open survey-depth items: sub-turnoff giants beyond 150 pc, hot young white dwarfs beyond the near field, per-star dust extinction, unresolved-binary brightening; cluster/group members are identifiable but not yet individually addressable (their ages live outside the population model)
- [x] Whole-galaxy view: the galaxy renders as a raymarched volume of the same density model the sky integrates — the identical emission/dust/knee/reddening constants as the glow map, marched per pixel from wherever the camera is (fine steps across the disk slab, coarse through the halo). It crossfades in as the sky-sphere backdrop's parallax breaks down (60–450 pc out), the altitude ceiling now reaches 45 kpc, and the wheel rides toward the panned anchor at large scale so zoom goes where you look. Spiral arms, bulge, disk, and dust patchiness all emerge from density.ts; the volume's ray geometry provably matches the 3D neighborhood layer. Open polish: dust lanes aren't arm-correlated (the cloud population doesn't know about arms yet), the clump noise is the statistical limit of the cloud field rather than the clouds themselves, and no bright-star sparkle rides the volume
- [ ] Galaxy population: the universe currently holds one Milky-Way-like spiral; derive multiple galaxies (ellipticals with smooth band-less glows, irregulars, dwarfs) from parameterized density models so a seed's sky depends on which galaxy — and where in it — the system lives
- [x] Belt materialization: belts are full populations — orbital cells (semi-major-axis band × epoch mean-longitude sector, Keplerian-shear-predicted) instantiate deterministically near the camera; each member is a true body rendered as a shaped spinning rock when resolved and a reflected-sunlight glint (with a faint marker floor) when subpixel, and clicking any of them promotes it to a full streamed-terrain focus body. The additive point cloud remains the far-field statistical limit; counts follow a main-belt-normalized SFD, so belts are honestly sparse.
- [ ] Exotic showcases: circumbinary worlds, eyeball planets, Io-class volcanism, pulsar systems
- [x] Constellations: the sky chart now cuts the celestial sphere around the landmarks it actually shows, replacing the old projection of 3D territory adjacency (sight lines marched to the first neighboring province, which named the sky after territories you could not see while the visible nebulae hung unlabeled — in one probe sky, none of the seven visible nebulae anchored its own region and a border ran straight through the brightest complex). The sprite-carried nebulae and rifts — the same first-class clouds the provinces anchor on — rank by salience (angular size, emission counting beyond bulk) and seat a prominence-weighted angular Voronoi: each landmark owns its own face and reaches beyond it by stature, and one that could not hold its own heart against the seated stays a named object inside a greater constellation, the way minor nebulae live inside Orion. A sky region, the landmark at its heart, and the province that landmark anchors share one seed and name, so the chart, the hover tooltips, and the flat map finally agree — and like human constellations the cut is a viewpoint artifact: every home system letters its own sky
- [x] Provinces anchor on real landmarks: each chart territory is the region of a prominent molecular-cloud complex — the same first-class objects that carve the rifts and light the nebulae — selected by dust-mass prominence, with great complexes claiming broader reach through a weighted (power-diagram) metric and jittered frontier anchors only where clouds thin out (outer rim, halo). A province and its anchor cloud share a seed and therefore a name, so naming the nebulae in hover tooltips later is a lookup, not a feature. The map now reads as geography about the galaxy's contents instead of abstract Voronoi noise. The chart is a toggle in the control strip, and the sky's extended objects are hoverable: a nebula introduces itself by its cloud's name ("the Zutou Nebula · molecular cloud lit by its newborn stars"), a dark rift by the same naming ("the X Rift") — containment-only and last in pick priority, so they never steal a star's hover. Open: click a nebula/rift to travel toward it; cluster-member tooltips could carry their natal cloud's name
- [x] Gazetteer polish: provinces are density-weighted (extra territory sites where the stellar density runs high, so sectors run small along the arms and inner disk and sprawl in the rim and halo, capped in the core), and both charts are lettered — province names at their visible centroids on the flat map with home emphasized, and each neighboring territory's patch of sky named on the star map. The flat chart is now a local patch matching the discrete star catalog's 2.5 kpc reach, fading out at that survey horizon, so the whole-galaxy view shows a small charted bubble in an unmapped galaxy; labels withdraw with camera distance to keep the far view clean. Open: name the molecular clouds/nebulae and clusters
- [x] Galactic gazetteer, first cut: the structure the model already carries gets chartable names drawn deterministically from the universe seed — the two spiral arms have proper names, a 400 pc chart-sector grid names every locale, and the structural zones (core, arm, inter-arm gap, outer rim, halo) come from the density model itself. The galaxy panel shows the system's full address, stable across neighborhood hops and consistent for every traveler. Open extensions: name the molecular clouds/nebulae and clusters (they're seeded objects already), show sector names on the whole-galaxy view
- [ ] Ground-frame completeness: ring shadows on terrain; asteroid focuses ignore their random spin axis in the frame the way solid planets honor obliquity
- [x] Free flight, step 1: right-shift + drag pans the camera through space in every view — a screen-plane grab scaled by altitude (meters over a ridge, parsecs across the neighborhood), with altitude re-derived from wherever the camera ends up instead of snapped to the focus sphere, the terrain floor as a hard clamp, and the orbit pivot traveling with the pan (re-anchoring on focus change). Wheel ride, orbit drag, hover picking, and terrain streaming all compose with it. Scoped to the space views: on the ground (solid focus below the horizon-gaze altitude) the original surface controls stand untouched, and descending there re-anchors the orbit on the body — WASD walking is its own future feature
- [ ] Surface free camera, step 2: WASD + mouse-look movement on the ground. The ground-fixed frame is built for this — terrain vertices are static world geometry, up is radial, and the sky/frame contract (see frameQuat in unifiedViewer) keeps every sky object correct regardless of how the camera moves
- [ ] Zodiacal light: belts and comet reservoirs carry a collisional dust component — production from the belt's size-distribution cascade and the comet population, spread inward of the source belt by Poynting–Robertson drift — rendered as the ecliptic-plane sky band it actually is: a faint zodiacal wedge and gegenschein in surface and low-orbit views, emergent from belt geometry, dust column, and stellar luminosity (the frame contract already carries the ecliptic; the backdrop's band machinery is the template). The rocks stay honestly invisible; the dust is what a naked eye would actually see of a belt, and only from inside the system.
- [ ] Performance hardening (belt LOD, chunk budgets, memory eviction, terrain geomorphing)
- [ ] WebGPU renderer evaluation

## M8 — Human Scale ([plan](plan/09-human-scale.md))

Walking the worlds: to-scale terrain from orbit to the footstep, every feature a model object. Ordering: S1 → S2 → S4-graph → S3 → S4-rendering → S5 → S6.

- [x] S1 Boots: WASD + mouse-look ground camera — eye height, body-g gravity, terrain collision, slope limits; land-anywhere transition from orbit and clean exit back to it
- [ ] S2 Ground truth: detail cascade extended ~100 m → ~5 cm with per-substrate band parameters; geomorphing; normal-map micro-detail; chunk budget/eviction and local-frame precision hardening
- [ ] S3 The lay of the land: structural ridge/valley networks with consistent divides, fault scarps, talus at angle of repose, strata in cliff faces, coastal morphology (beaches, cliffs, tidal flats)
- [ ] S4 Water finds its way: global drainage graph (uplift × precipitation → basins, reaches with discharge/width/slope) as first-class model objects; valleys carved by the graph; rivers, lakes, waterfalls, deltas rendered to scale; orbital river traces and walked banks from the same data
- [ ] S5 Weather on the map: spatial climate fields (circulation bands, orographic rain, rain shadows, soil moisture, snowline) feeding hydrology and a first-class Whittaker biome field that colors every scale
- [ ] S6 The living ground: procedural plant species per biome (star-spectrum pigments), deterministic hierarchical placement, instanced trees → impostors → canopy-in-terrain-color LOD chain, undergrowth near the camera

---

## Log

- **2026-08-23** — Repository created; architecture and all level plans (00–08) written.
- **2026-08-23** — M0 core landed (seed tree, RNG/distributions, Kepler solver, noise, spectral color pipeline) and M1 star level landed (full generator with evolution/classification/activity/multiplicity, HDR photosphere+corona rendering, interactive viewer). 33 tests green; verified in-browser across K/M/A dwarfs and a red giant.
- **2026-08-23** — Corona reworked into a star-anchored 3D field: true parallax when orbiting, co-rotation with the surface, activity-driven structure over spot bands.
- **2026-08-23** — M2 system level landed: disk→slots→elements→stability generation with S/P-type binaries, zones, Kirkwood belts, reservoirs, and end-state effects (engulfment, supernova sterilization); system map view with live Keplerian motion and shearing belts. 45 tests green; verified in-browser on a circumbinary resonant chain and an M-dwarf eyeball-world system with a gapped asteroid belt.
- **2026-08-23** — M3 planet level landed: full characterization (bulk, interior, rotation, atmosphere retention/greenhouse, climate fixpoint with snowballs and biospheres, appearance) plus planet rendering (solid worlds, banded giants, scattering limbs) and a true-scale planet view with exact phases. 54 tests green; verified in-browser on a biosphere crescent, a banded gas giant, a brown-dwarf-lit mini-Neptune, and a locked ocean world.
- **2026-08-23** — M4 moons/rings/small bodies landed: satellite systems with Io-calibrated tidal heating (volcanic/cryovolcanic/subsurface-ocean states), Roche-limit rings with moon-resonance gaps, deterministic belt asteroid instantiation, and comets. Rendering: analytic eclipse shadows (moon transits, ring shadow bands), forward-scattering rings, live moons in the planet view, comet apparitions in the map. 67 tests green; verified on a ringed super-Jupiter around an L8 brown dwarf.
- **2026-08-23** — Comet legibility and moon discoverability fixes: activity-capped comas, physical dust-tail lag, motion trails; moon orbit guides and adaptive marker dots in the planet view.
- **2026-08-23** — M5 core landed: pure surface fields (plate-belt mountains, lattice-cell craters, erosion, solved sea levels, climate-driven color), worker-streamed cube-sphere quadtree terrain with floating-origin rendering, and the surface view descending from orbit to the ground. 75 tests green; verified on a crater-field Mercury analog and a locked habitable world. Open: fluvial/glacial erosion detail, scatter instancing, geomorphing, asteroid surface mode.
- **2026-08-23** — Surface shakedown from live testing: OrbitControls-based camera (no more flipped axes), seam-free border-ring normals, priority-ordered chunk requests, detail-band cascade to ~100 m features with LOD fading, chunk-aligned water tiles (ocean z-fighting gone), GPU mottling and micro-relief shading, relief-scaled color windows, minimum LOD floor for orbital views.
- **2026-08-24** — Single-renderer step 2: star, system, and planet views unified into one focus-centric scene (km units, focused body at the origin, everything else translated in doubles). The real photosphere is now every planet's sun — the additive sun proxy is gone — with companions on barycentric orbits, other planets at true positions with adaptive markers, belts/comets/zone rings folded in, and the orbit-line map as an altitude-triggered overlay; the sky dome went additive (scattering adds light, never occludes) and scales with surface pressure. One wheel ride runs from the system's rim to any planet's ground. Verified on a 12-planet G system (all presets, click-through, planet→system zoom-out) and a circumbinary brown-dwarf pair.
- **2026-08-24** — Milestone backlog cleared (every M0–M6 checkbox): galactic population mix feeding stellar age/metallicity; open clusters, OB associations, and nebulae in the sky (plus the galactic-bulge glow fix from live testing); photometric star sprites unified across all views; fluvial/glacial/dune erosion suites; asteroid close-ups as focusable streamed-terrain bodies; instanced boulder/ground-cover scatter; branded unit types at the orbital-mechanics boundary. Only M7 depth items remain.
- **2026-08-24** — Single-renderer step 3: the galaxy view retired as a separate renderer. The neighborhood (every sector star within 30 pc) is now 3D content of the unified scene under a pc-scaled group, photometrically identical to the backdrop stars it replaces but parallax-correct at every altitude; the backdrop carries only far shells and dust glow. Galaxy is a camera preset (focus star, 15 pc altitude, time frozen); the travel panel swaps systems inside the same viewer. Belt point clouds fade below sub-pixel size so distant systems don't bloom into false blobs; near/far planes scale into the interstellar regime. Verified: galaxy preset, neighbor travel (G dwarf → L6 brown dwarf), system→interstellar wheel-out, and the ground night sky retaining its near field.
- **2026-08-23** — M6 core landed: galactic density model with deterministic sectors, per-seed sky fields (resolved star shells + dust-reddened Milky Way glow) rendered as backdrops in every view with daylight washout on surfaces, and the galaxy view — the real 20 pc neighborhood, flyable, with click-to-travel to any neighbor system. 83 tests green. Open: population gradients into star properties, clusters/nebulae.
- **2026-08-25** — Travel became spatially honest: clicking a star now takes you to where it actually is. Destinations used to derive their locale by hashing the seed (always landing in the 5.2–12 kpc belt), so visiting a star near the core dropped you somewhere unrelated — invisible until the whole-galaxy view let you see your position. A catalog star's true galactic position now rides through travel (pick targets, the travel panel, the URL's `at=` param) into generateSystem: the destination's sky, neighborhood, galaxy view, and population mix are all built at the star's real place, and sky photometry draws each star's age from its true position (high-latitude stars now correctly skew old). Bare typed seeds keep the seed-derived locale as their address. 97 tests; browser-verified that the arrival locale matches the clicked star to 0.01 pc.
- **2026-08-25** — The band pointed the wrong way, and the whole-galaxy view caught it: the glow/rift maps are written with longitude in [0, 2π) but were sampled with an atan2 origin plus a half-turn — every interior sky's Milky Way band has been rotated 180° in galactic longitude since the glow map existed, its bright bulge end pointing anti-center, silently contradicting the far-star density gradient and the cloud sprites. Unnoticeable from inside; blatant the moment the volumetric galaxy rendered the truth beside it (reported from flight as "the angle is correct, but it's flipped" — exactly a longitude half-turn). One-line sampler fix; backdrop and volume now hand off seamlessly. Also: the far catalog stars are now true 3D points (direction × distance in the neighborhood frame) instead of a sky-sphere shell — parallax-correct at every altitude, they persist through the backdrop→volume crossfade (only the unresolved-glow representations swap), and hover/travel picks them at their real positions.
- **2026-08-25** — Moons rise and set: moon groups now carry the ground frame's diurnal sweep (spin only — the equatorial plane doesn't lean), so moons wheel across a fixed landscape like the sun and stars, lagging by their orbital rate, instead of hanging geostationary over one longitude with half of them below the horizon for weeks. The frame's spin sign flipped to prograde while wiring it: the sky had been sweeping opposite the planets' revolution, so every solid world was silently a retrograde rotator — now base spin matches the revolution (and the envelope planets' band direction), and genuinely retrograde worlds still emerge from obliquity > 90°, verified on a 150°-tilt rocky whose solar day correctly runs shorter than its sidereal day. The frame contract is documented in one place (frameQuat): new sky content parents under a frame group and is correct by construction. Considered and rejected: rotating the camera with the planet instead — a planet-radius terrain mesh spinning under float32 transforms jitters at the meter scale at ground zoom; static terrain is also what makes the planned surface free-camera easy.
- **2026-08-25** — Rings recovered on solid planets: since the unified body viewer, a terrain-focused planet's rings were added flat in the ecliptic — the very plane the sun moves in — so they sat at permanent equinox, edge-lit down to the shader's 5% floor, and read as lost (gas giants kept theirs because PlanetObject tilts its whole group). The fix models the cause: the ground frame now composes the planet's axial tilt with its spin, so the ecliptic — sun, planets, belts, sky — leans by the obliquity while rings and moons keep the equatorial plane, and seasonal sun declination emerges for free. Browser-verified on a 34°-tilt ringed rocky (ring lit and planet-occluded) with the icy-giant view unchanged. Remaining truth, not a bug: solid-planet rings are all faint dusty debris (τ ≤ 0.08) in the model, honestly near-invisible; the showpieces are the icy giants. Follow-up by request: a disclosed legibility lift in the ring shader (compressive optical-depth curve, softer illumination floor — the belt-glint marker-floor convention) so dusty rings read as a faint band, and ringed worlds now greet the camera from ~29° above the ring plane, since an in-plane arrival collapses any ring to a one-pixel sliver.
- **2026-08-25** — Real far starfield: the star catalog. A star's initial mass and population age now live in its seed (bit fields of the invertible mixed seed feeding the IMF and population-mixture inverse CDFs), and the galaxy's stellar field is realized as mass×age-stratified Poisson cells — fine cells for the faint bulk and the post-luminous slices, coarse cells for the rare bright young strata so the sky sweeps them to 2500 pc. The statistical far shells are deleted: every sky point is a real star whose seed travels, with hover showing its true spectral type and distance (a universe re-roll: all seeds keep their hex but resolve to new stars). 96 tests green; browser-verified hover identities across the sky and click-travel to a 313 pc B9V.
- **2026-08-26** — The interface became a survey console: a full-height sidebar replaces the floating info panel and bottom control strip, designed as a slate instrument — dim cool chrome with a teal accent kept below the sky's brightness, condensed-caps display type for designations and labels over mono measurements. Structure: a permanent sector-address eyebrow; a fixed catalog plate for the selected body (designation, a spectral strip smeared from the body's own model color, measurements, and the body stepper); a scrolling level table beneath it, under a sticky section bar cut like the address bar, listing the current level's contents as a real catalog (units in headers, right-aligned numerics) — companions, planets (rows click to focus), moons, or the nearest travel targets; and the four framing scales along the bottom, each annotated with the scale it frames (R☉ · AU · R⊕ · kpc). The instrument settings (seed, random, time, exposure) fold into a cog menu in the viewport's top-right corner, the chart toggle stands alone as a chip in the bottom-right, and time defaults to the slowest dial stop instead of per-view presets. The galaxy plate now reads out the full address (region, nearest arm, galactocentric radius, height); the travel table caps at the nearest 80 of tens of thousands. Browser-verified across all four levels: row-click planet focus, stepper, neighbor travel with honest at= URLs, chart toggle, no console errors.
- **2026-08-26** — Sky glints answer again at low altitude: since the far catalog went true-3D, most sky stars sit beyond the camera's far plane in the planet/star/system views (far floors at 75 pc; the catalog reaches 2500). The GPU kept drawing them — at those scales their float32 depth rounds to exactly the far plane, which three.js's less-equal depth test admits — but the CPU hover scan, computing the same projection in float64, saw z a hair past 1 and rejected them: the sky's brightest glints (distant giants, exactly the ones worth clicking) were visible yet unpickable. The pick scan now rejects only behind-the-camera, never the depth range, and the star-point shader clamps depth just inside the clip so beyond-far sky points draw by design instead of by rounding luck. Verified from the system and planet views: an F4V at 107 pc and a B8V at 273 pc hover and offer travel.
- **2026-08-26** — The sky chart became constellations. The complaint: you could see a named nebula in the sky, yet the sky chart didn't make it a region — the old layer partitioned the sky by which 3D province each sight line exited into, charting invisible adjacency while the visible landmarks went unhonored (probe: 0 of 7 visible nebulae anchored their own province; the brightest complex had a border through its face). The fix realigns the sky cut with the model's existing unification (province = cloud = nebula/rift, one seed, one name): buildConstellations partitions the celestial sphere by a salience-ranked, prominence-weighted angular Voronoi over the sprite-carried nebulae and rifts, reusing the lattice/marching/bisection border tracer (now cheaper — an angular argmax replaced the exit ray-march). Seating is self-consistent with the Voronoi cost: a landmark joins only if it would still win its own center against everyone seated, so the M78-in-Orion case falls out naturally. Tests assert every constellation label names a nebula or rift that same sky renders. Browser-verified at seed 78e011100ad27e30: the great Marusyaveim complex wears its own name, borders wrapping around it instead of cutting through, with the home province's namesake cloud (Syavelaekryak) lettering its own patch of the home sky.
- **2026-08-26** — The sky's extended objects are hoverable in practice, not just in principle: the star hover snapped from a shared 26 px radius (~17 px effective per glint), which in a catalog sky blankets nearly every pixel — the containment-only nebula/rift pass almost never ran. Point stars now snap only within 10 px (STAR_SNAP_PX; solid pickables keep their generous reach), so the space between glints reaches the clouds. And a nebula's face is dense with its own coeval members — physically honest, but they are anonymous and unclickable, and they smothered the very object they light: an unseeded cluster member now yields to a cloud that contains the cursor, standing only where no cloud claims it, while every seeded star keeps its priority. Browser-verified: the Marusyaveim complex's face hovers as "the Marusyaveim Nebula", matching its constellation, and neighboring glints still snap and offer travel.
- **2026-08-26** — Spin got its physics (a universe re-roll: generator draw sequences changed). Stars now carry a full spin axis — convective envelopes (the cool side of the Kraft break) tidally realigned to ~9° scatter, radiative stars looser, 3% wildly misaligned up to retrograde — rendered through the photosphere's orientation and read out on the plate. Activity quenches with photospheric ionization: below ~2400 K the atmosphere goes neutral and lets go of the field, so late-M/L/T objects lose their spots and flares for the physical reason, and the L/T transition carries patchy condensate clouds instead (a T6 now shows clouds, not starspots). Planet spin split by family: gas-rich worlds (giants and mini-Neptunes) spin fast, prograde, nearly upright with rare resonance-walked poles; terrestrials remember their last giant impacts with isotropic obliquity — retrograde as often as not; and near the outer edge of the despin zone thermal tides in a thick atmosphere can stall the lock into a Venus state — slow, nearly upside-down, day running backwards against its year (~2% of planets). Captured irregular moons (60% retrograde, always in the model) now render and list with their retrograde-capture notes instead of being filtered out beyond 100 planet radii. Verified in-browser: a T6 with 15% patchy clouds and no spots, a rocky HZ Venus-state world at 19.3 d retrograde under 1.13 bar of N₂, an M5V spinning backwards at 120° tilt. Deferred: asteroid spin axes in the ground frame (existing roadmap item), Mercury-style 3:2 capture never fires because close-in eccentricities damp below its 0.15 gate (pre-existing; needs an eccentricity-history-aware gate).
- **2026-08-26** — Wide binary companions exist again: setSystem skipped any companion beyond 3× the planet extent entirely — no photosphere, no glint — so a binary's partner at tens or hundreds of AU (brighter than magnitude −10 from anywhere in the system) simply wasn't in the sky, even while its truncation shaped the planet system. Every companion now gets its real photosphere and photometric sprite (only the orbit line stays map-scaled), and companions are hoverable in every view, identifying as "companion · click for star view". Companion/binary audit findings logged for follow-up: the second sun of a P-type pair casts no light on planets or terrain (climate already counts it; rendering lights from the primary only); distant binaries in the sky render as single primary-luminosity glints; companions are not separately focusable or travelable.
- **2026-08-26** — The second sun casts light: every lit shader (solid and banded planets, atmosphere limbs and ground skies, terrain, ocean, clouds, rings, scattered rocks) takes a second light direction and flux-premultiplied color, and the viewer feeds it each frame from whichever companion delivers the strongest flux — at the focus for the ground and moons, per-body for the other planets. Below a 0.4% flux threshold a companion stays a glint, not a light, so single-star systems pay nothing. Circumbinary worlds now get double terminators and their nights lit by the second sun; body shadows (moon transits) block both lights through the shared analytic occluders. Known simplifications, disclosed: star-on-star eclipse dimming is not modeled (occluder lists carry bodies only), and specular highlights come from the primary alone. Verified on an A8V+F3V circumbinary mini-Neptune — both photospheres in frame, second-light uniforms live at an 11.7° separation.
- **2026-08-26** — Binaries glint with their whole light: sky-catalog and neighborhood photometry now add unresolved companion luminosity — the same multiplicity draws generateStar would make, each companion evolved at the system age — so a binary's glint is up to ~0.75 mag brighter than its primary alone, matching what a traveler finds on arrival. Color stays the primary's (disclosed approximation); the sky build pays ~5% for it. The far-catalog acceptance floor still tests the primary's luminosity alone (a pair whose members each sit under the floor stays unlisted — edge case, disclosed). Remaining companion-audit item, deferred: focusing/orbiting a companion star directly (clicking one gives the primary's star view).
- **2026-08-26** — Companion stars became destinations: the star level's table is now "System stars" — primary first, then companions, the focused row marked — and clicking a row (or a star in the viewport) focuses that star: its own catalog plate, its photosphere at the origin, arrival orbit scaled to its radius, and a `companion=` URL param so the address survives reload. The focus machinery gained a companion target ({ companion: i } in FocusTarget); positions ride the same per-frame Kepler solutions the star nodes already use. Travel to a new system resets focus to the primary.
- **2026-08-26** — Chart lettering sinks to the back and bodies block the hover. The label sprites drew with depth-testing off at renderOrder 5 — names floated over planets and photospheres — and wore a generic monospace; they now render in the console's own condensed display face (tracked caps at 2x resolution), depth-tested so any body occludes them, and ordered with the sky domes so stars, borders, and nebulae all paint over the names, never the reverse. Picking got the same honesty: the hover previously tested occlusion only against the focus body and only for solid pickables and neighborhood stars, so far-catalog glints, and the nebula/rift pass, hovered straight through planets and stellar discs. Every star, planet, and moon in the scene now registers as a sphere occluder each frame; all four hover passes test against the list, and the extended-object pass tests the cursor's own sight line out past the whole scene (its first version tested a point at a quarter of the far plane — short of the photosphere in close views, found by hovering a star that still introduced the nebula behind it). Browser-verified in an isolated worktree (the working tree carried another session's in-flight edits): hovering the disc shows nothing, off-disc glints still travel, the open-sky nebula hover still answers.
- **2026-08-26** — Companions became systems, not just stars. Model: the planetary-formation pipeline (disk → layout → elements → stability → characterization → belts) extracted into a per-host core, and every stellar companion now runs it around itself — truncated by the primary's tide (Holman–Wiegert S-type with roles swapped), planets named for their host, coeval and deterministic from the companion's own seed. A close P-type pair's partner hosts nothing (the circumbinary planets are the pair's system). Primary systems are byte-identical (fork streams untouched — no re-roll). Viewer: each companion family rides its star as a group — zone rings, orbit lines, true-scale planet spheres, markers — lit by its own sun with any other bright star as the second light; family framing gives the system preset a per-host extent; hosted planets are pickable, focusable ({ companion, planet } focus targets), and arrive facing their own sun. UI: the current star (companionIndex, in the URL) scopes the system and planet levels, so the system table shows the K8V's seven worlds and its debris belt, rows click to visit, and the star table marks the focused star with a ring around its dot instead of an edge bar. Deferred, disclosed: companion belts render only as panel data (no belt points/streamers), comets remain the primary's, and companion-system planets skip the notable-asteroid stepper.
- **2026-08-26** — Course correction on companion rendering: no parallel pipeline. The per-companion render groups are gone; instead the ONE system pipeline (planet nodes, belt points, zone rings, orbit lines, belt streamer) re-roots on the selected star via viewer.setHost — the chart groups and every orbit solution simply ride the host's position, so selecting the K8V renders its system exactly the way the primary's renders (its belts now render too, for free), and nothing else's. Unselected companions are what you actually see at that scale: their photospheres, plus — new — their stellar orbit ellipses drawn primary-anchored like planet orbits, visible whenever the orbit map is. Focus targets simplified back to 'star' | number; the host rides the companion= URL param.
- **2026-08-26** — Stars join the gazetteer: hex designations give way to chartable names, layered the way human astronomy layers its own. The luminous few (L ≥ 10 L☉ — giants and hot stars, exactly the far catalog's bright hoverable tail) carry proper names drawn from the gazetteer's phonology, so sectors, nebulae, rifts, and bright stars all sound like one civilization charted them; the bulk file into their sector's catalog by number, Gliese-style ("Syavelaekryak 87451"). Bayer-style constellation names were considered and rejected as identity: constellations here are viewpoint artifacts, so "α Marusyaveim" would name a different star from every world. One pure function (starDesignation in star/naming.ts) serves the generator and both hover paths; components letter as B, C off the primary's name, and planets ("… b"), moons ("… c II"), comets ("…/C1"), and notable asteroids all inherit through star.designation untouched. Names are designations, not identities — the seed stays the id (a great sector can eventually repeat a catalog number, as surveys do), kept visible as a Survey id row in the star plate. Browser-verified: the travel table reads as a survey of the home province, a K2III glint hovers as "Naemiaoraer", and the plate titles "Syavelaekryak 87451 · M8V".
- **2026-08-26** — The debris-belt glow was fake and is gone: belt cloud points had a flat painted brightness whose additive stacking built a bright ring at system zoom — a structure reflecting ~10⁻¹³ of its star's light, invisible in reality (real debris disks are 10⁻⁴–10⁻⁶ contrast, coronagraph territory). Each point now carries the honest reflected-light luminosity of the large member it represents (~40 km at 10% albedo, the streamer's own formula) through the same photometric energy mapping as every star glint, with no marker floor — the belt disappears from afar and individual glints only surface as the camera closes in, handing off to the belt streamer's real rocks. Zoomed-out legibility moves to the chart where it belongs: a faint map-ink annulus alongside the zone rings.
- **2026-08-26** — The naming follow-ups: travel-table designations trim their sector prefix to three letters the way atlases write "61 Cyg" ("Sya O7G8" in the table, full name in plates and hovers; proper names stand whole), and the Bayer garnish landed — within each constellation of the current sky, the brightest glint with a seed of its own is its α, computed in the sky worker from the same partition that draws the borders and carried as a seed-keyed map into both hover paths. Viewpoint-local by nature, exactly as Earth's Bayer names are: every home system letters its own α stars. Browser-verified: the K2III giant near home hovers as "Naemiaoraer · α Syavelaekryak · K2III · 5.63 pc" — a proper name over a Bayer line, the Sirius/α CMa layering.
- **2026-08-26** — M8 S1 Boots: the wheel ride now ends on your feet. At the descent floor over wadable ground, one more scroll notch starts a touchdown glide to a 1.7 m eye height (streaming catches up on the way down); then WASD walks, ShiftLeft runs, Space jumps, click takes pointer-lock mouse-look, and a scroll notch out glides back to the orbit regime, which resumes untouched. Physics is honest and runs on the same analytic height field the meshes are built from: gravity is the body's own g with inverse-square falloff (a fixed human takeoff speed makes jump height emerge — 0.51 m on the 0.93 g Earth-twin, and on a kilometer asteroid a jump genuinely escapes, handing the camera back to orbit), grades above 42° refuse to climb, chest-deep water stops the walker, walking off an edge falls with carried momentum. Asteroids walk too, with g from taxonomy densities (new asteroidGravityMs2). Deep open water refuses to land, with the hint chip saying why; the near plane drops to centimeters on foot (mid-distance depth precision deferred to S2 hardening, disclosed). Six walker physics tests (108 total green); browser-verified on the Earth-twin — landing glide to exactly 1.700 m, 1.70/5.5 m/s paces, jump peak 0.513 m vs 0.526 m analytic, a 108 m run inland from a daylit surf line with zero eye-height error, liftoff back to the 50 m floor.
- **2026-08-26** — S2 lands its core: the ground is real at eye height. The detail cascade grows four bands (~30 m to ~24 cm features, amplitudes on the same ~1/f law), gated per substrate — sand seas, sediment lowlands and drowned floors, and ice read smooth at walking scale while highlands and airless regolith keep their rubble; all honestly derived (erg field, height-vs-relief, freeze mask). The streamer descends five more levels (MAX_LEVEL 22 ≈ 6 cm vertices) — which exposed a latent LOD bug: split distances measured to the datum sphere, so land riding hundreds of meters above datum stalled the quadtree that far short of the walker (level 16 forever); distances now measure to the local ground sphere. Geomorphing kills the swap pops without timers: every vertex carries its height above the parent-LOD surface, and the shader removes it as a function of camera distance, hitting zero exactly at the swap-in distance — a tile replacing its parent is byte-identical to it, detail grows in on approach, and adjacent LOD rings agree at their boundary by the same rule. The walker's feet low-pass the new texture (bands under a stride are stepped over, not bobbed across). Two new property tests (walked-scale texture invisible to coarse LOD; morph deltas reproduce the parent surface exactly), 110 green. Browser-verified on the Earth-twin shore: level-22 tiles underfoot, 2.7 ms frames, zero eye-height error running, the jump finally reads against sub-meter ground, and the 2,400 m highland came out snow — the lapse rate said so. Still open in S2: sub-geometry normal detail (planet-frame f32 blocks fragment noise past ~80 m wavelengths — needs chunk-local frames), velocity-aware stream priority, mid-distance depth precision (log-depth/reversed-z).
- **2026-08-26** — S4's keystone lands: water finds its way. Rivers stop being painted noise and become model objects — a global cube-sphere grid (98k cells, with seam-crossing adjacency built by uv-overstep re-projection, no hand-coded edge tables) routes every land cell to the sea by priority-flood, so depressions fill to their spill level (that's where lakes live), runoff accumulates downstream into per-cell mean discharge (placeholder latitude-curve precipitation until S5; arid eroded worlds route their paleo-discharge — the erosion parameter encodes the wet history that shaped them), and the cell→downstream segments are the river network. The graph carves the field: each nearby segment cuts a discharge-scaled valley (hydraulic-geometry widths, stream-power-flavored depth that digs hardest in highlands and grades to the sea) with a channel notch, yielding to the freeze mask; a seeded meander warp bends the straight cell-to-cell course at meander-belt scale (the first amplitude re-routed rivers 45 km — probe-caught, tightened to ~6 km). Line features fade by width but surface at half a sample, so continental rivers trace from orbit. The painted valleyRidge/tributaryRidge carving is deleted. Emergent bonus: near the coast the channel digs below sea level and the existing sea-surface tiles flood it — real estuary water with zero new rendering. One graph will feed the terrain, the orbital traces, and S4-rendering's water. 7 new grid/graph property tests (117 green); tsx-verified on the Earth twin: 6,724 river cells, max discharge 52,000 m³/s, and a 35,000 m³/s river crossing a clean 7 km-wide, 264 m-deep valley profile graded to 10 m above sea.
- **2026-08-26** — S3 lands the lay of the land, conditioned by the graph. Plates now ride at their own elevations (Worley gains id1, a stable per-cell identity): crossing a boundary steps — the fault scarp is the discontinuity itself, degraded by erosion and often buried under the belt's own ridges. River valleys grow gully networks on their flanks — ravines that strengthen toward the river and die at the divide shoulder, the sub-cell dendritic texture the 87 km graph can't carry itself. Waves work every shore: a flat beach and shallow shoreface in a ±6 m band about sea level (inactive until the level is solved, same closure trick as the rivers). Steep faces expose rock with a history: volcanic provinces show dark basalts, the rest gets elevation-keyed sedimentary bedding (seeded 35–115 m thickness, fading where vertex spacing would alias it to speckle). Performance en route: the crater field's string-keyed cache was the single hottest cost of the whole surface (54 lookups per height sample) — packed-integer keys plus merged-neighborhood caching cut heightAt 22.6→~13 µs and the test suite 16→7 s. 118 green (new beach-slope property test); strata banding probe-verified at the seeded period. Open: talus at angle of repose (nonlocal — needs its own model), wave-cut cliff vs beach differentiation by exposure, tidal flats from the moons, field creation still synchronous on the main thread (~1.4 s on wet worlds at focus).
- **2026-08-26** — Water renders to scale: rivers and lakes join the sea. The graph now computes a graded bed profile per cell — monotone downstream, using the same depth law the carve applies, so the two always agree — and the channel floor blends to it absolutely instead of carving relative depth, which means the water that fills it steps downhill reach by reach instead of stranding on band noise. Stage rides the bed at hydraulic-geometry depth with its own monotone clamp, and that clamp IS the lake model: where beds dip below the downstream-controlled level the stage flattens into a pool, so lakes emerge at the level of their breached outlet — a big river cuts through a basin rim that a small creek fills behind (the first version filled coarse-grid depressions to their raw spill and drowned a valley under 170 m of false lake; probe-caught). Water tiles take per-vertex levels (sea, lake fill, or river stage; dry vertices tuck under their terrain), the walker wades by the local water surface instead of the sea alone, landing refuses deep rivers and lakes too, and the camera floor rides lake surfaces. 120 tests green (graded-stage monotonicity; an emergent-lake property test with a rimmed basin). Browser-verified: a 9,400 m³/s river carries 1.9 m of water between sandy banks at eye height and winds as a sinuous blue ribbon from 5 km up. Still open in S4: distinct waterfalls at knickpoints, deltas and alluvial fans at outlets, flow animation on river surfaces.
- **2026-08-26** — S5's core lands: weather becomes a map. A climate field on the same global grid the drainage uses: zonal circulation bands from rotation (trades / westerlies / polar easterlies for fast rotators, one sluggish cell for slow ones) set a wind per cell, oceans evaporate by their local temperature (Clausius-Clapeyron-flavored), and moisture advects downwind to a fixpoint — raining a base fraction per cell plus a forced-ascent term. Nothing painted, everything emergent: wet windward coasts, rain shadows behind ranges, interiors that dry with fetch, polar deserts. The drainage graph now drinks the real precipitation field instead of its placeholder latitude curve, so rivers gather where the rain actually falls, and the biosphere's ground colors read moisture from the field — deserts sit where the shadows are. The Earth twin's headwater cells now span 0 mm/yr (true rain-shadow desert) to ~3,000 (soaked windward coasts), median 276. 123 tests green: rain-shadow and continental-drying property tests on synthetic worlds (a meridional wall with identical flanks separates them by wind alone), plus determinism. Browser-verified from orbit: tan desert interiors with green wet fringes where uniform green used to be. Open in S5: temperature/snowline as first-class field outputs with maritime moderation, soil-moisture coupling to the ravine/substrate gates, and formal Whittaker classes (arriving with S6's plant communities).
- **2026-08-26** — S6's core lands: the ground stands up. Tree species are model objects (universe/surface/flora.ts): two per biosphere world, seeded per species — conifer or broadleaf habit, trunk height, canopy spread and taper, blob count — with pigments derived from the world's own land palette, which already carries the star's spectrum. The renderer grows low-poly geometry from the recipe (bark and canopy colors baked per vertex, instances tinted by a neutral tone; the scatter material learned vertex colors). Placement rides the climate field: tree probability follows local precipitation through the same deterministic per-tile scatter streams as the rocks, so forests close in on wet coasts, thin to lone trees at the desert edge, and vanish in the shadows — the biome, standing up. Browser-verified in a rainforest cell: 9,400 tree instances drawn across the visible tiles, walked under at eye height. 124 tests green (species determinism + forests-in-the-rain property). Open in S6: undergrowth instancing near the camera, impostor mid-LOD and true canopy-in-terrain-color far LOD, wind sway, tree collision for the walker, and a skylight ambient term so canopy undersides stop reading black.
- **2026-08-26** — The spectrum pass, on the m8-human-scale branch (all M8 implementation moved here off main pending feel-approval): the mid-scale was starved. Between ~100 m and ~25 km wavelengths — where Earth's feel lives — amplitudes gave 0.3–1% slopes against Earth's 5–20%, tuned once for orbital smoothness; that was the "vast spans of not much going on." The falloff now runs much flatter through the middle (25 km band 40→160 m, 4 km 12→79 m, 790 m 4→30 m, 158 m 1.2→9.4 m, down to 24 cm features at 5 cm), landform bands ridge-blend along noise zero-sets into connected ridge-valley systems (free — same noise eval, shaped), and two vector warp fields (~120 km and ~3 km) shear the band domains so hills sweep and flow instead of sitting isotropic. The LOD fade divisor relaxes 4→2.5 — the geomorph absorbs transitions now, so detail arrives a level earlier and the finest band actually shows at max LOD. Land slope medians after: 18% at 5 m, 11% at 30 m, 5.9% at 200 m, 2.6% at 1 km. The beach test was rebuilt on medians — some coasts are honest wave-cut cliffs and should stay steep, and its old inland control sampled underwater. 124 green. Eye-height verified: hummocky flowing ridge-and-swale ground with shaded hollows where a featureless sheet used to be. Known limit for the from-altitude read: 2–6% plains under a high sun are physically subtle without cast shadows — terrain shadow mapping is the next legibility lever and its own feature.
- **2026-08-26** — Course correction on the spectrum, per feel-test: resolution, not exaggeration. The raised amplitudes made 0.7 m hummocks every 5 m — a half-meter jump cleared the terrain sea and read as hundreds of meters of altitude. Amplitudes recalibrated to Earth-reference relief per wavelength (steppe ~1 m over 30 m, ~15 m over 800 m, ~50 m over 4 km; 5 m median slope 9.3%, 30 m 6.4%, 200 m 3.5%), keeping the ridge structure and warp flow. The resolution budget went where it belongs: tile meshes densify 48→64 vertices per edge — ~4 cm spacing at the walking floor and a sharper mid-distance error angle at every LOD ring (chunk cap trimmed to keep GPU memory in its old envelope). Two tests rebuilt on the way: the walked-texture property moved to second differences (mid-band gradients now register at every LOD, so first differences no longer separate the fine bands) and onto the lightly-cratered fixture (crater walls survive all LODs by design). 124 green; eye-height verified — fine-grained ground over rolling relief, jumps back to human scale.
- **2026-08-26** — Ride out: a chip beside the chart toggle starts a hands-free pull-back from wherever the camera stands — a planet's ground included — to the galaxy frame. It drives the existing wheel ride rather than a separate camera path: each frame multiplies the pending wheel factor at a constant pace in scale decades (0.6/s — ground to galaxy ~28 s, a system view about half), so every crossfade, focus rule, floor, and ceiling behaves exactly as manual zooming does, and orbit drag still steers mid-flight. The wheel, a travel, or a second press takes the ride back; it ends itself at the altitude ceiling and the chip clears. From a walking start the first push clears the walker's liftoff threshold explicitly, whatever the frame rate. Browser-verified from a planet's face: terrain → orbit lines → constellations → the charted bubble adrift in the galaxy disk, chip active for the duration and self-clearing at the top.
- **2026-08-26** — The non-Earth audit: the spectrum had only ever been judged on the wet worlds, and everywhere erosion was low it was catastrophically wrong — airless cratered moons ran 900–5700% *median* slopes at a 5 m step (a 35 km moon carried the full 26 km relief cap), arid and icy dead worlds 110–130%, magma worlds 65%, against the real Moon's sub-10%. The model's only smoother was fluvial erosion, so airless worlds kept full fractal roughness to sub-meter scales; reality mantles every old surface. Mantling is now first-class: each detail band is capped at the grade its wavelength can hold — bedrock repose on fresh surfaces easing to a mantled ~10% below a gardening wavelength that grows to basin scale (200 m → 20 km) with crater age, since Gyr of impact gardening relax relief out to the megaregolith's kilometers; young unmantled crust (fresh volcanics, actively resurfacing airless worlds) deliberately keeps blocky 25–45% fine texture. Crater populations get degradation states (later impacts garden earlier bowls toward flat) instead of rendering every bowl fresh, and relief is capped at 8% of the body radius — beyond that it's shape, not terrain. Wet and hothouse worlds are numerically untouched (every tuned Earth-twin band clears its cap; probe medians byte-identical), and the archetype probe now sweeps living/dead-ocean, hothouse, arid, ice, magma, and airless worlds across four system seeds. 124 green. Browser-verified: a Pluto-amber cratered ice world walks as rolling mantled regolith under a rugged ridge horizon with visibly varied crater degradation from orbit; a young airless stagnant-lid world stays knobbly and harsh by design; the Earth twin is pixel-for-pixel the accepted steppe.
- **2026-08-26** — Shipped to the web: a GitHub Pages workflow builds main (tests, then vite build at /universe/) and deploys dist; the stylesheet moved from a JS import to a head link, deleting the flash of unstyled chips before the app module loads; and a first-visit welcome card introduces the survey — one deterministic seed, real physics, the sky as the model, the gazetteer — with an Explore button, dismissed once per browser. Verified against the built bundle via vite preview at the Pages base path: workers load, the dialog greets, dismisses, and stays dismissed.
- **2026-08-27** — Flight replaces walking, moons take focus, and moonlight arrives. The ground regime is a camera now, not a body: one notch past the wheel floor hands over to free flight (W flies along the gaze, A/D strafe, Space/C rise and dive, Shift boosts, one notch out leaves), with speed scaling by height above terrain — valley floors at walking pace, ranges in a keystroke — and a hard 2 m clamp over ground and water both, so open ocean stopped refusing visitors. Moons are destinations: ?planet=N&moon=M, a click on a moon in the sky, or the (now clickable) moon list all land the same terrain streamer on the moon, with the whole moon system shifted so the focus sits at the origin — the parent planet hangs at its true offset with rings and phases, and a tidally locked moon holds it fixed in the sky by construction. And the night has light: the brightest sunlit body in the sky becomes the surface's second sun with direction, phase, and flux from true geometry (albedo × (R/d)² × Lambert phase — ~2e-6 of sunlight for a full-Moon analog), displayed through a scotopic lift (ratio^0.2) that fades in as the sun sets, disclosed as eye adaptation. Browser-verified on a found Earth-analog pairing (seed 00000000000b8ef5 p1: 19.5 h day, 80% ocean, biosphere, a 0.7°-wide moon at 3× full-Moon flux): amber gibbous disc among the stars, moonlit ground texture under a clear night, a moonlit overcast deck glowing from above, daylight untouched; and on its arid sibling's moon, the parent super-earth hanging over the horizon. 131 tests green. Eclipse shadowing of moonlight (a moon in its planet's shadow still shines) is a known open item.
- **2026-08-27** — Travel desync fixed (user-reported: "you don't go/view what you clicked"). Two bugs compounding: clearSystem duplicated clearHostContent's disposal without resetting hostIndex, so setHost's change guard skipped rebuilding the destination system — planet clicks focused a notable asteroid out of an empty planet list, and star focus sized the camera off the previous star (once parking the view *inside* a 258 R☉ giant's photosphere, which read as "teleported but can't see it"). And the galactic locale — part of a seed's generative identity — was dropped on any same-seed reload (tab switches, body steps), silently regenerating a different star at the seed-default locale, which is why the info panel and renderer told different stories until a page reload restored the &at= URL param. Host teardown now goes through clearHostContent, and same-seed reloads keep the current locale. Browser-verified on the reporter's exact repro: random seed → click sky star → System tab → click planet → the ringed gas giant on screen is the one on the plate.
- **2026-08-27** — Follow-up to the travel fix (user: "orbit markers didn't load after travel"): not staleness this time — the orbit overlay's map-heights gate (25 stellar radii) is unreachable inside a giant star's own system, and the luminous travel-tail of the sky is mostly giants, so every traveled-to giant hid its orbit map. Gate now capped at half the system extent; verified on the same 258 R☉ system (zones, belts, orbit all surface) with dwarf systems pixel-identical. Swept the other radius-scaled gates (horizon gaze, pan availability, near plane, rotate speed) — all safe on giants.
- **2026-08-27** — M8 merged to main and pushed; rocky worlds signed off. Next pass planned: gas giants ([plan](plan/10-gas-giants.md)) — the atmosphere becomes a first-class circulation model (jets from the Rhines scale, bands with per-jet drift and shear turbulence, a deterministic storm catalog with lifecycles, polar cyclone regimes, aurora from the magnetic model, locked/quiescent regimes for hot Jupiters and ice giants) and the shader becomes its renderer, replacing today's eight-number sine-stripe banding.
- **2026-08-27** — The gas-giant pass, G1–G6 on the gas-giants branch ([plan](plan/10-gas-giants.md)): giants are circulation, not wallpaper. deriveCirculation runs a one-shot PV-mixing spin-up per planet — seeded convective stirring homogenizes potential vorticity in patches one local Rhines length wide (eddy-velocity closure: the arrest scale, which is why feeble interiors like Uranus really carve many narrow faint bands), with radiative restoration so the staircase equilibrates, a gauged inversion for the wind profile, mixing efficiency scaled to the forcing, and a deep equatorial jet the flow relaxes toward (prograde warm, retrograde cold — the solar system's split). Bands classify zone/belt from their shear; a chromophore draw gives each planet its own palette; storms are recurring catalog slots whose live population is a pure function of (seed, t); poles carry cyclone clusters, hoods, and seeded hexagon-analog waves; aurora reads the dynamo. The shader renders these as uniforms: per-band drift (bands genuinely shear past each other in sim time), festoons boiling where the model says the shear is, churn at the convective decorrelation rate, sub-band striping for the physics' surplus bands, aged storm ovals, the polar regime, tilted-dipole auroral ovals on the night limb, thermal glow, and a locked regime with superrotation-shifted hotspot and west-terminator cloud crescent. Focused envelopes take their moons as eclipse casters — transit shadows crawl the deck. 142 tests green. Browser-verified: a Jupiter-class ammonia giant with irregular zones/belts, festooned edges, polar aurora, and visible deck evolution at 0.5 d/s; a 999 K giant with alkali bands and thermal rim; a locked pale streaked mini-neptune; a teal methane world whose pole-on night side shows the auroral oval as a glowing ring; the full banded parent with rings hanging in a volcanic moon's sky. Open: visual confirmation of polar cyclone vortices and a live moon transit; fine-band striping tuning on cold worlds.
- **2026-08-27** — Century weather, per feel-test ("are we simulating the large century-scale storms?"): the spot analog was eternal and static — now it runs a century arc (swell fast, shrink through a long maturity as the GRS has, pale, die, successor at a rehashed latitude), condensable-rich giants erupt planet-circling white storms once per orbit (the true year, plumbed from the elements — Saturn's Great White Spot habit), and belts run decadal fade-and-revival cycles under fresh white deck (the SEB's). All pure functions of (seed, t). Also from the same feel-test round: the deck became a stirred tracer — multi-scale eddy displacement of the band lookup itself, so belts curl into zones and band widths wander with longitude; boundaries feather at a width set by their jet's shear (strong jets hold crisp fronts). 145 tests green; browser-verified a mature faded spot riding a stirred band on a white-dwarf system's ringed giant.
- **2026-08-27** — Depth, per feel-test ("flat and 2D"): the deck gained a cloud-top height field inherited through the color (bright decks high, belts deep, storm heads towering, cyclones sunk), bump-lit via screen derivatives so the terminator rakes across real relief, plus a slow-drifting high haze that thickens with the slant path toward the limb. The giant reads as a ball of weather with a floor and a ceiling. 145 green.
- **2026-08-27** — Polar fix, per feel-test ("central dot with smaller dots around it, like indents"): the cyclone gaussians — embossed into dimples by the relief lighting — became spiral-armed vortices with individual phases and drift rates, and the cap keeps its weather, shearing the deck into a converging polar spiral instead of airbrushing it flat under the hood. Pole-on now reads as Juno saw it: streaked convergence, a crowd of small cyclones ringing the central one, the auroral oval around everything. 145 green.
